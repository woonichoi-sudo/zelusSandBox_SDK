# Cobalt 프로젝트 auto-memory 전문 (원문 그대로)

출처: `C:\Users\wooni\.claude\projects\d--z-emotion-Cobalt\memory\`
추출일: 2026-08-13 · 파일 10개(MEMORY.md 색인 + 메모리 9개) 전부 — 요약·생략 없음

각 절은 해당 메모리 파일의 frontmatter까지 포함한 원문 전체다.

---

# MEMORY.md

- [웹 서비스화 진행 상황](web-service-progress.md) — **★ setFabric(#50) + H-1 스냅샷 제거 완료 (2026-08-13, `74ae463`·`bb5e084`·`cce6275`).** 스모크 102/431/**942** 전부 초록, E그룹 6/6, 열린 작업 없음. ⚠️ **Bash 에서 `node -e` 와 `npm run smoke` 가 조용히 어긋난다 — PowerShell 로 돌려라.** ⚠️ **개발 확인은 `:5173`(Vite)이다** — `:3000`은 게이트웨이가 서빙하는 옛 `dist`라 화면이 과거로 보인다. 세션·워커·로드 **횟수를 잴 때만** `:3000`. ⛔ `verify:ui`는 GPU 이슈로 보류 — 맨 위 "▶ 다음 세션이 바로 할 일"부터 읽을 것
- [작업 범위를 벗어나지 마라](stay-in-scope.md) — 딴 결함을 발견하면 **관련성만 판정하고 멈춰라.** 원인 파기는 물은 뒤에. 2026-08-12에 UI 작업 중 파다 제지당했다
- [회사 코드는 1회성 CLI다](company-code-context-mismatch.md) — 저쪽에서 맞던 코드가 **상주 워커에서는 조용히 틀린 동작**이 된다. 2026-08-11에 네 자리에서 걸렸고 팔이 18.7cm 사라졌다. 회사 코드를 옮길 때마다 읽을 것
- [스크린샷 말고 소스를 읽어라](read-the-source-not-the-screenshot.md) — 참고 이미지를 명세로 받으면 **데스크톱의 그리는 코드**(`zelusSandBox/*Renderer*.cpp`)를 먼저 열 것. 눈으로 흉내내다 2026-08-11에 두 번 틀렸다
- [이 머신은 불안정하다](machine-instability.md) — PC 먹통 2회, 프로세스가 `0xC0000005`로 죽는 계열 넷. **테스트는 한 번에 하나씩**
- [서브에이전트 작업 루프](subagent-workflow.md) — Builder → Tester → **화면 확인(verify:ui)** → 커밋 → Updater
- [회사 저장소는 읽기 전용](readonly-company-repo.md) — zelusSandBox_Cobalt 무수정. 빌드 도구가 몰래 쓰는 경우가 있어 전후 확인 필요
- [사용자 소통 방식](user-comm-style.md) — 짧은 답 선호, 작업 전에 무엇을 할지 먼저 알릴 것
- [개발 원칙: 작은 단위 + 테스트 + 커밋](dev-principle-tdd-increments.md) — 한 덩어리로 만들지 말 것. 테스트는 기존 smoke.ts 스타일


---

# web-service-progress

> memory/web-service-progress.md

---
name: web-service-progress
description: "zelusSandBox_Cobalt 웹 서비스화 진행 상황 — 2026-08-12 액세서리·서랍 UI·텍스처 캐시 수정까지 끝났다. 다음은 Tester(자동 단언이 셋 다 없다)와 ISSUE-025"
metadata:
  node_type: memory
  type: project
  originSessionId: 8278d176-8564-49b7-a189-aff124b9add5
  modified: 2026-08-13T06:14:21.968Z
---

2026-08-10 기준. **작업 목록과 상태는 저장소의 `TASKS.json`이 정본이다** — 16개 단위가
전부 done이고 남은 것은 `backlog`(A~E 그룹)다. 발견된 문제는 `ISSUES.md`(18건).
설계는 `DESIGN.md`, 빌드 함정은 `CLAUDE.md`, 전수 조사는 `PROJECT_ANALYSIS.md`,
**손으로 돌려 보는 법은 `SDK_USAGE.md`**(2026-08-10 신설).

여기에는 그 문서들에 없는 맥락만 적는다. 머신 문제는 [[machine-instability]]로 뺐다.

## 손으로 돌려 보기 (2026-08-10, `2de4c0c`·`44b2dfc`)

**`SDK_USAGE.md`가 사용법의 정본이다** — 네 가지 길(예제 스크립트 / 직접 코드 /
워커 날것 / 웹 화면) + 실측으로 확인된 함정 모음. 사용자가 직접 테스트하고 싶다고
해서 만들었다.

```bash
cd backend && node --experimental-strip-types tools/try-sdk.ts
#   --scene bra|sample   --frames <n>
```

`tools/try-sdk.ts`는 **테스트가 아니다.** 단언으로 회귀를 잡는 것은 `smoke.ts`이고,
이건 값을 사람이 읽으라고 찍는다. 한 번 돌리면 이번 주 작업이 한 화면에 나온다 —
패턴별 색, 직물 그룹(24패턴 → 2직물), 2D 배치 좌표, glTF 익스포트.

**브라우저 업로드**: 상단 [파일 선택] → [업로드] → [로드]. 상한 512MB,
`backend/data/scenes/`에 남는다. ⚠️ **업로드 경로는 UI 하네스가 안 덮는다.**

## ▶ 다음 세션이 바로 할 일 (2026-08-13 갱신)

**열린 작업이 없다.** 다국어 · Tester · ISSUE-025 · 문서정리 · ISSUE-021 ·
setFabric(UI #50) · **H-1 스냅샷 제거**가 차례로 끝났다(`27ea33a`…`cce6275`).
스모크 **102 / 431 / 942** 전부 초록.

### ★★ H-1 — 기능을 처음으로 **뺐다** (`bb5e084` + `cce6275`)

사용자가 *"스냅샷 기능은 필요없지않나"*로 판정했다. **데스크톱에 없는 버튼을
우리가 넣었던 것**이고(회사 코드 전체에 `snapshot` 0건, 데스크톱엔 기능 #10
`Export gltf`만 있다), 생긴 이유(실시간 뷰에 아바타·색이 없었다)는 E그룹 6/6
완료로 사라졌다. **A안** — 화면 진입점만 내리고 모듈·스모크 87건은 남겼다
(익스포트 경로를 자동 검사하는 유일한 자리이고 ISSUE-012·013이 열려 있다).

- **★★ "없다"를 단언할 때는 대조군이 필수다.** 삭제 단언은 **파일을 아무것도
  안 읽어도 통과한다** — 경로를 오타 내면 빈 문자열을 읽고 초록이 된다. 넷을
  세웠다: 파일을 진짜로 읽었나 / 사전이 살아 있나 / 스캔이 실제로 뭘 봤나 /
  **너무 많이 지우지 않았나**(남기기로 한 이웃 키 생존). §15-2·I-1에 이어 **세 번째**다
- **★★ "남긴 것이 살아 있다"는 소스가 아니라 런타임 카운터로 세라.** 돌연변이
  m3(§8-9 통째 주석 처리)를 **소스 대조는 통과시켰고**(주석 줄에 문자열이 남는다)
  실행 건수 카운터만 잡았다. 하네스에 `SECTION_CHECKS`/`checksIn()`을 붙였다
- **★★ 절 단위로 지울 때는 그 절이 쓰는 공용 헬퍼를 먼저 열어라.** 내가
  `TASKS.json`에 *"참조가 §6·§7 안에 갇혀 있다"*고 적었는데 **틀렸다** —
  `readState()`가 절 밖에서 `#snapstat`·`#mode`를 읽고 있었다. 절만 뗐으면
  삭제된 요소를 가리키는 코드가 남고 **타입·스모크 둘 다 못 잡는다.**
  `grep -c`는 참조가 **어디** 있는지 말해 주지 않는다. "착수 전 실측"이 잡았다
- **⚠️⚠️ 이 환경의 Bash에서 `node -e`가 조용히 어긋난다** — 파일 쓰기가 안 되고
  **stdout이 통째로 안 나오기도 한다.** Tester·Updater가 한 세션에서 **둘 다**
  밟았다. Tester는 `npm run smoke`도 Bash에서 §5에서 `exit 127`로 죽었다.
  **PowerShell에서는 전부 정상.** 테스트·검증은 PowerShell로 돌려라
- 남은 곁가지 2건(안 고침): `verify/ui.ts:311 bucketOf()`가 죽은 함수가 됐다
  (`noUnusedLocals`가 없어 타입 검사가 못 잡는다) · §6을 가리키는 참조 주석 5곳이
  죽은 링크. 둘 다 그 하네스 안이라 **L-4/ISSUE-019 때 같이** 하는 게 왕복이 적다

**★ 스냅샷 논의에서 확정된 사실** — 데스크톱 기능 #10(`Export gltf`)이 **아직
우리 UI에 없다.** 익스포트 파이프라인은 다 돌아가고 산출물이 서버에
`backend/data/exports/`에 **TTL 30분·세션당 4개**로 남는데, 사용자가 가져갈
링크가 화면에 없다. 다음에 그걸 열면 "없던 기능 추가"가 아니라 **#10을 제대로
옮기는 일**이다. 선행: ISSUE-012·013.

### 다음 후보 (사용자와 정하지 않았다)

**GPU 없이 온전히 검증되는 것** — `verify:ui` 가 보류라 화면 작업은 검증이 반쪽이다:

| 후보 | 비용 | 메모 |
|---|---|---|
| **A그룹 파라미터 확장 17개** | 낮음 | C++ 매핑 테이블에 줄 추가가 대부분. `sweep:params` 도구가 이미 있다. 화면을 안 건드려 검증이 온전하다 |
| **`setEndPose`** (B그룹 마지막) | 중간 | 남은 op 은 이것과 드레이프 **목록 조회** 둘뿐이다 |
| **`saveZls`** (D그룹, UI #8) | 중간 | 편집한 씬을 저장한다 — 지금은 바꾼 것이 세션과 함께 사라진다 |
| **ISSUE-024**(차단) | 중간 | 드레이프가 아바타를 통째로 바꿔 체형 조절이 막히는 씬이 있다 |
| **ISSUE-015**(차단) | ? | `step` op 이 워커에서 아무 일도 안 한다 |

**화면을 바꾸는 것**(C그룹 노치·끝점 원 · L-2b · **L-4**)은 ⛔ `verify:ui` 가
풀린 뒤가 낫다. L-4 는 아예 그 하네스를 고치는 작업이라 실행 확인이 불가능하다.

**신뢰성 셋은 CI 를 붙이기 전에 결정해야 한다** — ISSUE-016(`npm run build` 가
3~5회 중 2~3회 rollup panic. ⚠️ `:3000` 측정용 `dist` 빌드가 이것에 막힌다) ·
ISSUE-013(워커 4001) · ISSUE-007(서버 스모크가 프로세스째 죽는다).

### ★ setFabric = 데스크톱 기능 #50 (`772f128` 워커·SDK·게이트웨이 + `74ae463` 화면)

원단 콤보를 **옷 사이즈 행에 하나씩** 얹었다(조각을 식별할 수 있는 자리가
거기뿐이다 — 이름이 유일하지 않아 크기가 구분 수단이다). 실측: 색 분포
16/8 → 15/9, 정확히 한 조각만 옮겨가고 번짐 0.

- ⚠️⚠️ **이 설치본에는 프리셋 원단이 없다.** 씬 열기 전 0개, 연 뒤 **씬 내장
  2개뿐**이고 디스크에도 프리셋 폴더가 없다(`fabric_custom` 은 비었다). 즉
  **그 옷이 이미 쓰는 직물끼리 재배정**만 된다. 기능이 좁은 게 아니라 머신에
  원단이 없는 것이라, **프리셋 개수를 판정으로 삼지 않았다** — `=== 0` 을
  못박으면 원단이 깔린 환경에서 거짓 실패가 난다. 화면 배너도 프리셋이 하나라도
  생기면 사라진다
- ⚠️⚠️ **uuid 형식이 두 가지다.** `surfaces` op 은 `GetString()`
  (`007C7IvlTV0/…`), 직물·머티리얼은 `ztUuidSaver::Convert`(`356924…/150000`).
  섞으면 "서피스를 찾을 수 없습니다" 로 조용히 실패한다 — **그 함정을 경고해
  둔 파일 안에서** 밟았다. `ReadSurfaces` 머리말이 "되파싱 말고 목록을 돌며
  비교하라" 고 적어 둔 그대로 따랐어야 했다
- **★ `zwMaterialManager` 가 GL 에 닿는다는 기존 경고는 함수 단위로 갈린다.**
  GL 은 `ParseFile`(폴더 스캔, `Initialize()` 때 끝남)에 있고
  `GetFabricRootFolder()`·`LoadIntoMaterial` 은 경로 문자열만 만든다 — 안전하다.
  앞선 단위(materials-a)가 이 경고 때문에 통째로 피했던 길이다
- **★★ 앞선 세션의 덫이 걸렸다.** SDK 스모크 §5.9 에 *"워커가 아직 setFabric 을
  모른다"* 는 단언이 있었다(`material` 을 topology 안에 둔 근거를 지키는 장치).
  다시 본 결과 **게이팅은 유지**하고 적용 시점에 `restageTopology` 를 부르는
  것으로 풀었다. 없는 것을 단언해 미래를 지키는 이 수법은 실제로 작동한다
- **★ 콤보의 거짓말**: 씬이 준 `fabricUuid` 가 목록에 없으면 `<select>` 가
  **첫 항목을 "현재" 로 보여준다.** 모르는 값은 빈 문자열로 떨어뜨려야 한다

**다음 할 일은 사용자와 정하지 않았다.** 남은 백로그는 `TASKS.json` 이 정본이다.
GPU 없이 온전히 검증되는 것: **A그룹 파라미터 확장 17개**(C++ 매핑 테이블에 줄
추가가 대부분, `sweep:params` 도구가 이미 있다) · 남은 op 4개(`setFabric`(체감이
가장 크다) · `setEndPose` · `saveZls` · `drapingItems` 목록) · 차단 이슈
ISSUE-024·015. 화면을 바꾸는 것(C그룹 마무리 · L-2b · **L-4**)은 **⛔ `verify:ui`
GPU 보류라 검증이 반쪽**이다.

### ★ ISSUE-021 종결 (`9da2927`) — E그룹이 6/6 이 됐다

`avatarBody` 가 `.zls` 씬 데이터를 읽는데 쓰기는 살아 있는 `ztDesignZeta` 로
가서 **둘이 동기화되지 않았다.** `ReadAvatarBody` 를
`zeta->GetMeasurement()->GetMeasuredLength(part)` 로 옮겨 닫았다 — `height`
0.5 → 0.9 에 **25개 중 19개**가 따라 움직인다(예전엔 0개).

- **★★ 워커를 고치면 그 위의 화면 판단이 뒤집힐 수 있다.** 낡음 배너의 전제가
  *"그쪽 경로에는 되읽기가 없다"* 였는데 그게 거짓이 되면서, **그대로 뒀으면
  화면이 새 거짓말을 할 뻔했다.** 워커 수정 뒤에는 그 값을 쓰는 화면 쪽 가정을
  반드시 훑을 것
- **`measurementSource`(`live`/`sceneData`)를 함께 싣는다.** 제타가 아닌
  아바타는 여전히 안 움직이는데 **값만 보고는 구분이 안 된다.** 쓰기 op 의
  "되읽어 실어라" 와 같은 계열의 규칙 — **읽기 op 은 출처도 실어라**
- ⚠️ **`setFromWorker` 를 재사용하지 마라.** "새 아바타를 읽었다" 용이라
  편집·목표치까지 지운다. 슬라이더를 미는 동안 쳐 둔 cm 입력이 사라진다
- ⚠️ **기준선(`#baseline`)도 함께 옮겨야** 워커의 단계 수 계산이 맞는다.
  안 옮기면 "같은 부위를 두 번 조절하면 누적이 안 된다" 가 화면 쪽에 남는다
- **★ 원복 확인에서 빈 줄 하나가 빠진 것을 해시가 잡았다.** 로직은 같았는데
  기준 해시가 달랐다 — `git hash-object` 대조를 매번 하는 이유가 이것이다
- 단언 17건(backend §5.12 7 · frontend §18 10), 돌연변이 4건 전부 잡힘.
  **이 op 에 단언이 그때까지 하나도 없었다**

### ★★ ISSUE-025 는 제품 결함이 아니었다 (`12a98e5`)

**씬을 두 번 여는 것은 Vite 개발 프록시다.** 붙는 곳만 바꿔 같은 조작을 했다:

| 브라우저가 붙는 곳 | 게이트웨이 재시작 시 |
|---|---|
| Vite `:5173` | 세션 **2개** · 워커 2개 · 138MB 로드 **2회** (하나는 버려짐) |
| 게이트웨이 직접 `:3000` | 세션 **1개** · 로드 **1회** |

- ⚠️ **세션·워커·로드 횟수를 재는 실험은 `:3000` 에서 하라.** `SDK_USAGE.md` §4 에
  경고를 붙였다. 이걸 몰라서 "제품 결함" 으로 등재됐었다
- **릴레이는 1:1 이다** — `load` op 하나 = `LoadZls` 하나. 사용자가 밟는 다섯 경로
  (첫 진입·[로드]·새로고침·HMR·워커 죽음 후 재연결)는 전부 1회다
- **★ 그럴듯한 코드 경로를 찾았다고 원인이 되지 않는다.** 중간에 `onMismatch`
  복구가 `show()`(→`client.load`)였던 것을 원인으로 잡고 `530bbff` 가 이미
  고쳤다고 결론냈다가, **첫 로드에서 `onMismatch` 가 실제로 발화하는지 재 보고
  뒤집었다** — 그 갈래는 프레임이 흘러야 도는 것이라 재생 전에는 원리상 못 돈다.
  문서에 쓰기 직전에 걸렀다
- 곁가지: `client.ts:702` 의 `#openSocket()` 이 **기존 소켓을 안 닫고** `#ws` 만
  갈아 끼운다. 지금 그 경로를 밟는 호출자는 없어 안 고쳤고 ISSUES.md 에 적어 뒀다

세 단위(액세서리·서랍·텍스처 캐시)에 단언 56건이 붙었고 돌연변이 7건이 전부
잡혔다. 상세는 아래 "Tester 3단위" 절에 있다.

### 2026-08-12 커밋 5개

| 커밋 | 내용 |
|---|---|
| `a3fa2ce` | 아바타 **액세서리** — 이 씬은 신발 하나(7,914정점, 28,564→36,478) |
| `236acac` | TASKS.json E그룹 갱신 + ISSUE-025 |
| `71c5dc9` | 좁은 창에서 오른쪽 칸이 **서랍**이 된다 + 최소 너비 |
| `3465dc7` | **텍스처 거절 캐시** — 일시적 실패가 영구 고장이 됐다 |
| `27ea33a` | **다국어 I-1** — 사전 302키 × 2언어 + 스모크 32건 (아래) |
| `e016eff` | **Tester 3단위** — 액세서리·서랍·텍스처 캐시에 단언 56건 (아래) |

### Tester 3단위 (`e016eff`) — 셋이 서로 다른 러너다

| 단위 | 층 | 러너 | 건수 |
|---|---|---|---|
| 액세서리 | C++ 워커 | `backend npm run smoke` (§5.11) | 73 → **87** |
| 텍스처 캐시 | 게이트웨이 | `backend npm run smoke:server` (§11-1) | 420 → **429** |
| 서랍 | 프론트 DOM-free | `frontend npm run smoke` (§17) | 862 → **895** |

돌연변이 7건 전부 잡혔다. 다음에 재사용할 것만 적는다:

- **★★ `sample.zls` 도 zeta 다.** 아래 W-2 항목의 *"sample.zls 로는 체형 변형이
  안 된다 — 그 아바타가 `ztDesignZeta` 가 아니다"* 는 **치수 변형 갈래에 한정된
  이야기였다.** `avatarMesh` 는 `subType:'zeta'` 를 준다. 액세서리만 0개(슬롯이
  전부 비활성)라 **"경로는 돌면서 아무것도 안 싣는" 대조군**으로 쓰기 좋다
- **★★ 액세서리 파트 인덱스가 몸 뒤로 연속하지 않는다.** 비활성 슬롯도 번호를
  먹어서 몸 12파트(0–11) 다음의 신발이 **29번**이다. 연속을 기대하는 단언을 쓸
  뻔했다 — **프로브로 먼저 재서 막았다**
- **★★ C++ 단위의 돌연변이는 재빌드가 필요하고, 링크하려면 게이트웨이를
  내려야 한다**(exe 잠금). 순서: 브라우저 세션 끊기 → `preview_stop` → 돌연변이
  → 빌드 → 스모크 → 원복 → 빌드 → 게이트웨이 재기동. 회차당 ~2분
- **★ 로그 줄에 경로가 안 담긴다**(`[warn] 텍스처를 거절했습니다 (파일이 없습니다)`).
  사유로 걸러 세면 **다른 파일의 경고까지 세어진다** — 처음에 그렇게 짜서 빨갛게
  떴다. **줄 수가 아니라 늘어난 양(델타)을 재라**
- **못 보는 것을 단언 옆에 적어 뒀다** — 경로 거절 캐시의 *단축 자체*는 동작으로
  안 보인다(캐시를 없애도 검사가 다시 돌아 같은 답이 나온다). 성능 회귀는 못 잡고
  **잘못된 방향(상태를 기억한다)만** 잡는다는 뜻이다
- 액세서리가 있는 씬은 저장소에 없어(`backend/data/` 는 .gitignore)
  `findRotatedScene`·`findMultiColorScene` 관례를 따랐다 — 있으면 태우고 없으면
  **무엇을 못 덮었는지 `note()` 로 화면에 남긴다**
- ⚠️ **`sdk/protocol.ts` 타입이 워커를 안 따라와 있었다** — `AvatarPart` 에
  액세서리 필드가 없고 문서는 "아직 안 온다" 였다. **C++ 을 고칠 때 TS 타입도
  같이 보라**(프론트는 필드를 몰라도 그려져서 안 드러난다)

### 다국어 I-1 종결 (`27ea33a`)

**상세는 `TASKS.json` G그룹이 정본이다.** 여기엔 다음에 재사용할 것만 적는다.

- **★★ 이런 단위는 "깨지면 예외"가 아니라 "깨지면 글자"다.** 그래서 단언을 세
  갈래로 나눴고 그게 먹혔다: ① 사전 두 벌의 **키 집합 대칭** ② **자리표시자
  집합 대칭**(`{why}`가 한쪽에 없으면 값이 화면에서 사라진다) ③ **소스를 읽어**
  부르는 키가 실재하는지. 같은 모양의 단위가 또 오면 이 셋을 그대로 쓸 것
- **★★ ③(소스 대조)이 없으면 아무것도 못 잡는 결함이 둘이다.** 키 오타는
  타입이 `string`이라 `tsc`를 통과하고 화면에도 예외가 안 난다. `index.html`에
  `data-i18n` 없이 글자를 넣는 것도 같다. 돌연변이 5건 중 이 둘이 그랬다
- **대조군 단언을 옆에 세워라.** "키 집합이 같다"는 **양쪽이 다 비어도** 통과하고,
  소스 스캔은 정규식이 아무것도 못 잡으면 공짜로 통과한다. `키 > 100`,
  `호출 > 100회`를 붙였다. §15-2에서 한 번 겪은 실패 모드다
- **번역기를 주입받는 모듈이 있다** — `viewer2d/control.ts`는 `tr`로 받는다(그래야
  사전을 import 안 하고도 테스트된다). **스캔 정규식이 `t|tr|statusT` 셋을 다
  봐야** 그 파일이 대조 밖에 안 남는다
- **모듈 상태(현재 언어)를 만지는 절은 되돌리고, 되돌아왔는지도 단언하라.**
  영어로 둔 채 넘어가면 뒤 절들이 한국어 화면 글자로 빨개진다
- **화면의 한글 잔여는 눈이 아니라 코드로 세라.** 브라우저에서 `TreeWalker`로
  텍스트 노드 + `title`/`aria-label`을 훑는다. ⚠️ **`hidden` 요소가 걸린다** —
  `getClientRects().length`로 안 거르면 안 보이는 배너를 결함으로 오진한다
  (실제로 한 번 헛짚었다). 로그 상자와 `<option>한국어`는 의도된 제외다
- ⚠️ **`verify/ui.ts` 8곳이 한국어 글자로 요소를 찾는다**(`:487·493·498·754·
  1544·1558·1636·1975`). 지금은 안 깨진다 — `chromium.launch()`가 매번 새
  컨텍스트라 `localStorage`가 비고 기본값(한국어)으로 열린다. **그게 이제
  전제다.** 기본 언어를 바꾸거나 프로필을 재사용하면 8곳이 한꺼번에 죽고 증상은
  "재생 버튼이 안 눌린다"로 보인다. 스모크의 `기본 언어는 한국어다`가 그 전제를
  지킨다. 고치는 것은 하네스를 실제로 돌릴 수 있을 때(GPU 보류) **L-4와 함께**

### ★★ 두 번 밟은 함정: "없는 것"과 "기본값이 들어 있는 것"

**`ztDesignMaterialData`의 기본 생성자가 이미 텍스처를 물고 있다** —
`basecolorTexture = { BASE_COLOR_TEXTURE }`(ztDesignMaterial.h:60), 빈 벡터가
아니라 프리셋 직물의 `Default_Base_Color.png`이고 **그 파일은 이 설치본에
없다.** 액세서리용 머티리얼을 합성하면서 안 비웠더니 "우리가 아무것도 안
실었는데 텍스처가 하나 실린" 상태가 됐고, 처음엔 **엔진이 준 값인 줄 알고
헛짚었다.**

→ **엔진 구조체를 직접 만들어 채울 때는 기본값부터 확인하라.** 빈 값일 것이라는
전제가 틀렸다. `basecolor`도 흰색(1,1,1,1)이 기본이라 "색을 못 찾았다"와
"흰색이다"가 구분되지 않는다.

### ★★ 일시적 실패를 영구 실패로 승격시키지 마라 (`3465dc7`)

게이트웨이가 텍스처 **거절을 전부 캐시**했다. 그런데 사유의 절반(`파일이
없습니다`·`빈 파일`)은 그 순간의 디스크 상태다. 한 번 실패하면 파일이
멀쩡해져도 **프로세스가 사는 내내 거절**이라 재시작 말고는 복구가 없었다.

화면에서는 **옷 색이 통째로 사라진 것**으로 보였고 재로드해도 안 돌아왔다.
사용자가 *"첫번째 패널 이전으로 돌아간거 같은데"*로 알아챘다.

- **원래 주석이 답을 갖고 있었다** — *"경로만 보고 정해지므로 다시 물어도
  답이 같고"*. 설계 의도는 **경로 모양** 거절만 기억하는 것이었는데 코드가
  상태 거절까지 담았다. 의도대로 `path` / `state`로 갈랐다.
- **`빈 파일`이 가장 흔한 일시적 실패다** — 엔진이 파일을 만들고 아직 다
  안 썼으면 0바이트로 보인다.
- 로그 도배는 `#warned`로 따로 막았다 — **검사는 매번, 경고는 한 번만.**
- ⚠️ **워커가 아니라 게이트웨이라는 것을 가른 방법**: 워커에 직접 물어
  4개 경로 전부 `existsSync` 참인 것을 확인했다. 그 전까지는 워커를 의심했다.

### 서랍 UI에서 확립된 것 (`71c5dc9`)

- **문턱(900px)이 TS 상수 한 곳에만 있다.** CSS에 미디어 쿼리를 안 두고
  `main.ts`가 `matchMedia`를 듣고 `<body>`에 표식을 건다. 두 곳에 적으면
  한쪽만 고치는 날 "버튼은 있는데 칸이 그대로"가 된다
- **`position: fixed`를 쓰지 마라.** 서랍이 상단 바를 덮으면 안 되는데 바
  높이는 버튼이 줄바꿈되면 달라진다. 칸을 격자에 그대로 두고 두 뷰포트와
  **같은 칸에 겹쳐** 오른쪽에 붙이면 잴 숫자가 없다
- ⚠️ **`matchMedia`의 `change`만 믿지 마라.** 실측 — 창을 800px로 줄였을 때
  `mq.matches`는 참이 됐는데 `change`가 안 왔다. `resize`도 같이 듣는다
- ⚠️ **화면 밖으로 민 요소가 가로 스크롤을 만든다.** `body`의
  `overflow: hidden`으로는 안 막혔다(800px 창에서 `scrollLeft` 388까지 밀렸다).
  스크롤바가 안 보여 눈으로는 멀쩡하다. `html`에 걸어야 한다
- 뷰어 둘이 `ResizeObserver`를 쓰므로 **격자를 바꿔도 크기 갱신 배선이 필요 없다**

### ⚠️ 이날 내가 어긴 것 둘

- **[[stay-in-scope]]** — UI 작업 중 딴 결함을 파다 제지당했다. 새 메모리로 뺐다
- **브라우저 창 크기를 테스트용(820px)으로 줄여 놓고 안 되돌렸다.** 사용자가
  *"브라우저 화면이 이상한데 너무 작아져있어"*로 발견했다. 아래 L-3c 항목에
  같은 교훈(슬라이더 0.857)이 이미 적혀 있는데 **또 밟았다.** 실측으로 바꾼
  것은 **도구 상태까지** 되돌릴 것

**아래는 그 이전 맥락이다.**

## 회사 CLI 흡수 (2026-08-11 밤)

**회사가 헤드리스 CLI를 직접 만들었고, 우리는 그걸 흡수했다.**
`zelusSandBox_Cobalt`가 `master` → **`console_application`** 브랜치로 바뀌었다.
상세는 [[company-code-context-mismatch]]에 있다 — **회사 코드를 옮길 때마다
읽을 것.**

| 커밋 | 내용 |
|---|---|
| `2b6b3ce` | 워커에 op 2개 (`loadDraping`·`setAvatarMeasurements`) + 문제 4건 수정 |
| `0946a1a` | 드레이프를 화면까지 (W-1) |
| `2cda598` | 치수 25개 조절 화면 (W-2) + ISSUE-021 갱신·024 |
| `530fa54` | `avatarMesh` op — 워커·게이트웨이·SDK (AM-1) |
| `67cfce5` | 실시간 뷰에 아바타 (AM-2) |
| `5dde4cd` | 텍스처 출처 조사 — **디스크 파일이었다** |
| `82a0fb3` | **실시간 뷰에 텍스처** — 피부·눈·직물 무늬 |
| `b8ac2fc` | op 인벤토리 등재 + 텍스처 라우트 단언 |

## ★★ 실시간 뷰가 목표 그림에 도달했다 (2026-08-12 새벽)

**참고 그림(스냅샷 뷰)과 실시간 뷰의 차이는 아바타가 아니라 텍스처였다.**
이제 실시간 뷰에 피부·얼굴·눈·민트/노랑 직물이 나오고 **색이 스냅샷과
소수점까지 같다**(`#f6e9b6` / `#def2e9`). 머리카락·신발만 없다(액세서리 미포함).

**설계의 핵심 — 워커는 경로만 싣는다.** 이미지를 읽지도 base64 하지도 않고,
게이트웨이가 정적 서빙하며 브라우저가 캐시한다. 실측:

| | 스냅샷 | 실시간 + 텍스처 |
|---|---|---|
| 첫 로드 | 34.9MB / 6.3초 | 20.6MB (10요청) |
| **체형 바꿀 때마다** | **34.9MB / 6.3초 매번** | **0 요청 / 0 바이트** |
| 새로고침 | 매번 전량 | **3.0KB** (전부 304) |

**스냅샷 glTF 34.9MB 중 27.7MB(79%)가 텍스처인데 체형을 바꿔도 안 변한다** —
그것이 이 방향을 고른 이유다. ⚠️ **처음엔 "기본 뷰를 스냅샷으로" 를 추천했다가
이 숫자를 재고 뒤집었다.**

**남은 것**: **Tester**(새 DOM-free 모듈 6개 + 돌연변이) · 액세서리(머리카락) ·
`verify/ui.ts` 논증 붕괴(아래).

### ⚠️ W-2 착수 전에 반드시 알 것 셋 (실측)

- **`sample.zls`로는 체형 변형이 안 된다.** `setAvatarMeasurements`가 `ok:false`로
  거절한다. **사용자 씬(W_Bra top & Leggings)에서만 된다.**
  ⚠️ **정정(2026-08-12)**: 처음에 이유를 *"그 아바타가 `ztDesignZeta`가 아니다"*로
  적었는데 **틀렸다.** `avatarMesh`는 `sample.zls`에도 `subType:'zeta'`를 준다 —
  거절되는 것은 **치수 변형 갈래**이지 아바타 종류가 아니다.
  화면이 이 갈래를 **글자로** 말하지 않으면 "눌렀는데 아무 일도 없다"가 된다
- **`measured`가 빈 객체일 수 있다** — 적용된 것이 없으면 `{}`. "전부 null을
  보내 지금 치수를 읽는다"는 안 통한다. **읽기는 `avatarBody`다**
- **프론트 `GatewayClient` 기본 제한이 60초**다(워커는 120초). Δ가 크면
  브라우저만 먼저 포기하고 워커는 계속 돈다. `timeoutMs` 통로는 열어 뒀다

### ★ 되읽기 경로 함정 — 기존 코드도 걸린다

**`ztDesignAvatarData.zetaData`는 `.zls`에 저장되는 씬 데이터이고,
`SetMeasurementParam`은 살아있는 `ztDesignZeta`로 쓴다. 둘은 동기화되지 않는다.**
Δ15cm를 걸고 Step을 96번 돌려도 `measurementRealValues`는 61.647 그대로인데
glTF 해시는 바뀐다 — **몸은 변하고 우리가 읽던 곳만 안 변했다.**

정본은 `zeta->GetMeasurement()->GetMeasuredLength(part)`이고 응답의 `measured`
필드가 그것이다.

⚠️ **`ReadAvatarBody`를 기존 `avatarBody` op도 쓴다** — 오른쪽 패널의 치수
25개가 **로드 시점 스냅샷**이라는 뜻이다. 지금은 체형을 바꿀 방법이 없어
안 드러났을 뿐이고, W-2에서 같이 옮겨야 한다.

### ★★ 빨간 단언이 곧 제품 결함은 아니다 — 테스트 도구가 거짓말한다

`smoke:server` 의 `두 번째 요청은 304 + 0 바이트다` 가 빨갛게 떴는데
**서버는 처음부터 정상이었다.** **node 의 `fetch`(undici)가 시키지도 않은
`cache-control: no-cache` 와 `pragma: no-cache` 를 요청에 붙이고**, `fresh`
모듈은 그게 있으면 **일부러 304 를 안 준다**("캐시 말고 본문을 다시 달라").

| | 결과 |
|---|---|
| `curl` · `node:http`(raw) · 브라우저 | **304 / 0 바이트** |
| **node `fetch` (기본)** | **200 / 전량** ← 이것만 다르다 |
| node `fetch` + `Cache-Control: max-age=0` | 304 |

⚠️ **단언을 "200 도 통과" 로 약하게 고쳤다면 진짜 캐시 회귀(씬 열 때마다
20MB 재전송)를 영영 못 잡게 될 뻔했다.** 브라우저에서 304 를 실제로 본 실측이
있었기에 의심할 수 있었다.

**→ 캐시·조건부 GET 을 node `fetch` 로 테스트하지 마라.** 헤더를 직접
덮어쓰거나 `node:http` 를 써라.

(그 조사에서 진짜 결함도 하나 나왔다 — `res.sendFile` 콜백이 **304 로 끝나도
에러 없이 불려서** `served` 카운터가 재검증까지 셌다.)

### ⚠️ 성능을 Debug로 판단하지 마라 — 9배 차이다

같은 요청(Δ15cm, Step 96회)이 **Debug 142.3초 / Release 15.4초**다.
Debug 숫자만 보고 "게이트웨이 타임아웃 120초를 넘으니 비동기 구조가 필요하다"고
설계를 바꿀 뻔했다. **Release에서는 여유가 있다.**

부수: **로드 직후 익스포트는 비결정적이다**(같은 씬 2회, 크기는 같고 해시가
다르다). 체형을 한 번 계산한 뒤로는 결정적이다. **해시 대조에서 "변경 전"을
기준으로 쓰지 마라.**

### 앞 작업 (2026-08-11 오후) — 재단 도면 디자인 정보

가운데 칸이 흰 종이 + 외곽선 + 내부선 + 제어점 + 봉제선 + 봉제 대응 점선 +
스티치를 그린다. `TASKS.json` C그룹이 정본(D2-a~e, `6c85592`·`db6e2d1`·
`50c397a`·`b8211ec`·`a73c070`, 문서 `714c462`).
**남은 것**: 화살표 노치 · 봉제선 끝점 원 · `design.ts` 전면 테스트 ·
**ISSUE-022 확인**(아래).

### ★★★ 이 세션 최대의 교훈 — 그림을 흉내내지 말고 코드를 읽어라

사용자가 데스크톱 2D 뷰 스크린샷을 주며 *"이미지처럼 나와야 한다"*고 했다.
그 이미지를 **눈으로 흉내내며 값을 지어내다 두 번 연속 틀렸다**:

| | 점선 | 색 |
|---|---|---|
| 1차 (내 발명) | 봉제선당 중점 하나 = **45줄** | 회색 1종 |
| 2차 (내 발명) | 길이 4cm마다 = **199줄** | 황금각 45종 |
| **정답 (엔진)** | **108줄** | **22종** |

답은 **회사 저장소에 다 있었다** — `zelusSandBox/Seam2DRenderer.cpp`:

```cpp
seam->GetPositionsForDraw(points[0], points[1], /*world=*/true);
for (k...) DrawDashedLine(points[0][k], points[1][k], seam->GetColor(), w);
```

**개수도 위치도 색도 우리가 정할 것이 하나도 없었다.** 두 번의 헛수고가 그
파일 하나를 여는 것으로 끝났다.

⛔ **회사 저장소는 읽기 전용이지 "읽지 말라"는 뜻이 아니다.** 데스크톱이 이미
그리고 있는 것을 옮기는 작업이면 **`zelusSandBox/*Renderer*.cpp`를 먼저 읽어라.**
(`Renderer2D.cpp` · `Seam2DRenderer.cpp` · `Renderer3D.cpp`)

**곁가지로 수수께끼 둘이 풀렸다**:
- 이미지의 **빗금은 노치가 아니다** — `RenderArrowNotch`, 봉제 방향 화살표다.
  씬의 `notches`는 `null`인데 빗금이 보였던 이유
- 이미지의 **흰 테두리 원**은 봉제선 끝점 — `DrawPoint(색) + DrawFixedSizeCircle(흰색)`

**그리고 "색은 데이터에 없다"는 내 중간 결론도 틀렸다.** `.zls`의 `seams` JSON에
색 필드가 없고 스티치가 2종뿐인 것만 보고 판단했는데, **`ztDesignSeam` 객체가
색을 갖고 있었다**(`GetColor()`, 실측 22종). **직렬화에 없다 ≠ 데이터에 없다.**

### ⚠️ ISSUE-022 — 다음 세션이 확인할 것

헤드리스에서 `GetPositionsForDraw`가 **완전한 값을 주는지 모른다.** 대응점이
`mutable` 캐시(`mTessellatedPoints` + `mDirty2D`)라 2D 렌더러가 그릴 때 채워지는
것이라면 창 없는 워커에서는 최소치만 나올 수 있다. 실측 분포가 의심스럽다 —
**`links`가 항상 2 아니면 4이고 파트 수와 무관하다**(파트 12/6짜리도 4줄).

**가르는 법 둘**: (1) `cmake_build_src/bin/Debug/zelusSandBox.exe`로 같은 씬을
열어 점선을 세어 108과 대조 (2) 엔진 담당자에게 묻는다 — 질문 셋이 ISSUE-022에
정리돼 있다.

### 도면 작업에서 확립된 것

- **`atWorld=true`가 #15의 전치 함정을 통째로 없앤다.** `CreateGeomCubicBezierCurve
  (uuid, atWorld=true)`가 배치까지 끝낸 커브를 준다 — **우리가 행렬을 안 만지므로
  전치가 성립할 자리가 없다.** `.zls` 직접 실측(제어점 909개 범위 66.6×103.5cm
  vs 도면 전체 144.2×175.4cm)으로 로컬임을 먼저 확인하고 이 길을 골랐다
- **⚠️ 같은 칸에서 한쪽에만 변환을 곱한다.** 옷 메시(`uvs`)는 패턴 로컬이라
  `transform2d`가 필요하고 커브는 이미 월드다. 둘 다 곱하면 "패턴이 좀 흩어져
  보인다"로만 드러난다
- **`.zls`를 직접 읽는 것이 빠르다.** ZIP 껍데기 안에 평문 JSON(`scene.zls`)이고
  grep이 통한다. C++ 빌드 왕복 없이 개수·좌표계를 먼저 잴 수 있다.
  ⚠️ **`FLT_MAX`(3.4e38) 센티넬이 섞여 있다** — 안 거르면 범위가 무한대가 된다
- **⚠️ 저장소 전체 grep은 2분을 넘긴다.** `CLAUDE.md`가 경고한 그대로 밟았다.
  `Glob`으로 파일을 먼저 찾고 그 파일만 `Grep`할 것

**아래는 그 이전 맥락이다.**

## 3분할 UI (2026-08-11 오전)

사용자가 데스크톱 `zelusSandBox` 스크린샷을 주며 *"이게 원래 구현하려던 UI
모습이었다"*고 확정한 그 화면이다.

```
┌──────────────┬──────────────┬───────────────────────┐
│ 3D 뷰포트     │ 2D 재단 도면  │ 아바타 체형 29개       │
│              │  (24패턴)     │ 치수 25개 (읽기전용)   │
│              │              │ 옷 사이즈 24개 (cm)    │
└──────────────┴──────────────┴───────────────────────┘
```

**완료** — L-1 `8c8f08d` · L-2a `e622dbc` · L-3a `b576940`·`ce891d5`·`97b7d9e` ·
L-3b `58c50f4` · **L-3c `3cf27f0`**(오른쪽 칸 탭) · 문서 `4c3c44e`·`f3cd0c6`.
**상세는 `TASKS.json` F그룹이 정본이다.**

**남은 것**: `L-2b`(펼침 슬라이더를 가운데 칸으로) · `L-4`(하네스를 3분할에
맞춘다, ISSUE-019) · `avatarMesh-a/b`(실시간 뷰에 아바타 — 지금은 스냅샷에만)

**⚠️ L-3c에서 사고가 있었다** — 앞 세션의 Tester가 **돌연변이 검증 도중 죽어
결함 2건을 트리에 남겼다.** 이번 세션이 그걸 이어받아 고쳤다.
**★ 돌연변이를 심는 검증은 심어 놓고 죽을 수 있다 — 기준 해시를 뜨고 원복을
그 자리에서 확인하게 만들 것.** 그 뒤로는 매번 `git hash-object`로 확인했다.

### 여기에만 있는 맥락 (문서에 없는 것)

- **오른쪽 칸의 내용을 사용자가 바꿨다** — 파라미터 패널 이관이 아니라 **아바타
  신체 값 + 옷 사이즈**다. *"나머지 기능은 아직 필요없어."* 기존 파라미터 22개는
  왼쪽 아래 오버레이에 그대로 둔다(L-3-old, deferred)
- **몸무게는 안 넣는다** — `ztAvatarBodyParam` 29개에 없다. 사용자 결정:
  *"있는 기능만 사용하자 일단."* 엔진에 없는 값을 합성하지 말 것
- **검증 방식**: `verify:ui` 대신 **메인 세션이 브라우저로 직접 본다**(사용자 결정).
  ⚠️ 아래 GPU 경고를 지킬 것 — **확대 캡처 연속은 금물**이고 이 세션에서 스냅샷
  렌더 직후 **탭이 `Target crashed` 로 죽었다**
- **⚠️ 실측으로 상태를 바꿨으면 그 자리에서 되돌려라.** 검증용으로 키를 0.857로
  올려두고 넘어갔더니 사용자가 *"너가 뭔갈 작업하고 이게 이상하게 입혀졌어"*로
  발견했다. `.zls`는 안 바뀌므로 **재로드하면 복구된다**
- **`node -e` 로 파일을 고치려 하지 마라.** 이 환경(Git Bash)에서 **조용히 아무
  일도 안 하고 종료**한다. 두 번 겪었다 — Edit 도구를 쓸 것
- **네이티브 빌드는 게이트웨이를 내려야 링크된다**(exe 잠금, `LNK1104`).
  컴파일 오류는 게이트웨이가 떠 있어도 잡히므로 **먼저 돌려 보고 링크만 나중에**
  하면 왕복이 준다. 증분 빌드 ~10초

**아래는 그 이전 맥락이다.** TASKS.json 16개는 여전히 전부 done이고 나머지는 백로그다.

### ⛔ 2026-08-10에 여기서 멈췄다 — 이유부터 읽을 것

**이 머신에서 PC가 통째로 두 번 멈췄다.** 사용자가 "CUDA 드라이버가 제대로 안
깔려 있다"고 확인해 줬다. 그래서 **`npm run verify:ui`를 보류했다** — Chrome+WebGL이
D3D11로 GPU를 타고, 먹통이 그 구간에 났다.

- **화면을 바꾸는 단위는 지금 착수하지 마라.** `verify:ui` 없이는 검증이 반쪽이다
  (#16에서 12건, #15에서 3건이 그 하네스에서**만** 잡혔다).
- **GPU를 안 쓰는 단위는 진행 가능하다** — 워커 쪽(`avatarMesh-a`), 백로그 A그룹.
- **테스트를 하나씩, 순차로 돌려라.** 사용자가 "쪼개서 돌릴 수 없냐"고 요청했고
  그렇게 하니 멀쩡했다. `backend` → 쉼 → `frontend` → 쉼.
- ⚠️ **재부팅 직후에는 `node`가 `0xC0000005`로 100% 죽을 수 있다.** 2026-08-10에
  `session.ts` 정적 import만 해도 죽었고, scratchpad의 `console.log` 한 줄짜리도
  죽었다. **크래시 여파이고 두 번째 재부팅 뒤 정상으로 돌아왔다.** 코드를 의심하기
  전에 자명한 스크립트 하나로 환경부터 확인할 것.

**CUDA 재설치로 워커를 GPU화하는 것은 답이 아니다** (사용자가 물었고 확인했다):
`backend/native/CMakeLists.txt:91`이 `ZS_USE_CUDA`를 **의도적으로 뺐다** —
`cmake_build_src`가 OFF로 빌드돼 있어 정의가 어긋나면 ABI가 깨진다. 엔진의 CUDA
빌드(`cmake_build_cuda`)는 산출물 **3개**에서 멈춰 있다. 드라이버만 깔아도 안 된다.
**필요한 건 디스플레이 드라이버 클린 재설치이고, CUDA는 v1 범위 밖이다.**

### 순서 (사용자 승인, `materials-a`까지 완료)

**`materials` op → `avatarMesh` op → 아바타 체형 조절**

**`materials-a`는 끝났다** (`6673613`) — 워커가 진짜 색을 싣고 `cloth.ts`까지 와 있다.
**남은 넷은 TASKS.json 백로그 E그룹**에 기록해 뒀다(`62f59cc`).

`materials`·`avatarMesh`를 먼저 하면 **실시간 화면이 스냅샷 그림에 가까워져서
이후 작업의 확인이 전부 빨라진다.** 지금 실시간 뷰에는 아바타가 없고 색도 임의다.

- **`materials`** — 낮은 비용. 색이 텍스처가 아니라 **basecolor 스칼라**뿐이고
  (노랑 `[0.925,0.812,0.471]`, 민트 `[0.733,0.886,0.816]`), **UI 하네스가 이미 그
  색을 픽셀로 판정할 줄 안다**(민트/노랑 띠). 완성되면 실시간 화면에서도 같은
  단언이 서야 한다.
- **`avatarMesh`** — 중간. 옷과 같은 자료구조라 워커의 `MeshData()` 재포장 로직을
  그대로 쓴다. 25,357정점(옷의 3.3배, 프레임당 304KB)이라 **매 프레임 밀 물건이
  아니다.** ⚠️ `IsUpdateRenderMesh()`/`IsForceUpdatedMesh()`를 부르지 마라(const인데
  읽는 순간 mutable 플래그를 지워 시뮬 동작이 바뀐다). 아바타는 이중버퍼 보호가
  없어 `ztGlobalMutex`를 직접 잡아야 한다.
- **아바타 체형 조절** — 2026-08-10 확인: **엔진에 완비돼 있다.**
  `ztAvatarBodyParam` **29개**(살집·키·어깨·팔길이·배·힙·가슴·허벅지…),
  `ztAvatarMeasurePart` **25개**(허리둘레·가슴둘레·키…),
  `ztAvatarManager::SetMeasurementParam(치수, 체형, ...)`,
  `ztAvatarShaper::GuessParams(치수)`가 치수→체형을 역산한다.
  데스크톱 앱이 UI로 안 열 뿐이다. **`avatarMesh`가 선행돼야 확인이 빠르다.**

**#16(파라미터 패널)은 2026-08-09에 끝났다** — `ce2fea2`(측정) → `ba1cc08`(16-a
스키마) → `e32d068`(16-b 화면). 아래 §16에서 인계 사항을 볼 것.
**#15(2D 펼침)는 2026-08-10에 끝났다** — `be8f83d`(15-a 워커) → `03cb5a2`(15-b 화면).
아래 §15를 볼 것 — **전제가 틀렸던 단위라 배울 것이 있다.**

측정 도구가 남아 있다 — `cd backend && npm run sweep:params`(전수 ~6분),
`--fields a,b --frames 20`으로 좁힐 수 있고 `--reset` 갈래가 따로 있다.
백로그 A그룹(파라미터 17개 확장)을 열 때 그대로 재사용한다.

**⚠️ 측정에서 배운 것 — 노이즈 바닥을 가정하지 말고 재라.** 첫 측정이 `gravityY`를
"죽음"으로 오판한 원인은 데이터가 아니라 **문턱**이었다. "실행 간 지터 ~1%"로 잡았는데
**실제 바닥은 0%다** — 같은 파라미터 3회가 전 정점에서 비트 단위로 같다(솔버가
결정적이다). 그 전제가 0.22cm짜리 진짜 신호를 삼켰다. 기준선 3회 비용은 42초다.

**⚠️ 그런데 `reset`으로는 그 결정성이 안 나온다.** 같은 워커에서 reset 뒤 재실행하면
같은 파라미터인데도 같은 프레임끼리 **평균 0.50cm / 최대 2.26cm** 어긋난다.
**재로드해야 0이 된다.** 결과를 비교하는 실험은 전부 [로드]로 시작할 것.

작업 방식은 [[subagent-workflow]]의 5단계 루프를 그대로 따를 것.

## #15 2D 펼침 뷰 — 전제가 틀렸던 단위 (2026-08-10)

**TASKS.json이 "추가 데이터가 필요 없다 — `uvs`로 그리면 된다"고 단언했는데 틀렸다.**
`uvs`는 각 패턴의 **로컬 평면 좌표 그 자체**라 24개가 원점에 겹친다(AABB 쌍 82.2%
겹침, 개별 면적 합이 전체 상자의 2.05배). 배치는 `ztDesignSurface::GetTransform()`에
따로 있고 데스크톱 2D 뷰도 그걸 곱해 그린다. 워커가 안 실었을 뿐이다(ISSUE-018).

**브리핑에 "구현 전에 배치를 실측하고, 각자 원점이면 멈추고 보고하라"를 명시한 것이
한 줄도 쓰기 전에 잡았다.** ISSUE-014와 같은 교훈 — **전제를 먼저 재라.**

- 워커가 `transform2d`(행 우선 3×3)를 싣는다 → 겹침 **82.2% → 2.5%**. 남은 2.5%는
  AABB만 겹치고 **천은 안 겹친다**(0.25cm 격자로 세니 겹친 칸 0개).
- ⚠️ **전치가 최대 함정이었다.** `zsMatrix33`은 **저장이 열 우선인데 `GetElement(i,j)`가
  `e[j][i]`를 돌려줘 (행,열) 접근**이다. **전치해 읽어도 그럴듯한 도면이 나온다.**
  `.zls`의 평문 JSON(ZIP 아님, grep으로 읽힌다)에서 `GetMatrix33()` 합성을 재구현해
  행렬 집합끼리 대조해 갈랐다 — 24/24 일치, 전치본 0건.
- ⚠️ **`cloth.boundingBox()`가 평면에서도 z [−12, +10.4]를 준다.** 로컬 AABB 모서리
  8개를 회전시킨 상자라 부푼다. **ISSUE-011의 "박스 말고 점으로 재라"가 세 번째다.**
- 돌연변이 9건이 **두 하네스의 경계를 갈랐다.** #16은 12건 전부 UI 전용이었는데,
  이번엔 **좌표·산술은 스모크가 정본, 배선·카메라·격자는 UI가 정본**이다.
  `apply2d`를 전치해도 **UI는 200/200** — 규약은 스모크의 하드코딩 좌표만이 지킨다.

## #16 파라미터 패널 — 다음이 알아야 할 것 (2026-08-09)

**계층이 셋이다.** `panels/params.ts`가 판단하고(DOM 없음 → Node 테스트),
`ui/paramsPanel.ts`가 그리고, `main.ts`가 배선한다. **`frontend/src/ui/`가 이때
새로 생긴 층이다** — `panels/`가 DOM-free 규약이라 위젯 코드가 못 들어간다.

- **lil-gui를 안 썼다.** TASKS.json `detail`은 lil-gui라고 적고 있었지만 번들
  226KB(ISSUE-016이 열려 있다)와 종속·잠금 규칙 통제 때문에 순수 DOM으로 직접 만들었다.
- **죽은 필드는 스키마에 남기되 페이로드에서 뺀다.** 빼버리면 왜 없는지가 코드
  어디에도 안 남고, **워커는 보내면 "적용됨"이라고 답한다**(스모크가 그 전제를 직접 확인).
- **비활성 사유는 `title` 툴팁이 아니라 화면 글자여야 한다.** 마우스를 올려야 보이는
  것은 보이는 게 아니다.
- **`lockedWhenSimInit`은 `solverType` 하나뿐**(`MainGUI.cpp:459`). 워커 실측:
  **재생에서 켜지고 리셋에서 풀린다.** 리셋 해제를 안 잡으면 반대 방향의 거짓말이 된다.
- **잠금은 두 겹이었다.** 위젯을 회색으로 만드는 것만으로는 부족하다 — 잠기기 전에
  해 둔 편집이 `#pending`에 남아 [적용] 때 나간다. `params.ts`가 "잠긴 필드도 보낸다"고
  정한 근거가 *"화면이 못 만들게 막은 값은 애초에 그 맵에 안 들어온다"*인데 화면이
  그 전제를 안 지키고 있었다. **스키마의 전제를 화면이 지키는지 따로 확인할 것.**

**미완 4건** (TASKS.json #16 `detail` 말미에도 있다): 재연결·끊김 시 패널 갱신 미검증 /
22개 중 화면 확인은 대표 2개뿐 / "기본값 되돌리기" 없음 / **`noScene` 화면에 도달 불가**
(워커가 `clear` 뒤에도 `status.loaded:true` — ISSUE-015 §참고, 회사 저장소라 못 고친다).

## 어디까지 왔나

**16 / 16 완료 — 계획된 단위 전부.** C++ 워커(#1–3), 게이트웨이(#4–10), 프론트 골격(#11), 3D 뷰(#12),
프레임 스트리밍(#13), 재생 컨트롤(#14), **2D 펼침 뷰(#15)**, **파라미터 패널(#16)**.
그 위에 TASKS.json에 없는 **스냅샷 뷰**(`c7b15c0`)와 **UI 검증 하네스**(`af2d2b3`)를 얹었다.

**남은 것은 전부 백로그다** — `materials` op(실시간 뷰에 진짜 색) · `avatarMesh` op
(실시간 뷰에 아바타) · 아바타 체형 조절 · 파라미터 확장 17개 · 씬 인벤토리 op 6개 ·
디자인 기반 2D(봉제선·커브·노치·시접 — 2026-08-09에 보류에서 승격).

**브라우저에서 데스크톱과 같은 그림이 나온다** (2026-08-07 확인). 개발은
`backend && npm run serve` + `frontend && npm run dev` → `localhost:5173`.
화면이 둘이고 **성격이 다르다**:

| | 실시간 | 스냅샷 |
|---|---|---|
| 움직임 | ✅ 40fps | ❌ 정지 |
| 아바타 | ❌ 없음 | ✅ 있음 |
| 색 | 패턴 구분용 임의 5색 | ✅ 진짜 (민트/노랑) |
| 좌표계 | 패턴 로컬 (ISSUE-011) | 노드 변환 박힘 |

**둘이 배타인 이유가 좌표계다** — 겹치면 같은 옷이 어긋난 채 두 벌 보인다.
ISSUE-011이 닫히면 겹쳐 보기가 오히려 검증 도구가 된다.

⚠️ **스냅샷은 시뮬을 돌린 뒤에 찍어야 옷이 입혀진다.** 익스포트는 찍는 순간의
포즈를 담는다. 로드 직후 찍으면 패널이 펼쳐진 채로 나오고, 이걸 모르면 "익스포트가
깨졌다"고 오진한다. sample.zls는 애초에 드레이프 상태가 아니다(사용자 씬은 맞다).

## ⚠️ 비교 기준을 확인하고 시작할 것 (2026-08-09에 배운 것)

사용자가 "이렇게 나와야 한다"며 준 스크린샷이 **`zelusSandBox`가 아니라 Z-Emotion의
실제 제품 앱**이었다. 그걸 모르고 조사팀 + Scout까지 붙여 몇 시간을 썼다.

**구분법**: `zelusSandBox`는 창이 하나뿐이고 ImGui `Operation` 패널 안에 접이식 헤더
10개가 들어 있다(`MainGUI.cpp:210`). **상단 메뉴바가 없다** — `BeginMainMenuBar`가
코드에 아예 없다. 제품 앱은 File/Edit/Window/Help 메뉴바 + 3D·2D·속성 3분할이다.

조사 자체는 값이 있었다 — 그 과정에서 ISSUE-014(`setParams` 무효 필드)를 찾았다.
하지만 **화면 비교로 시작하는 조사는 "어느 앱 화면인가"를 먼저 못 박아야 한다.**

관련 사실: `.zls`에 `drapingBoard/`가 들어 있고 저장된 드레이프(`simWorldData` 2.9MB)를
갖고 있다. 로드만으로는 적용되지 않고 `ZestManager::LoadDrapingItem`을 불러야 하는데
(데스크톱은 Apply 버튼), **우리 프로토콜에 그 op이 없다**(백로그 B그룹).

## UI 검증 하네스 (2026-08-09, `af2d2b3`)

`cd frontend && npm run verify:ui` — **68건 22초.** Playwright + **시스템 Chrome**
(`channel: 'chrome'`. 번들 chromium은 이 머신의 빌드와 버전이 어긋난다). 헤드리스인데
WebGL이 ANGLE/D3D11로 실제 GPU를 탄다.

**전제**: 게이트웨이(3000)와 Vite(5173)가 떠 있어야 한다. 하네스는 서버를 안 띄운다.

이 하네스가 Node 스모크로는 **원리적으로 못 덮던 것**을 덮는다 — `frameCamera()`
실제 동작, 배타 모드, 텍스처가 진짜 픽셀이 되는가. 스크린샷은 `frontend/verify-out/`
(gitignore).

**함정 둘:**

- **⚠️ 연달아 돌리면 워커가 죽는다**(ISSUE-013, code 4001). 게이트웨이를 재시작하면
  68/68이 된다. **하네스가 "UI 회귀가 아니라 워커가 죽은 것"이라고 지목하고 5초 만에
  중단하도록** 만들어져 있으니, 그 메시지가 나오면 UI를 의심하지 마라.
- **색 판정이 두 겹인 이유**: 민트 basecolor의 채도가 0.173이라 회색을 거르는 문턱
  0.22를 원리상 못 넘는다. 그래서 "색조는 민트인데 채도는 옅은" 띠를 따로 센다.
  이게 판정력의 핵심이다 — 실시간 팔레트의 민트빛(채도 0.289)은 그 띠를 못 채우므로
  "파일의 색인가 팔레트인가"가 갈린다.

`readPixels`로 화면을 재려 하지 마라 — 프레임 표시 후 백버퍼가 비어 0이 나온다.
`page.screenshot()`의 PNG가 컴포지터 결과라 실제 픽셀이다.

## 아바타·머티리얼 — 조사 결과 (2026-08-07)

사용자가 데스크톱 스크린샷("이렇게 나와야 돼")을 준 뒤 조사 3갈래 + Scout을 돌렸다.
결론은 **엔진에 다 있고 우리 프로토콜에 노출이 안 됐을 뿐**이다.

- **아바타 지오메트리는 GL 없이 나온다.** `ztSceneQueryInterface::GetAvatars()` →
  `ztDesignAvatar::GetMeshes()/GetRenderMeshs()` → `zsTriMesh`. **옷과 같은 자료구조라**
  워커의 `MeshData()` 재포장 로직이 그대로 재사용된다. Zest 전체에 GL 호출 0건이고,
  같은 API를 쓰는 참조 구현(glTF 익스포터)이 저장소 안에 있다.
- **⚠️ `IsUpdateRenderMesh()` / `IsForceUpdatedMesh()`를 부르지 마라.** const인데
  읽는 순간 mutable 플래그를 지운다. 시뮬 실행기가 이미 소비 중이라 **워커가 부르면
  시뮬 동작이 바뀐다.** 두 조사가 독립적으로 잡았다.
- **아바타 메시는 옷과 달리 이중버퍼 보호가 없다.** `ztGlobalMutex`를 직접 잡아야
  찢어진 프레임을 피한다.
- 아바타는 시뮬 중 움직이지만 애니메이션이 끝나면 정지한다(`IsAnimationFinished()`).
- 크기: 아바타 25,357정점(사용자 씬 28,564) — **옷의 3.3배.** 프레임당 304KB라
  매 프레임 밀 물건이 아니다.
- **색은 텍스처가 아니라 basecolor 스칼라다.** 사용자 씬 내부 JSON 실측: 노랑
  `[0.9254902, 0.8117647, 0.4705882]` 16개, 민트 `[0.7333333, 0.8862745, 0.8156863]` 8개.
  텍스처는 직물 조직 무늬다. **즉 실시간 뷰에 색만 보내도 대부분이 재현된다.**
- `zwGltfExporter::SaveBuffer()`가 **파일 없이 메모리로 GLB를 준다**(파일 생성 0건 실측).
  PROJECT_ANALYSIS가 DEAD로 분류했지만 이유는 "호출자가 없어서"였다. 지금은 안 쓴다.

**ISSUE-011(좌표계)은 닫혔다** (`6091b10`) — 옷 패턴 변환을 `topology:true`일 때 한 번만
싣고 `Mesh`에 건다. 실시간 옷의 월드 박스가 glTF 정답지와 **0.035cm 이내**로 맞는다.
그때 배운 것: **박스만 비교하는 테스트는 좌표계 문제에 불충분하다** — 쿼터니언을
`[w,x,y,z]`로 잘못 읽어도 AABB는 거의 안 흔들려서(오차 0.036cm) 통과했다. 실제로
잡은 것은 "점이 어디에 놓이는가"를 보는 단언이었다.

**남은 순서**: `materials` op(색만) → `avatarMesh` op. 색은 basecolor 스칼라뿐이라
코드가 적고, UI 하네스가 **이미 그 색을 픽셀로 판정할 줄 안다**(§6의 민트/노랑 띠) —
완성되면 실시간 화면에서도 같은 단언이 서야 한다.

## #9 백프레셔 (2026-08-07, `cc2b788` + `ade7077`)

무제한 1.82MB/s 문제가 닫혔다. 상세는 TASKS.json #9와 코드 주석에 있고, 여기엔
**다음 세션이 오해할 만한 것만** 적는다.

- 서버 스모크가 283 → **310건**이 됐다(#9가 27건 추가). 소요 6.7 → 9.8초.
- **테스트에서 백프레셔 경로를 밟으려면 프레임이 커야 한다.** 30바이트 프레임은
  아무리 몰아쳐도 소켓 쓰기가 동기로 끝나 `ws.bufferedAmount`가 0에서 안 올라간다 —
  임계값을 0으로 낮춰도 발화하지 않는다. `smoke.ts`의 `bulkyMesh()`(실측 47.8KB 흉내)와
  `frameHighWaterMark: 0`이 짝이다. 이걸 모르면 "구현이 안 됐다"고 오진한다.
- **가짜 세션이 실제 워커보다 강한 단언을 준다.** `frameHighWaterMark: 0`이면 파이프라인
  깊이가 1이라 `보낸 수 = 도착 + 드롭`이 정수로 맞아떨어진다. 실제 워커로는 워커가 몇
  프레임을 냈는지 몰라 원리상 불가능하다. 실제 워커 쪽(9-3)은 "진짜 1.9MB/s가 289KB에서
  멎는다"를 보는 역할이고, **드레인 경로에는 이빨이 없다**(재개 후 시뮬이 계속 돌아
  새 프레임이 그냥 나가므로 `#drainFrame`을 죽여도 통과한다). 그 몫은 9-1이 전부 진다.
- **ISSUE-007이 10회 중 0회 재현됐다.** 실제 워커를 하나 더 띄우는 테스트를 추가했는데도
  안 났다. 빈도가 낮아진 것인지 조건이 달라진 것인지는 모른다.

미검증으로 남긴 것 2건(`#detach`의 보류 프레임 비우기에 테스트 없음 / 위의 9-3 이빨
없음)은 TASKS.json #9 `detail` 말미에 있다. **ISSUES.md로 올릴지는 미결** — ISSUE-004가
정확히 같은 성격(등급 `진단`)이라 전례는 있다.

## 테스트 자산

세 러너가 있고 전부 프레임워크 없이 `check(label, ok)` 스타일이다.

| 명령 | 위치 | 건수 (2026-08-11) |
|---|---|---|
| `npm run smoke` | `backend` | **102** (SDK + 2D 배치 + 재질 + 액세서리 §5.11 + 아바타 치수 §5.12 + **직물 §5.13**) |
| `tools/probe-avatar.ts` | `backend` | 측정 도구 — 체형 29개·치수 25개 (테스트 아님) |
| `tools/probe-surface.ts` | `backend` | 측정 도구 — 서피스 24개 크기 (테스트 아님) |
| `tools/probe-design2d.ts` | `backend` | 측정 도구 — 커브·봉제선·스티치·좌표계 (테스트 아님) |
| `tools/probe-bodyparam.ts` | `backend` | 측정 도구 — 체형 변형·드레이프. `--geom`(glTF 해시 대조) `--exe Release` `--delta` |
| `npm run smoke:server` | `backend` | **431** — 전부 초록 (텍스처 거절 갈래 §11-1 포함) |
| `npm run smoke` | `frontend` | **921** (프로토콜 + 3D + 2D + 상태기계 + 파라미터 + 재질 + 탭 + 도면 스위치 + 다국어 §16 + 서랍 §17 + 치수 낡음 §18 + **직물 §19**) |
| `npm run verify:ui` | `frontend` | **200** (Playwright, 서버 2개 필요, ~114초) — **⛔ GPU 이슈로 보류 중** |
| `npm run sweep:params` | `backend` | 측정 도구 (테스트 아님, 실제 워커 ~6분) |

**DOM-free 모듈이 자동 테스트가 붙는 유일한 자리다** — `viewer3d/{cloth,loader,
frameStream,snapshot}.ts`와 `panels/{playback,shortcuts}.ts`. 로직이 `viewer.ts`나
`main.ts`로 새면 Node에서 못 돌고 수동 확인만 남는다. 새 단위를 만들 때 **상태 기계를
DOM-free 모듈로 빼고 `main.ts`는 배선만** 하는 것이 확립된 패턴이다(`playback.ts`가
가장 최근 모범 — 의존성을 `port`로 주입받아 가짜로도 실제 게이트웨이로도 돈다).

**⚠️ 그래도 배선은 화면에서만 보인다.** #14에서 실증됐고 **#16에서 12건으로 재현됐다** —
결함을 심은 12건이 **전부 스모크는 초록이고 UI 하네스만 잡았다.** 두 하네스가 겹치는
것이 낭비가 아닌 이유다. #16은 **스모크에 한 건도 못 더했다**(새로 생긴 것이 전부
DOM과 배선이라 Node에서 import조차 안 된다).

## 조사 문서에 없던 사실들

- **SDK 프리빌드 엔진은 `ZELUS_USE_LICENSING=ON`이다.** `PROJECT_ANALYSIS.md`가 "세 빌드
  모두 OFF"라고 한 것은 소스 빌드 기준이었다. SDK 바이너리로 시뮬을 돌리면 라이선스를
  요구하며 실패 시 에러를 무한 반복한다(73만 줄까지 확인). 현재는 저장소의
  `cmake_build_src`와 우리가 빌드한 Release 엔진(둘 다 LICENSING=OFF)을 쓴다.
- **저장소 최상위 CMakeLists로는 소스 빌드가 안 된다.** `InstallSDK.cmake`가 Lumia를
  export하는데 Lumia가 링크하는 Zelus가 export set에 없어 생성 단계에서 죽는다. 그래서
  `backend/native/engine/CMakeLists.txt`로 엔진만 따로 빌드한다.
- **`Initialize()`는 GL 없이 통과한다.** §12.4가 우려한 크래시는 없고 §12.5의 3줄 우회는
  애초에 불필요했다. 반대로 `Initialize()`를 건너뛰면 `LoadZls`가 죽는다.
- **`ZestManager::LoadZls`가 파일 형식을 검증하지 않는다.** 14바이트짜리 아무 텍스트나
  넘겨도 `loaded:true`를 준다. 없는 파일만 실패한다. 사용자가 깨진 파일을 올리면
  "성공"을 받고 빈 화면을 본다 — 어디선가 다뤄야 한다.
- **워커 인덱스가 `Int32Array`인데 WebGL은 INT를 안 받는다.** `cloth.ts`가 같은 비트
  패턴을 `Uint32Array`로 재해석해 복사한다. 지우면 아무것도 안 그려진다.

## 미해결 (진행에 영향)

- **라이선스 미확보.** 배포용 빌드를 검증하지 못했다. 동접 = 인스턴스 수이므로 계약
  조건이 실질적 상한이 된다. 사용자는 "라이선스는 사용자 PC에 있을 것"이라고 했다.
- **동시 인스턴스 미측정.** 세션당 360MB는 알지만 CPU가 먼저 막힐 가능성이 있다.
- **⚠️ 이 머신에서 도구가 무작위로 프로세스째 죽는 계열이 셋이다.** 같은 뿌리인지는
  미확인이고, **CI를 붙이기 전에 결정해야 한다.**
  - **ISSUE-007** — 서버 스모크가 `0xC0000409`로 죽는다(빈도가 낮아졌다. 최근 16회 0회)
  - **ISSUE-013** — 워커가 `code 4001`로 죽는다. 첫 재생에서도 난다. 게이트웨이를
    재시작하면 풀린다
  - **ISSUE-016** — `npm run build`가 3~5회 중 2~3회 rollup 파서 panic으로 죽는다.
    깨끗한 트리에서도 재현되므로 우리 코드와 무관하다
  - 공통 대응: **끊긴 회차를 통과로 세지 마라.** 재실행하고, 하네스가 원인을
    지목하면(예: "워커가 죽었다") UI를 의심하지 마라

## 새 기능을 여는 순서 (L-3에서 확립, 2026-08-11)

엔진에 있는 기능을 웹으로 여는 일이 반복되므로 순서를 적어 둔다.

1. **엔진 헤더에서 API가 실제로 있는지 먼저 잰다.** `zelusSandBox_Cobalt/SDK/include/`
   와 `zelusSandBox/ZestManager.h`. ⚠️ **저장소 전체 grep은 2분을 넘긴다**(vcpkg·빌드
   캐시). `Glob`으로 헤더를 먼저 찾고 그 파일만 볼 것
2. **워커가 그 객체에 닿는지 따로 잰다.** ★ **메서드가 있는 것과 우리가 부를 수
   있는 것은 다른 사실이다** — `ztAvatarManager`에 setter가 다 있는데 포인터를 얻는
   공개 경로가 없었다. 메모리의 "엔진에 완비돼 있다"가 이 지점에서 절반만 맞았다
3. **워커 op을 짜고 컴파일까지** (게이트웨이 떠 있어도 됨) → 게이트웨이 내리고 링크
4. **프로브 스크립트로 실측.** ★ **되읽은 값만으로는 부족하다** — 값이 저장된 것과
   지오메트리가 바뀐 것은 다른 사실이다. **glTF 익스포트 해시를 대조**해 가른다.
   이 방법이 L-3a·L-3b 양쪽에서 결정적이었다
5. SDK → 게이트웨이(`OPS` 테이블이 `Record<Op, OpRule>`이라 **안 더하면 컴파일이
   깨진다** — 좋은 설계다) → 프론트 프로토콜
6. **스모크의 화이트리스트 인벤토리를 갱신한다.** op을 더하면 `smoke.ts`의
   개수 단언이 **반드시 실패한다.** 그게 제 역할이다 — 의식적으로 등재하라는 뜻
7. 판단은 `panels/`(DOM-free), 그리기는 `ui/`, 배선은 `main.ts`
8. **브라우저로 눈 확인** → 커밋

**응답 설계 규칙 하나**: 쓰기 op은 **요청값을 메아리치지 말고 되읽어 실어라.**
메아리치면 엔진이 아무 일도 안 했을 때조차 성공으로 보인다. 실제로 이 규칙이
`UpdateAvatar`의 반영 여부를 가른 자리다.

## 이 세션에서 확립된 작업 습관 (지킬 것)

- **Builder가 끝나면 `git diff --numstat -- '*smoke.ts' '*verify/ui.ts'`를 찍어라.**
  0이 아니면 되돌리고 Tester에게 넘긴다. 실제로 한 번 어긴 적이 있다([[subagent-workflow]])
- **Tester에게 돌연변이를 요구하라.** 테스트가 코드에 맞춰 휘었다면 돌연변이가 안
  잡힌다. 지금까지 전 단위에서 이게 마지막 안전장치 역할을 했다
- **화면이 바뀌는 단위는 `npm run verify:ui`로 확인하고 커밋한다.** 메인 세션이 직접
  돌린다(서브에이전트 말고)
- **커밋 전 매번**: 회사 저장소 `git status --short`(기준점 = 미추적 `.claude/`,
  `cmake_build_cuda/`, `cmake_build_src/`, **`build/internal/새 텍스트 문서.txt`**
  — 2026-08-12 밤에 4번째가 늘어 있었다. 우리가 만든 것이 아니고 추적 밖이라
  저장소 내용은 무변경이다) + 좀비 `zelusSandBoxd-demo.exe` 확인
- ⚠️ **`backend npm run typecheck` 의 `tools` 갈래가 깨져 있다**(2026-08-12 확인).
  `tools/probe-bodyparam.ts` 가 없는 필드 `MeasureResult.skipped` 를 쓴다. HEAD
  에서도 깨지므로 새 변경을 의심하지 말 것 — 제품은 `npx tsc -p tsconfig.json`
  으로 따로 볼 수 있다
- ⚠️⚠️ **개발 화면은 `:5173`(Vite)이다.** `:3000` 은 게이트웨이가 `frontend/dist`
  를 서빙하는데 그 번들이 **2026-08-09 자로 멈춰 있다** — 열면 "옛날 작업 화면"
  이 뜬다(사용자가 그렇게 알아챘다). ⚠️ **`preview_start gateway` 가 매번
  `:3000` 탭을 연다** — 게이트웨이를 재시작한 뒤에는 탭 URL 을 확인하고
  `:5173` 으로 되돌릴 것. `dist` 를 최신화하려면 `npm run build` 인데 그건
  ISSUE-016 으로 3~5회 중 2~3회 죽는다
- ⚠️ **Vite 가 조용히 죽어 있을 수 있다.** 화면이 빈 채로 뜨면 콘솔이 아니라
  네트워크를 볼 것 — `ERR_CONNECTION_REFUSED` 면 서버가 없는 것이다
  (`preview_list` 에서 `web` 이 빠져 있다). 다시 띄우면 그만이다
- ⚠️ **게이트웨이를 재시작한 직후에는 아바타가 검게 보인다.** 텍스처 19.6MB 를
  내려받는 중이라 그렇고 몇 초 뒤 피부가 돌아온다. `⚠ 거절` 이 0칸이면 정상이다 —
  "텍스처가 깨졌다" 로 오진하기 딱 좋다
- ⚠️ **스모크의 §9 좀비 검사는 브라우저를 띄워 두면 빨갛다.** 게이트웨이가
  세션당 워커를 하나 띄우므로 탭이 열려 있으면 그게 잡힌다(부모 PID로 확인 가능).
  깨끗한 회차를 뜨려면 `window.cobalt.client.close()` 로 세션만 끊었다가 되돌린다
- `.claude/settings.json`은 권한 허용목록이 자동 누적되는 것이라 **커밋에서 제외**

관련: [[readonly-company-repo]], [[user-comm-style]], [[dev-principle-tdd-increments]],
[[subagent-workflow]], [[stay-in-scope]], [[company-code-context-mismatch]]


---

# stay-in-scope

> memory/stay-in-scope.md

---
name: stay-in-scope
description: 작업 중 딴 결함을 발견하면 관련성만 빠르게 판정하고 멈춰라 — 원인 파기는 사용자에게 물은 뒤에. 2026-08-12에 UI 작업 중 파다가 제지당했다
metadata: 
  node_type: memory
  type: feedback
  originSessionId: a8efdbee-ccb5-4637-96b0-00b85875e27e
  modified: 2026-08-12T01:32:11.534Z
---

2026-08-12, 사용자가 UI 하나(오른쪽 칸 최소 너비 + 접기)를 요청했다. 만들어서
화면을 확인하다 화면 구석의 `⚠ 거절 4칸`을 봤고, **거기서부터 원인을 파기
시작했다** — 워커 프로브를 새로 짜고, 게이트웨이 소스를 읽고, 로그 줄을 세고,
디스크를 뒤졌다. 사용자가 끊었다:

> *"아니. 잠시만 ui 수정하는데 뭐가 그렇게 할일이 많아."*

**같은 세션에서 이미 한 번 그랬다.** 액세서리 작업을 확인하다 "씬을 두 번 연다"
를 발견하고 한참 팠다(ISSUE-025). 그때는 제지가 없었을 뿐 같은 패턴이다.

**Why:** 사용자는 요청한 것이 끝났는지를 기다리고 있는데, 화면에는 관계없어
보이는 조사가 계속 흐른다. 진행 상황을 물어야 알 수 있게 되고
([[user-comm-style]] — "지금 무슨 작업이냐"를 반복해서 묻는 이유가 이것이다),
"UI 하나 고치는 데 왜 이렇게 오래 걸리나"가 된다.

## How to apply

- **관련성 판정까지만 하고 멈춰라.** "이게 내가 방금 만든 것 때문인가"는
  빠르게 갈라야 한다 — 그건 내 작업의 일부다. 거기서 **아니라고 판정되면
  거기가 끝이다.** 한 줄로 보고하고 요청받은 일을 마친다.
- **원인 파기·수정은 물은 뒤에.** "이런 게 보이는데 지금 팔까요, 적어만
  둘까요"면 30초다. 실제로 나중에 물었더니 사용자가 **"고쳐줘"**라고 했다 —
  물어보는 것이 막는 것이 아니다. 순서만 바뀐다.
- ⚠️ **그 조사가 결국 관련 있는 것으로 드러나도 마찬가지다.** 이번 것은
  실제로 사용자가 곧바로 알아챈 증상("첫번째 패널 이전으로 돌아간거 같은데")의
  원인이었다. 그래도 **먼저 물었어야 한다** — 결과가 좋았다고 순서가 옳았던
  것이 되지는 않는다.
- **한 세션에 커밋이 쌓이는 것 자체는 문제가 아니다.** 이날 4개를 커밋했고
  사용자는 그건 문제 삼지 않았다. 문제는 **요청한 것이 안 끝난 상태로 딴
  것을 파는 것**이다.

관련: [[user-comm-style]], [[dev-principle-tdd-increments]], [[web-service-progress]]


---

# company-code-context-mismatch

> memory/company-code-context-mismatch.md

---
name: company-code-context-mismatch
description: 회사 코드를 우리 워커로 옮길 때 — 저쪽은 1회성 CLI라 맞던 것이 상주 워커에서는 틀린 동작이 된다. 2026-08-11에 네 자리에서 걸렸다
metadata: 
  node_type: memory
  type: project
  originSessionId: 4a4009d4-9189-4fc5-8b07-b7380a11429b
  modified: 2026-08-11T12:05:15.982Z
---

회사 저장소가 `master` → **`console_application`** 브랜치로 바뀌며 헤드리스
CLI 타깃 `consoleApplication/`(= `Cobalt.exe`)을 추가했다. 앞으로도 회사가
기능을 여기에 얹을 것이므로, 옮길 때 무엇을 의심할지 적어 둔다.

## 브랜치의 실체 — 12,597줄 중 우리에게 새로운 건 250줄

| 회사가 추가한 것 | 우리 |
|---|---|
| `UpdateBodyParameter`(치수 변형), 드레이핑 호출 흐름, `BodyParameterReader.h` | ✅ 가져옴 |
| `consoleApplication/ZestManager.{h,cpp}` | ❌ **복사본. `SetExportGLTF()` 선언이 삭제돼 있어 우리 `main.cpp`가 깨진다** |
| `core/`·`scene/` 14개, `appdata/`·`media/` 87개 | ❌ `zelusSandBox/` 원본과 동일. 우리는 원본을 컴파일 중 |

즉 **파일을 가져오지 말고 로직만 베껴 우리 `protocol.cpp`에 구현한다.**
`zelusSandBox/`와 `SDK/`는 이 브랜치에서 한 줄도 안 바뀌어서 우리 빌드는 그대로 된다.

## ★ 핵심 — 회사 맥락에서 맞던 코드가 상주 워커에서는 틀리다

**회사 CLI는 프로세스 수명 = 요청 1건**이다. 그래서 취소·타임아웃·격리가
`kill` 하나로 공짜였고, 그 전제에 맞춰진 코드가 우리 쪽에서는 버그가 된다.
2026-08-11에 **네 자리**가 걸렸다:

| | 회사에선 왜 괜찮았나 | 우리에겐 |
|---|---|---|
| **기본값** `iterations=1`, `stepCm=100` | JSON에 명시하는 전제 | `(int)(maxDest/100)=0` → 중간 단계가 사라져 **몸이 한 번에 변하고 옷을 뚫는다**. 엔진팀 문서 값은 **6 / 1.0** — 회사 struct 기본값과 문서가 다르다 |
| **`Pause()`가 검증보다 먼저** | 시뮬을 시작한 적이 없어 no-op | **실패한 요청 하나가 조용히 재생을 멈춘다** |
| **`null`을 `rejected`로** | 파일 리더가 `-1`로 걸러 줌 | 문서상 `null`은 정상 입력("자연스러운 임의 값으로 계산됨"). 실제 파일을 그대로 보내면 23개가 rejected가 되고 전부 null이면 에러 |
| **되읽기 없음** | 적용 → 파일 뽑고 종료 | 화면이 "적용됨"을 보여줄 값이 필요하다 |

**⚠️ 회사도 버그를 낸다.** `8d73f85 "Modify arm length"`가 그 예다 — 목표 배열을
씬의 `measurementExpectedValues`에서 출발시키면 낡은 목표·잠금이 남아 **안 보낸
부위가 끌려간다.** 우리 실측: 허리만 +15cm 했는데 **팔이 57.08 → 38.33 (−18.7cm)**.
현재 실측치 + `isLock=false`로 새로 초기화하면 −0.4cm(자연스러운 재계산)로 내려온다.

**Why:** 옮긴 코드가 컴파일되고 크래시도 안 나면 "됐다"고 보이는데, 위 넷은
**전부 조용히 틀린 동작**이다. 화면에 옷이 좀 이상하게 걸리거나, 값이 안 변하거나,
재생이 멈추는 식이라 원인을 코드에서 찾기 어렵다.

## How to apply

- **옮기기 전에 "저쪽은 이 코드를 몇 번, 어떤 수명에서 부르나"를 먼저 물어라.**
  1회성 → 상주로 옮기는 것이 이 프로젝트의 반복 패턴이다.
- **기본값을 그대로 베끼지 마라.** 회사 struct 초기값이 그들의 문서와 다른 실례가 있었다.
  **엔진팀이 준 문서가 정본이다.**
- **되읽기는 반드시 다시 설계하라.** 요청값을 메아리치면 엔진이 아무 일도 안 했을 때도
  성공으로 보인다. 그리고 **읽는 객체가 쓰는 객체와 같은지 확인하라** —
  `ztDesignAvatarData.zetaData`(씬 데이터 사본)와 `ztDesignZeta`(살아있는 객체)가
  동기화되지 않아 값이 영원히 안 변하는 자리가 있었다([[web-service-progress]]).
- **★ 갈라내는 법은 glTF 익스포트 해시다.** 응답 값만으로는 "안 변했다"를 결론지을 수
  없다. 해시가 바뀌면 몸은 변한 것이고 읽기 경로가 틀린 것이다. 이 방법이 이 프로젝트에서
  세 번 결정적이었다.
- **★ 커밋한 뒤 그 자리를 옛 코드로 되돌려 재보는 실험이 싸고 강력하다.**
  팔 버그가 진짜였는지(−18.7cm vs −0.4cm)를 그렇게 갈랐다. ⚠️ **기준 해시를 먼저 뜨고
  (`git hash-object`) 원복을 그 자리에서 확인할 것** — 심어 놓고 세션이 끝난 사고가 있었다.
- **저쪽에 없는 것을 우리가 만들어야 한다**는 사실을 잊지 마라. 취소·타임아웃·진행률·
  자원 상한은 회사 코드에 아예 없다. 지금은 계획 밖으로 미뤄 뒀다(백로그).

관련: [[web-service-progress]], [[readonly-company-repo]], [[subagent-workflow]]


---

# read-the-source-not-the-screenshot

> memory/read-the-source-not-the-screenshot.md

---
name: read-the-source-not-the-screenshot
description: 참고 스크린샷을 명세로 받았을 때 그림을 흉내내 값을 지어내지 말고 데스크톱의 그리는 코드를 먼저 읽어라 — 2026-08-11에 두 번 틀렸다
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 47edd9ed-4321-44f7-b75b-bf3923bdbda2
  modified: 2026-08-11T08:34:52.361Z
---

사용자가 데스크톱 앱 스크린샷을 주며 *"이미지처럼 나와야 한다"*고 할 때가 있다.
그때 **그림을 눈으로 흉내내며 숫자를 지어내면 안 된다.** 데스크톱이 이미 그리고
있으므로 **그리는 코드가 명세의 정본이다.**

2026-08-11에 봉제 대응 점선에서 이걸 어겨 **두 번 연속 틀렸다**:

| | 점선 수 | 색 |
|---|---|---|
| 1차 (내가 발명) | 봉제선당 중점 하나 = 45줄 | 회색 1종 |
| 2차 (내가 발명) | 길이 4cm마다 = 199줄 | 황금각 45종 |
| **정답 (엔진)** | **108줄** | **22종** |

답은 `zelusSandBox_Cobalt/zelusSandBox/Seam2DRenderer.cpp` 한 파일에 다 있었다 —
`seam->GetPositionsForDraw(p0, p1, world=true)`와 `seam->GetColor()`. **개수도
위치도 색도 우리가 정할 것이 하나도 없었다.**

**Why:** 지어낸 값도 화면에서는 그럴듯하다. 45줄도 199줄도 "점선이 있는 도면"으로
보였고, 사용자가 두 번 지적해 줘서야 갈렸다. **비교 대상 없이는 눈으로 판정이
안 서는 종류**이므로 사람의 눈에 기대는 것 자체가 설계 오류다.

**How to apply:**

- **⛔ 회사 저장소가 읽기 전용이라는 것은 "읽지 말라"는 뜻이 아니다.** 수정·커밋만
  금지다. 데스크톱이 그리는 것을 옮기는 작업이면 **`zelusSandBox/*Renderer*.cpp`를
  먼저 열어라** (`Renderer2D.cpp` · `Seam2DRenderer.cpp` · `Renderer3D.cpp`).
- **⚠️ 저장소 전체 grep은 2분을 넘긴다**(vcpkg·빌드 캐시). `Glob`으로 파일을 먼저
  찾고 그 파일만 `Grep`할 것. `CLAUDE.md`가 경고해 둔 것을 실제로 밟았다.
- **"직렬화에 없다"와 "데이터에 없다"는 다른 사실이다.** `.zls`의 `seams` JSON에
  색 필드가 없어서 "색은 우리가 정해야 한다"고 결론지었는데, `ztDesignSeam` 객체가
  `GetColor()`를 갖고 있었다. **파일에 없으면 객체를 확인하라.**
- 같은 조사로 곁가지 수수께끼가 풀리는 일이 잦다. 이번엔 참고 이미지의 빗금이
  노치가 아니라 `RenderArrowNotch`(봉제 방향 화살표)라는 것과, 흰 테두리 원이
  봉제선 끝점이라는 것이 같이 나왔다 — 씬의 `notches`가 `null`인데 이미지에
  빗금이 있던 모순이 그렇게 풀렸다.

관련: [[web-service-progress]], [[readonly-company-repo]], [[subagent-workflow]]


---

# machine-instability

> memory/machine-instability.md

---
name: machine-instability
description: "이 머신은 PC가 통째로 멈추고 프로세스가 0xC0000005로 죽는다 — 테스트를 한 번에 하나씩 돌리고, 끊긴 회차를 통과로 세지 말 것"
metadata: 
  node_type: memory
  type: project
  originSessionId: abf17fd7-2d86-4dd4-9a57-b55621013229
  modified: 2026-08-11T01:12:48.359Z
---

**2026-08-10에 PC가 통째로 두 번 멈췄다.** 원인 미확정. 이 사실을 모르고 작업하면
멀쩡한 코드를 의심하며 시간을 태운다 — 실제로 그날 진단에 실제 작업보다 많은
시간이 들어갔다.

## 증상 넷 — 전부 같은 계열로 보인다

| | 무엇 | 코드 |
|---|---|---|
| ISSUE-007 | 서버 스모크가 죽는다 | `0xC0000409` |
| ISSUE-013 | 워커가 죽는다. 게이트웨이 재시작하면 풀린다 | `code 4001` |
| ISSUE-016 | `npm run build`가 3~5회 중 2~3회 죽는다 (rollup 네이티브 파서) | `0xC0000005` / rust panic |
| **신규** | **PC가 통째로 멈춘다** | — |

**같은 뿌리인지는 미확인이다.** ISSUE-016은 깨끗한 트리에서도 재현되므로 우리 코드와
무관한 것이 확정돼 있다.

## ⚠️ 브라우저 탭도 죽는다 (2026-08-11 신규)

메인 세션이 Chrome 도구로 화면을 확인하던 중 **탭이 `Target crashed`로 죽었다.**
직전 동작은 **스냅샷 렌더**(glTF 36MB를 three.js로 세우는 구간)였고, 그 직전에
스크린샷 권한 오류(`Cannot access contents of the page`)가 먼저 났다.

- **탭이 죽어도 워커·게이트웨이는 멀쩡했다.** 새로고침하면 씬을 다시 로드하고
  복구된다. 코드 문제로 오진하지 말 것
- **확대 캡처를 연속으로 찍지 마라.** 2026-08-10 PC 먹통 2회도 그 구간이었다
- **`javascript_tool`로 상태를 읽는 편이 스크린샷보다 싸다.** 값 판정은 그쪽으로,
  스크린샷은 "레이아웃이 맞나" 한 장만

## ⚠️ 재부팅 직후에는 `node`가 100% 죽을 수 있다

2026-08-10 실측: 첫 재부팅 뒤 `session.ts`를 **정적 import만 해도** `0xC0000005`로
죽었다(5회 5회 전부). scratchpad의 `console.log` 한 줄짜리 스크립트도 죽었다.
**두 번째 재부팅 뒤 정상으로 돌아왔다 — 크래시 여파였지 코드 문제가 아니었다.**

**코드를 의심하기 전에 자명한 스크립트 하나로 환경부터 확인할 것.** 그때 헛다리를
짚은 순서가 이랬다: Tester 추가분 의심 → 스태시했는데 그대로 죽음 → 큰 파일 의심 →
아님 → 경로 의심 → scratchpad만 깨진 것 발견 → 그런데 프로젝트 경로도 죽음 →
결국 재부팅으로 해결.

## GPU — CUDA와 그래픽을 갈라서 볼 것

사용자가 "CUDA 드라이버가 제대로 안 깔렸다"고 했다. **둘은 다른 스택이다:**

- **CUDA(연산)** — **우리는 아무데도 안 쓴다.** `CLAUDE.md`대로 v1은 CPU 솔버만
- **WebGL/D3D11(그래픽)** — `npm run verify:ui`가 헤드리스 Chrome으로 이걸 탄다.
  **실제로 잘 돌아갔다**(200/200 통과, 스크린샷 정상)

**⛔ 그래도 `verify:ui`를 보류했다** — 먹통 2회가 모두 그걸 반복해 돌리던 구간에
났다. 인과는 미확정이고 상관관계뿐이다.

**CUDA 재설치로 워커를 GPU화하는 것은 답이 아니다** (사용자가 물었고 확인했다):
`backend/native/CMakeLists.txt:91`이 `ZS_USE_CUDA`를 **의도적으로 뺐다** —
`cmake_build_src`가 OFF로 빌드돼 있어 정의가 어긋나면 ABI가 깨진다. 엔진의 CUDA
빌드(`cmake_build_cuda`)는 산출물 **3개**에서 멈춰 있다. 필요한 건 **디스플레이
드라이버 클린 재설치**이고 CUDA는 v1 범위 밖이다.

## How to apply

- **테스트를 한 번에 하나씩, 순차로 돌려라.** 사용자가 "컴퓨터 먹통 안 되게 나눠서
  테스트할 수 없냐"고 요청했고, `backend` → 쉼 → `frontend`로 나누니 멀쩡했다.
- **`npm run verify:ui`는 GPU를 타므로 드라이버가 정리될 때까지 보류.** 화면을 바꾸는
  단위는 그동안 착수하지 마라 — 검증이 반쪽이 된다(#16에서 12건, #15에서 3건이 그
  하네스에서**만** 잡혔다).
- **`npm run build` 금지** (ISSUE-016).
- **끊긴 회차를 통과로 세지 마라.** 재실행하고, 하네스가 원인을 지목하면(예: "워커가
  죽었다") UI를 의심하지 마라.
- 무거운 이유도 알아 둘 것: 워커는 **세션당 ~360MB**이고 씬이 **103~138MB**다.
  테스트는 그 프로세스를 수십 번 띄웠다 내린다.

관련: [[web-service-progress]], [[subagent-workflow]]


---

# subagent-workflow

> memory/subagent-workflow.md

---
name: subagent-workflow
description: "작업 한 단위당 Builder → Tester → 커밋 → Updater 서브에이전트 루프를 돌리고, 문제가 생기면 Recorder로 기록 후 중단한다"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 3c319180-81c9-46cb-9c7e-eb274b1f22f8
  modified: 2026-08-12T00:09:22.724Z
---

사용자가 설계한 작업 방식이다. `TASKS.json`의 작업 하나당 한 바퀴:

```
① Builder   구현 + 빌드만. 테스트 작성 금지, 커밋 금지
② Tester    통과 기준을 테스트로 옮기고 실행. 구현 코드 수정 금지
③ 화면 확인  메인 세션이 `cd frontend && npm run verify:ui` (화면이 바뀌는 단위만)
④ 메인 세션 커밋 (승인 불필요, 해시를 보고)
⑤ Updater   TASKS.json의 status/commit 갱신. 다른 파일 금지
```

**③이 2026-08-09에 추가됐다.** 근거는 실측이다 — 이 프로젝트에서 실제로 결함을 잡은
것은 대부분 스모크가 아니라 화면이었다(ISSUE-009·010, "시뮬을 돌려야 옷이 입혀진다",
아바타·색). Node 스모크는 `WebGLRenderer`가 없어 `frameCamera()`·배타 모드·텍스처
픽셀을 원리적으로 못 덮는다. 하네스(`frontend/src/verify/ui.ts`, 68건 22초)가 그 자리를
메운다. [[web-service-progress]]에 사용법과 함정이 있다.

실패하거나 별건 문제가 나오면 **Recorder**가 `ISSUES.md`에 기록하고, 메인 세션이
원인을 분석해 보고한 뒤 **중단하고 사용자 지시를 기다린다.**

**Why:** Tester가 코드를 못 고치는 것이 이 구조의 핵심이다. 한 에이전트가 구현하고
자기 테스트를 통과시키면 테스트가 코드에 맞춰 휘어진다. 통과 기준은 `TASKS.json`의
`test` 필드에 미리 고정돼 있어 Tester는 그것만 보고 쓴다.

**How to apply:**

- **브리핑에 선행 단위의 인계 사항을 반드시 넣는다.** 서브에이전트는 컨텍스트가
  분리돼 있어, 앞 단위에서 드러난 함정을 적어주지 않으면 같은 데 다시 빠진다.
  실제로 이게 가장 큰 효과를 냈다.
- **깨지는 기존 단언은 Builder가 "고치지 말고 보고"하게 한다.** Tester가 판단해서
  고친다. Builder가 고치면 자기 변경에 맞춰 테스트를 휘게 된다.
- **⚠️ Builder가 테스트를 쓰는 사고가 실제로 났다** (2026-08-07, ISSUE-011 작업).
  브리핑에 "테스트 작성 금지"가 있었는데 어겼고 보고에도 안 적었다. 지침만으로는
  안 막힌다 — **Builder가 끝나면 `git diff --numstat -- '*smoke.ts'`를 찍어 0인지
  확인해라.** 0이 아니면 되돌리고 Tester에게 넘긴다.
  그때 실질 위험을 걷어낸 것은 Tester의 돌연변이 검증이었다(6건 전부 빨간불). 즉
  **돌연변이를 요구하는 것이 이 워크플로의 마지막 안전장치다** — 테스트가 코드에
  맞춰 휘었다면 돌연변이가 안 잡힌다.
- Tester에게 **판단할 것을 명시적으로 물어라** — 실제 워커를 몇 번 쓸지, 어디까지
  고정할지. 좋은 Tester는 돌연변이를 심어 단언의 이빨을 확인하고, 대조군으로
  "우연히 통과"를 배제한다. 그렇게 하라고 요구하면 한다.
- 커밋은 메인 세션만 한다. 매번 회사 저장소 `git status --short`와 좀비
  `zelusSandBoxd-demo.exe`를 확인한다([[readonly-company-repo]]).
- `.claude/settings.json`은 권한 허용목록이 자동 누적되는 것이라 커밋에서 제외한다.
- **★ 브리핑에 "구현 전에 전제를 실측하고, 예상과 다르면 멈추고 보고하라"를 넣어라.**
  2026-08-10 #15에서 이 한 줄이 **한 줄도 쓰기 전에** ISSUE-018을 잡았다 — TASKS.json이
  "추가 데이터가 필요 없다"고 단언했는데 틀렸고, 그대로 짰으면 패턴 24개가 겹친 화면을
  만들어 놓고 원인을 팠을 것이다. **문서의 전제를 사실로 믿지 말고 재게 시켜라.**
- **서브에이전트가 도중에 죽어도 작업은 대개 남아 있다.** 2026-08-10에 Tester가
  중단됐는데 테스트 1,133줄이 트리에 그대로 있었고, **진짜 결함 1건까지 잡아 둔
  상태**였다. 출력 파일(`tasks/<id>.output`)에서 마지막 어시스턴트 메시지를 뽑으면
  어디까지 갔는지 알 수 있다. ⚠️ **돌연변이 원복이 끝났는지 반드시 직접 확인해라** —
  주입된 채 죽었을 수 있다(그때는 전치형 잔존 여부를 grep으로 확인했다).
- **★★ 사용자가 도구 호출을 거부해도 그 에이전트는 이미 일한 뒤일 수 있다.**
  2026-08-12에 **한 세션에서 두 번** 겪었다: 거부한 AM-2 Builder의 산출물이
  트리에 있었고(다음 Builder가 "이전 실행분이 있다"고 보고), 거부한 Tester는
  `smoke.ts` **+437줄**을 남겼다(임시 프로브 2개까지). 둘 다 **거부 직후의
  `git status`는 깨끗했고 나중에 나타났다.**
  → **중단·거부 직후 한 번, 그리고 다음 커밋 직전에 또 한 번 트리를 확인해라.**
  남은 것이 쓸모 있으면 살리되(그 Tester 작업은 실제로 좋았다) **검토 없이
  커밋하지 마라** — 내가 안 읽은 코드가 커밋에 섞인다.

관련: [[dev-principle-tdd-increments]], [[web-service-progress]], [[user-comm-style]]


---

# readonly-company-repo

> memory/readonly-company-repo.md

---
name: readonly-company-repo
description: "zelusSandBox_Cobalt는 회사 저장소라 절대 수정하지 않으며, 빌드가 그 안에 쓰지 않는지 매번 확인해야 한다"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 8278d176-8564-49b7-a189-aff124b9add5
  modified: 2026-08-06T00:39:31.371Z
---

`zelusSandBox_Cobalt/`는 회사 저장소(`z-emotion/zelusSandBox_Cobalt`, master)다. 사용자가
"이 프로젝트 레포는 건들지마"라고 명시적으로 지시했다. 파일 수정, 브랜치 생성, 커밋을 하지
않는다. 모든 작업은 루트(`d:\z-emotion\Cobalt`)의 git에 커밋한다.

**Why:** 사내 SDK 데모 저장소이고 서브모듈 3개(Zest, Lumia, vcpkg)를 물고 있다. 여기에
변경이 섞이면 회사 쪽 이력이 오염된다.

**How to apply:**

파일을 직접 고치지 않는 것만으로는 부족하다. **빌드 도구가 저장소 안에 쓰는 경우가 있다.**
실제로 `VCPKG_INSTALLED_DIR`를 저장소의 기존 트리로 지정한 채 저장소를 CMake로 구성했다가,
vcpkg가 매니페스트 모드로 그 디렉토리를 자기 소유로 간주해 **의존성 106개를 2개로 날린 적이
있다.** git status는 깨끗했다(해당 경로가 gitignore 대상이라).

그래서:

1. 저장소를 건드릴 가능성이 있는 명령 **전에** `git status --short`를 찍어 기준점을 잡고,
   **후에** 다시 찍어 비교한다.
2. git이 무시하는 경로(`cmake_build/`, `vcpkg/buildtrees/`)도 물리적으로는 저장소 안이다.
   git status만으로는 안전을 확인할 수 없다.
3. 새 산출물은 저장소 밖(`backend/native/...`)에 만든다. vcpkg 설치 트리도 우리 것을 따로 쓴다.

관련: [[web-service-progress]]


---

# user-comm-style

> memory/user-comm-style.md

---
name: user-comm-style
description: "짧은 답을 선호하고 \"한줄 설명\"을 자주 요청하며, 오래 걸리거나 되돌리기 어려운 작업은 먼저 알리기를 원한다"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 8278d176-8564-49b7-a189-aff124b9add5
  modified: 2026-08-10T07:50:27.490Z
---

이 사용자는 **짧은 답을 선호한다.** "한줄 설명", "무슨 말인지 이해 못했어 알기쉽게 한줄로",
"뭐가 문제인지 한줄 설명"을 반복해서 요청했다.

**Why:** 긴 설명이 나오면 핵심을 다시 물어봐야 해서 왕복이 늘어난다. 설계 논의처럼 깊이가
필요한 주제는 길게 써도 받아들이지만, 상태 보고나 진행 확인은 한두 줄이면 충분하다.

**How to apply:**

- 상태·진행·문제 보고는 **한두 줄**로 시작한다. 세부는 물어보면 준다.
- 설계 결정이나 트레이드오프처럼 판단이 필요한 주제만 길게 쓴다. 그때도 추천안을 먼저
  말하고 근거를 뒤에 둔다.
- **작업을 시작하기 전에 무엇을 할지 한 줄로 알린다.** 도구 호출을 승인해야 하는데 무엇을
  하려는지 모르면 거부한다. 실제로 폴더 생성과 커밋을 각각 한 번씩 중단시켰고, 이유는
  "뭐하고 있는건데", "어디에 커밋하려고 했는데"였다.
- 오래 걸리는 작업(빌드 등)은 예상 시간을 먼저 말한다. 20~40분짜리를 말없이 시작하면
  "뭐가 문제인지 이렇게 오래걸리냐"가 나온다.
- **작업 중에 "지금 무슨 작업이냐"를 반복해서 묻는다.** 2026-08-10에만 네 번 나왔다
  ("무슨 기능을 테스트 할려고 하는거냐", "지금 어디까지 진행됐냐", "뭘 하는 거냐").
  긴 서브에이전트 작업을 돌릴 때 **중간 보고를 먼저 주면 왕복이 준다.**
- **착수 전에 "이게 원래 계획에 있던 거냐 / 오래 걸리냐"를 확인한다.** 계획 밖 항목을
  제안할 때는 **그 사실을 먼저 밝혀라.** 2026-08-10에 `materials`를 추천했는데 나중에
  "원래 계획에 있던거냐"를 물었고, 계획 밖이라고 답하자 거기서 멈췄다.
- **긴 워크플로 대신 빠른 길을 선택지로 제시하면 좋아한다.** 5단계 루프가 30분~1시간
  걸릴 때 "제가 직접 하면 10분, 대신 자동 테스트는 없다"를 표로 주면 판단한다.
- **자기가 직접 하겠다는 것을 존중해라.** "내가 직접 서버를 실행할건데 명령어만
  알려줘" — 그때는 내가 띄운 것을 내리고 명령어만 준다.

관련: [[web-service-progress]]


---

# dev-principle-tdd-increments

> memory/dev-principle-tdd-increments.md

---
name: dev-principle-tdd-increments
description: "기능은 테스트 가능한 작은 단위로 쪼개 만들고, 테스트 통과 후 커밋하고 다음으로 넘어간다"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 3c319180-81c9-46cb-9c7e-eb274b1f22f8
  modified: 2026-08-06T01:01:18.804Z
---

사용자가 명시한 개발 원칙: **작은 단위 → 테스트 작성 → 통과 → 커밋 → 다음 단위.**
큰 덩어리를 한 번에 만들고 마지막에 검증하지 않는다. 각 단위가 끝날 때마다 커밋이 하나 생긴다.

**Why:** 되돌릴 지점이 촘촘해야 하고, 통과 여부가 확인된 상태에서만 다음으로 넘어가고 싶어 한다.

**How to apply:**

- 작업 시작 전에 단위 분할을 먼저 제시하고 동의를 받는다([[user-comm-style]] — 무엇을 할지 먼저 알린다).
- 테스트 스타일은 기존 것을 따른다. `backend`는 테스트 프레임워크가 없고
  `src/sdk/smoke.ts`처럼 `check(label, ok)` 헬퍼를 쓰는 손수 짠 스크립트를
  `package.json` 스크립트(`npm run smoke`)로 돌린다. 새 의존성을 들이지 말 것.
- 커밋은 루트(`d:\z-emotion\Cobalt`) git에만 한다([[readonly-company-repo]]).
  커밋 전에 어디에 무엇을 커밋하는지 말한다.

관련: [[web-service-progress]]

