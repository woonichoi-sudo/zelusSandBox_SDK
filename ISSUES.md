# ISSUES.md — 발견된 문제 기록부

이 문서는 **작업 중 발견됐지만 그 작업의 범위에서 고치지 않은 문제**를 누적한다. 대상은 `backend/`·`frontend/`의 우리 코드이며(회사 저장소 `zelusSandBox_Cobalt/`의 엔진 버그는 `PROJECT_ANALYSIS.md` §9 소관이다), 목적은 "지금 당장 안 고치기로 한 것"이 조용히 잊히지 않게 하는 것이다. 기록자는 **고치지 않고 적기만 한다** — 해결 방향을 여러 개 적되 어느 쪽을 택할지는 결정하지 않고, `결정` 항목에 `미결`로 남긴다. 새 이슈는 이 문단 아래에 **번호 순서대로 덧붙이고**(위에서 아래로 오래된 것 → 최신), 아래 [이슈 목록](#이슈-목록) 표에도 한 줄을 추가한다. 각 이슈는 `## ISSUE-NNN — 제목` 헤딩과 `<a id="issue-NNN"></a>` 앵커를 갖고, 아래 템플릿의 9개 항목(상태 / **심각도** / 발견 / 영역 / 증상 / 원인 / 영향 범위 / 검토된 해결 방향 / 결정)을 모두 채운다(심각도 등급은 [이슈 목록](#이슈-목록)의 범례를 따른다). 서술은 `PROJECT_ANALYSIS.md`의 표기 규약을 따라 **[코드 확인]**(파일을 읽거나 실행해 검증한 것)과 **[추론]**(분석·예측)을 구분하고, 코드 참조는 저장소 루트(`d:\z-emotion\Cobalt`) 기준 상대경로로 `backend/src/server/index.ts:112` 형태로 적는다.

## 이슈 목록

**심각도**는 ISSUE-005·006을 기록하며 도입했다. 그전까지의 이슈와 이 둘은 **실패했을 때 무엇이 망가지는가**가 다르기 때문이다 — 004까지는 요청 하나의 응답이 부정확하거나 회귀를 못 잡는 문제인 반면, 005·006은 **게이트웨이 프로세스 자체가 죽거나 반대로 영영 안 죽는다.** 같은 표에서 같은 무게로 읽히면 착수 순서를 잘못 잡게 되므로 열을 하나 늘렸다. 001~004는 소급해 분류만 붙였고 본문은 손대지 않았다.

| 등급 | 뜻 |
|---|---|
| **심각** | 프로세스 수명이 걸린다. 운영에서 게이트웨이가 죽거나 종료되지 않아 배포·재기동 자동화가 막힌다 |
| **차단** | 동작은 안전하지만 후속 작업이 이 위에 쌓이지 못한다 |
| **진단** | 요청은 올바르게 거절/처리된다. 에러가 원인을 지목하지 못하거나 성질이 테스트로 증명되지 않는다 |

**상태**의 `닫힘(전제부)`는 ISSUE-002를 닫으며 도입했다. 이 이슈는 **코드를 고쳐서** 닫힌 게 아니라 **문제가 성립하지 않는 구조를 택해서** 닫혔다 — 결함은 코드에 그대로 있고, 전제가 유지되는 동안만 드러나지 않는다. 그냥 `닫힘`으로 적으면 전제가 깨진 날 아무도 이 문서를 다시 열지 않으므로, 표기 자체가 "전제가 붙어 있다"고 말하게 했다.

| 상태 | 뜻 |
|---|---|
| **열림** | 미해결. 결정이 `미결`이거나 아직 반영되지 않았다 |
| **닫힘** | 코드로 해결됐다. 되살아나지 않는다 |
| **닫힘(전제부)** | 결함은 코드에 남아 있고 **전제가 성립하는 동안만** 드러나지 않는다. 결정 항목에 **무효 조건**이 명시돼 있으며, 그중 하나라도 하면 **상태를 `열림`으로 되돌린다** |

| ID | 상태 | 심각도 | 제목 | 발견 | 영역 |
|---|---|---|---|---|---|
| [ISSUE-001](#issue-001) | 닫힘 | 차단 | `Gateway.app` 게터로 등록한 라우트가 catch-all에 가려 도달 불가 | TASKS #4 (2026-08-06) | gateway |
| [ISSUE-002](#issue-002) | **닫힘(전제부)** | 진단 | `OPTIONS` 요청이 catch-all에 잡혀 404가 된다 — CORS preflight가 깨진다 | TASKS #4 후속 (2026-08-06) | gateway |
| [ISSUE-003](#issue-003) | 열림 | 진단 | 잘못된 `content-type`으로 업로드하면 원인을 지목하지 못하는 에러가 나간다 | TASKS #5 (2026-08-06) | gateway |
| [ISSUE-004](#issue-004) | 열림 | 진단 | 413 뒤 keep-alive 소켓 재사용이 테스트로 증명되지 않는다 | TASKS #5 (2026-08-06) | gateway |
| [ISSUE-005](#issue-005) | 열림 | **심각** | 종료가 진행 중인 acquire와 겹치면 `Gateway.close()`가 영원히 끝나지 않는다 | TASKS #6 (2026-08-06) | gateway |
| [ISSUE-006](#issue-006) | 열림 | **심각** | 워커 exe 경로가 틀리면 502가 아니라 게이트웨이 프로세스가 죽는다 | TASKS #6 (2026-08-06) | sdk |

---

<a id="issue-001"></a>

## ISSUE-001 — `Gateway.app` 게터로 등록한 라우트가 catch-all에 가려 도달 불가

- **상태**: **닫힘** — 2026-08-06, 커밋 `50b8dc6`으로 반영. 아래 [결정](#issue-001-결정) 참고.
- **발견**: 2026-08-06, TASKS.json #4 "HTTP 골격 + /api/health + 스모크 하네스"의 Tester 검증 중. **#4의 통과 기준(`서버 기동 → GET /api/health 200`) 자체는 PASS**다. 이 이슈는 그와 별개로, #5 이후 작업을 막는 구조 결함으로 함께 발견됐다.
- **영역**: gateway — `backend/src/server/index.ts`

> **[코드 확인]** 아래 "증상"·"원인"·"모순" 절의 행 번호는 **수정 전**(커밋 `02dec3d` 시점)의 `backend/src/server/index.ts` 기준이다. `50b8dc6`이 같은 파일을 크게 늘렸으므로 현재 파일에서 같은 행을 찾으면 다른 코드가 나온다. 기록을 사후에 고쳐 쓰지 않고 발견 당시 그대로 남긴다.

### 증상

**[코드 확인]** `Gateway`를 기동한 뒤 공개 `app` 게터로 라우트를 추가하면 그 라우트에 요청이 도달하지 않는다.

```ts
const gw = createServer();
await gw.start();
gw.app.get('/api/__probe', (_req, res) => res.json({ ok: true }));
// GET /api/__probe → 404 (기대: 200)
```

실측 결과 **404**. 응답 본문은 `backend/src/server/index.ts:113`의 catch-all이 만드는 `{ error: "찾을 수 없음: GET /api/__probe" }` 형태다.

### 원인

**[코드 확인]** `backend/src/server/index.ts`를 읽어 확인했다. 미들웨어 등록 순서 문제다.

1. `backend/src/server/index.ts:56-61` — `constructor`가 `express()`로 앱을 만든 직후 `this.#configure(this.#app)`을 호출한다. 즉 **생성자가 끝나는 시점에 이미 배선이 전부 끝나 있다.**
2. `backend/src/server/index.ts:92-127` — `#configure`가 한 번에 다음을 등록한다:
   - `backend/src/server/index.ts:98` — `express.json()` 본문 파서
   - `backend/src/server/index.ts:100` — `/api/health` 라우트
   - `backend/src/server/index.ts:112` — **404 catch-all** (`app.use((req, res) => …)`, 경로 지정 없음 → 모든 요청에 매치)
   - `backend/src/server/index.ts:118` — 에러 핸들러 (인자 4개)
3. Express는 미들웨어를 **등록 순서대로** 평가하고, `backend/src/server/index.ts:112`의 catch-all은 경로 제한이 없으므로 여기 도달한 모든 요청을 **응답을 보내며 종료**한다. `next()`를 부르지 않으므로 뒤 스택으로 넘어가지 않는다.

따라서 생성자가 끝난 뒤 `app` 게터로 붙인 라우트는 **구조적으로 항상 catch-all 뒤에 놓이며, 어떤 요청으로도 실행될 수 없다.** 기동 전에 붙이든(`createServer()` 직후) 기동 후에 붙이든 결과는 같다 — 결정적인 것은 `start()` 시점이 아니라 `#configure`가 생성자에서 이미 끝났다는 사실이다.

### 모순 — 주석이 동작하지 않는 사용법을 안내한다

**[코드 확인]** `backend/src/server/index.ts:63-66`:

```ts
/** 라우트를 더 얹고 싶을 때. #5 이후 단위가 여기에 붙는다 */
get app(): Express {
  return this.#app;
}
```

주석은 `app` 게터를 **후속 단위의 확장 지점으로 명시**하고 있으나, 위 원인에 따라 그 사용법은 동작하지 않는다. 이 문서가 이 이슈를 "#4는 PASS인데도" 기록하는 이유가 여기 있다: **[추론]** 그대로 두면 #5를 맡는 다음 사람이 주석을 신뢰해 라우트를 붙이고 404를 만나며, 원인이 자기가 방금 쓴 핸들러가 아니라 골격의 미들웨어 순서에 있다는 걸 찾는 데 시간을 쓴다. 증상(404)이 "라우트를 잘못 등록했다"는 흔한 실수와 구분되지 않아 오진하기 쉽다.

### 영향 범위

**라우트를 추가하는 모든 후속 작업이 영향을 받는다.**

| 작업 | 내용 | 영향 |
|---|---|---|
| TASKS #5 | 씬 업로드 + 목록 (`POST/GET /api/scenes`) | **직접 영향.** 다음에 착수하는 단위다 |
| TASKS #10 | export + glTF 다운로드 (`GET /api/exports/:id`) | **직접 영향** |
| (미할당) | 정적 파일 서빙 | **[추론]** 프런트엔드 배포를 붙일 때 같은 문제를 만난다 |

**영향받지 않는 것**: TASKS #6 (WS 연결 = 세션 수명)은 `backend/src/server/index.ts:69-71`의 `server` 게터로 `http.Server`를 직접 잡아 업그레이드를 처리하므로, Express 미들웨어 스택을 지나지 않는다.

### 검토된 해결 방향

Tester가 제시한 두 가지. **아직 채택되지 않았다.**

1. **`#configure`를 쪼개고 마감을 `start()`로 미룬다.** "라우트 등록"과 "catch-all + 에러 핸들러 설치"를 분리해, 후자를 `start()` 시점에 실행한다. → `app` 게터가 `backend/src/server/index.ts:63`의 주석대로 동작하게 되고, 주석을 고칠 필요가 없다.
   - **[추론]** 고려할 점: `start()`가 두 번 불릴 때 마감이 중복 등록되지 않도록 막아야 하고(`backend/src/server/index.ts:131`의 조기 반환이 이미 있으므로 그 뒤에 두면 된다), `start()`를 거치지 않고 `app`만 꺼내 쓰는 경로가 생기면 catch-all이 영영 설치되지 않는다.
2. **후속 라우트를 `#configure` 안, catch-all 앞에 직접 등록한다.** 확장 지점을 게터가 아니라 `#configure` 본문으로 삼는다. → 구조 변경이 없는 대신 `backend/src/server/index.ts:63`의 주석이 거짓이 되므로 **주석을 반드시 고쳐야 한다.**
   - **[추론]** 고려할 점: 라우트 모듈이 늘어날수록 `#configure`가 비대해지고, `backend/src/server/files.ts`(#5·#10의 예정 파일) 같은 외부 모듈이 `Gateway` 내부에 하드와이어된다.

<a id="issue-001-결정"></a>

### 결정

**2안(B) 채택.** 커밋 `50b8dc6` "fix(gateway): 라우트 등록 지점을 404 앞에 만들고 에러 status를 존중"으로 반영 완료.

**반영 내용** **[코드 확인]** (`backend/src/server/index.ts`를 읽어 확인):

- 404 catch-all은 `#configure` 안 **원래 자리에 그대로 두고**(`backend/src/server/index.ts:227`), 후속 라우트를 그 **앞**에 등록하는 지점을 만들었다(`backend/src/server/index.ts:200-224`, 주석 `// ── 라우트 등록 지점 ──`).
- **등록 경로는 둘이다.**
  1. `#configure` 안의 로컬 `use(register)` 헬퍼(`backend/src/server/index.ts:209-212`) — **항상 켜져 있어야 하는 내장 라우트용.** #5부터 여기에 한 줄씩 는다.
  2. `GatewayOptions.routes`(`backend/src/server/index.ts:78`) — **생성자에서만 읽히므로 늦게 붙일 방법이 구조적으로 없다.** 테스트·임베딩이 임시 라우트를 붙이는 정식 경로다. 내장 라우트 **뒤**, catch-all **앞**에 등록된다(`backend/src/server/index.ts:224`).
- 라우트 모듈은 `RouteRegistrar = (app, ctx) => void | RouteHooks` 형태이고, 의존성이 필요하면 함수를 직접 export하지 말고 **팩토리**로 만든다(`backend/src/server/index.ts:52-64`의 예시 참고).
- **비동기 준비는 `RouteHooks.prepare`로 돌려준다.** `start()`가 `listen()` **전에** 순차 await하므로(`backend/src/server/index.ts:259`), 준비가 끝나기 전에 요청이 라우트에 닿는 일이 없다. 생성자에서 await할 필요가 사라졌다. **`close()` 후 재기동될 때마다 다시 불리므로 멱등이어야 한다.**
- `app` 게터는 **남기되 주석을 사실로 고쳤다**(`backend/src/server/index.ts:138-155`). 이제 "여기에 라우트를 붙이지 마라 — catch-all에 가려 조용히 404가 난다"고 명시하고, 설정 조회·미들웨어 스택 검사처럼 라우팅을 바꾸지 않는 용도로만 남겨 뒀다.

**`gw.app` 사후 등록이 404인 것은 이제 의도된 계약이다. 회귀가 아니다.** 진단 메모였던 것을 스모크의 정식 테스트로 승격해 뒀다(`backend/src/server/smoke.ts:219-227`, `'app 게터 사후 등록 → 404 (의도된 동작)'`). **[추론]** 이 단언이 200으로 깨진다면 그건 "고쳐진" 게 아니라 등록 순서가 바뀐 것이고, 그때는 등록 지점이 여전히 유일한 길인지 다시 확인해야 한다.

#### 1안(A, catch-all 설치를 `start()`로 미루기)을 기각한 이유

**이 판단 근거를 반드시 보존한다.** **[추론]** A안은 겉보기에 더 깔끔하므로 — `app` 게터 주석을 고칠 필요조차 없다 — 나중에 누군가 "A가 더 나은데 왜 안 했지"라고 다시 꺼낼 수 있다. 그때 이 문단을 읽으라고 남긴다.

> A는 "라우트는 `start()` 전에 등록해야 한다"는 **보이지 않는 계약**을 만든다. 그걸 어기면 **똑같이 조용한 404**가 난다 — 지금 고치려는 실패 모드가 **마감 시각만 뒤로 밀린 채 그대로 남는다.** B는 등록 자리가 한 곳뿐이라 그 실패 모드 자체가 없고, 한 함수 안에서 순서가 눈에 보인다. `Gateway`가 라우트 모듈을 아는 것은 결합도 문제가 아니라 **조립 지점(composition root)의 정상 역할**이다.

이로써 발견 당시 2안의 대가로 적어 둔 두 가지에 답이 붙는다. "주석이 거짓이 된다" → 주석을 고쳤다. "`#configure`가 비대해지고 외부 모듈이 하드와이어된다" → 조립 지점의 정상 역할로 받아들이기로 했다.

**검증**: 서버 스모크 **12건 → 39건, 전부 통과.** SDK 스모크 **24/24 회귀 없음.**

---

<a id="issue-002"></a>

## ISSUE-002 — `OPTIONS` 요청이 catch-all에 잡혀 404가 된다 (CORS preflight)

- **상태**: **닫힘(전제부)** — 2026-08-06 결정. **코드는 고치지 않았다.** 같은 오리진 구조를 채택해 문제가 성립하지 않게 만들었을 뿐이므로, 전제가 깨지면 되살아난다. 아래 [결정](#issue-002-결정)과 [무효 조건](#issue-002-무효-조건)을 반드시 함께 볼 것.
- **발견**: 2026-08-06, TASKS.json #4 후속 수정([ISSUE-001](#issue-001)) 검토 중. B안의 부작용이 없는지 확인하려고 돌린 실측에서 드러났다. **A/B 선택과 무관한 기존 결함이며, 등록 순서를 어떻게 바꿔도 남는다** — catch-all이 존재하는 한 성립하기 때문이다.
- **영역**: gateway — `backend/src/server/index.ts`

### 증상

**[코드 확인]** 등록된 경로에 `OPTIONS`로 요청하면 **404**가 돌아온다. 실측은 `OPTIONS /api/scenes`로 했고, 해당 경로에 `GET`과 `POST`가 등록돼 있는데도 그랬다. 응답은 `backend/src/server/index.ts:227-229`의 catch-all이 만드는 `{ error: "찾을 수 없음: OPTIONS /api/scenes" }` 형태다.

기대 동작은 404가 아니라, Express가 그 경로에 등록된 메서드를 모아 `Allow` 헤더와 함께 돌려주는 자동 응답이다.

### 원인

**[추론]** Express는 **경로는 맞고 메서드만 없는** 요청에 대해 등록된 메서드를 모아 `Allow` 헤더로 자동 응답한다. 그런데 그 자동 처리는 **라우터가 매칭을 소진한 뒤에** 일어난다. 경로 인자 없는 catch-all(`backend/src/server/index.ts:227`, `app.use((req, res) => …)`)은 **모든 요청에 매치되므로 소진 전에 먼저 응답을 끝내 버린다.** `next()`를 부르지 않으니 자동 처리 단계에 도달할 기회가 없다.

**[추론]** 즉 이것은 ISSUE-001과 **같은 뿌리(경로 제한 없는 catch-all이 스택을 조기 종료한다)에서 나온 다른 증상**이다. ISSUE-001은 catch-all **뒤**의 라우트를 죽였고, 이쪽은 catch-all **뒤에 있는 express 자체의 기본 동작**을 죽인다. 등록 지점을 catch-all 앞으로 옮겨도(B안) 후자는 그대로다 — 죽는 것이 우리가 등록하는 무언가가 아니라 프레임워크의 내장 단계이기 때문이다.

### 영향 범위

**브라우저 CORS preflight가 깨진다.**

**[추론]** 프론트엔드(TASKS #11 이후)가 Vite 개발 서버(기본 5173)에서 게이트웨이(기본 3000)를 **직접** 호출하면 오리진이 달라 브라우저가 preflight `OPTIONS`를 먼저 보낸다. 그게 404를 받으면 브라우저는 preflight 실패로 보고 **본 요청 자체를 차단한다.** 증상은 네트워크 탭의 404 하나와 콘솔의 CORS 에러로 나타나며, 정작 본 요청은 서버 로그에 흔적조차 남지 않는다.

Vite 프록시로 같은 오리진을 만들면 preflight가 아예 생기지 않아 피할 수 있다. **다만 그건 결정 사항이지 자동으로 되는 게 아니다** — 프록시를 설정하지 않으면 그대로 걸린다.

| 대상 | 영향 |
|---|---|
| TASKS #11 이후 프론트엔드 | **직접 영향.** 프록시를 쓰지 않는 경우에 한한다 |
| `curl`·스모크 등 서버 대 서버 호출 | **영향 없음.** preflight를 보내지 않는다 |
| 단순 요청(simple request) | **[추론]** preflight가 생기지 않는 조합(GET/HEAD, 혹은 특정 content-type의 POST)은 이 결함을 지나가지만, `application/json` POST는 preflight를 유발하므로 우리 API 대부분이 해당된다 |

**[코드 확인]** 현재 스모크(`backend/src/server/smoke.ts`, 39건)에 `OPTIONS`를 보내는 케이스가 **없다.** 그래서 이 결함이 통과 중인 테스트로는 잡히지 않는다.

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **catch-all에서 `OPTIONS`를 제외하고 express 기본 처리로 넘긴다.** 가장 좁은 수정이며 새 의존성이 없다.
   - **[추론]** 고려할 점: 정말 존재하지 않는 경로로 온 `OPTIONS`가 어떤 응답을 받게 되는지 확인해야 한다. 그리고 이것만으로는 CORS 응답 헤더(`Access-Control-Allow-Origin` 등)가 붙지 않으므로, preflight가 200을 받아도 브라우저가 통과시킬지는 별개다.
2. **CORS 미들웨어를 명시적으로 두고 preflight를 직접 처리한다.** 허용 오리진을 게이트웨이가 정책으로 갖게 된다.
   - **[추론]** 고려할 점: `cors` 패키지를 도입할지는 별도 판단이다. **현재 방침은 새 의존성 최소화**이므로 기본값은 "직접 쓴다"에 가깝다.
3. **Vite 프록시로 같은 오리진을 만들어 preflight를 아예 없앤다.** 게이트웨이 코드를 건드리지 않는다.
   - **[추론]** 고려할 점: 개발 환경만 해결한다. 배포에서 프론트와 게이트웨이가 다른 오리진이면 문제가 되돌아오므로, 배포 형태(정적 파일을 게이트웨이가 서빙할지 여부)와 함께 정해야 한다.

<a id="issue-002-결정"></a>

### 결정

**3안 채택 — 같은 오리진 구조로 간다. CORS를 도입하지 않으며, 따라서 `OPTIONS` 404를 고치지 않는다.** 2026-08-06 확정.

발견 당시 3안에 붙여 둔 대가("개발 환경만 해결한다")는 **배포까지 같은 오리진으로 맞춰** 없앤다. 브라우저가 항상 한 곳만 보게 만들어 preflight가 **발생하지 않게** 하는 것이 요지다.

```
개발    브라우저 → Vite 개발서버(5173) ─프록시(/api, /ws)→ 게이트웨이(3000)
배포    브라우저 → 게이트웨이(3000) ← 프론트 빌드 결과물(dist/)도 여기서 서빙
```

**[코드 확인]** 이것은 새 방향이 아니라 **원래 설계로 돌아간 것**이다. `DESIGN.md:354`(§6.2 게이트웨이의 HTTP 엔드포인트 표)에 `GET /*` → `정적 파일 (프로덕션)`이 이미 있었다. 발견 당시 3안에 "배포 형태(정적 파일을 게이트웨이가 서빙할지 여부)와 함께 정해야 한다"고 적어 둔 항목의 답이 설계 문서에 이미 있었던 셈이다.

**1·2안을 지금 넣지 않는 이유** **[추론]**

- **2안(CORS 미들웨어)** — 같은 오리진에서는 preflight가 아예 생기지 않으므로, 넣어 봐야 **쓰이지도 않을 허용 오리진 설정을 유지**하게 된다. 별도 도메인이 실제로 필요해지는 시점은 곧 요구사항이 확정된 시점이므로, 그때 넣는 편이 더 정확하다.
- **1안(catch-all에서 `OPTIONS` 제외)** — 발견 당시 스스로 적어 둔 대로 이것만으로는 CORS 응답 헤더가 붙지 않아 브라우저가 통과시킬지가 별개로 남는다. 같은 오리진에서는 애초에 필요가 없다.
- 이 문제의 원인은 **포트가 다르다는 것**(`5173` vs `3000`)이고, 채택안은 포트를 하나로 만들어 원인 자체를 없앤다. 1·2안은 원인을 남긴 채 증상을 처리한다.

<a id="issue-002-무효-조건"></a>

#### 무효 조건 — 이 중 하나라도 하면 이슈를 다시 연다

**결함은 고쳐지지 않았다.** **[코드 확인]** 경로 제한 없는 catch-all은 그대로 있고(현재 `backend/src/server/index.ts:295-297`, 발견 당시 `:227`), `OPTIONS`는 여전히 404다. 위 결정은 **브라우저가 preflight를 보내지 않는다**는 전제 위에서만 성립한다. 다음 중 하나라도 하면 전제가 깨지고 ISSUE-002가 그대로 되살아난다.

- 프론트엔드를 CDN이나 별도 도메인에 배치한다
- 게이트웨이와 다른 호스트에서 프론트를 서빙한다
- 서드파티가 이 API를 브라우저에서 직접 호출한다

그때 필요한 조치는 위 [검토된 해결 방향](#issue-002)의 1안(catch-all에서 `OPTIONS` 제외) 또는 2안(CORS 미들웨어 도입)이다. **상태를 `열림`으로 되돌리고 이 결정을 다시 쓸 것.**

**[코드 확인]** 전제가 깨진 것을 테스트가 알려주지 않는다 — `backend/src/`에 `OPTIONS`라는 문자열이 **한 번도 등장하지 않는다.** 발견 당시 "스모크에 `OPTIONS` 케이스가 없다"고 적힌 상태가 지금도 그대로다. 게다가 전제 파기는 코드 변경이 아니라 **배포 구성 변경**으로 일어나므로 코드 리뷰에도 걸리지 않는다. 감지 지점은 이 문단뿐이다.

#### 후속 작업

구현은 **TASKS #11 "Vite 골격 + WS 클라이언트"**에서 한다 — `frontend/vite.config.ts`에 `/api`·`/ws` 프록시, 게이트웨이에 `dist/` 정적 서빙 라우트. 후자는 [ISSUE-001의 결정](#issue-001-결정)이 만든 등록 지점(catch-all **앞**)에 붙는다. 발견 당시 ISSUE-001의 영향 범위 표가 "(미할당) 정적 파일 서빙"을 **[추론]**으로 예고해 둔 바로 그 항목이다.

**지금 하지 않는 이유**: **[코드 확인]** `frontend/`가 비어 있어 서빙할 빌드 결과물이 없다. 지금 라우트만 만들면 빈 껍데기다.

### 참고 — 깨진 JSON은 없는 경로에서도 404가 아니라 400이다

**이슈가 아니라 참고 사항이다.** Tester가 스모크 §5에서 명문화했다.

**[코드 확인]** `backend/src/server/smoke.ts:452-462`가 이를 테스트로 고정해 두고 있다 — 존재하지 않는 경로(`POST /api/__no-such-route`)에 깨진 JSON 본문(`{`)을 보내면 **404가 아니라 400**이 온다. 원인은 본문 파서 `express.json()`이 `backend/src/server/index.ts:187`에서 **라우팅보다 앞에** 등록되기 때문이다. 파서가 먼저 던지므로 요청이 라우터에 도달조차 하지 않는다.

**[추론]** **버그가 아니라 express 표준 동작이다.** 이 문서의 대상("발견됐지만 고치지 않은 문제")에 해당하지 않아 독립 이슈로 올리지 않고, 미들웨어 순서가 응답 의미를 바꾼다는 점에서 ISSUE-002와 같은 계열이므로 여기에 붙여 둔다.

기록하는 이유는 하나다: **클라이언트가 한 응답만으로 "경로가 틀렸는지 본문이 틀렸는지"를 구분할 수 없다.** 400을 받은 쪽은 경로가 맞다고 가정하기 쉬운데 실제로는 경로도 틀렸을 수 있다. **[추론]** API 클라이언트를 만들 때 400을 "경로는 유효함"의 근거로 삼지 말 것.

---

<a id="issue-003"></a>

## ISSUE-003 — 잘못된 `content-type`으로 업로드하면 원인을 지목하지 못하는 에러가 나간다

- **상태**: 열림
- **발견**: 2026-08-06, TASKS.json #5 "씬 업로드 + 목록"(커밋 `41ff68e`)의 Tester 검증 중. **#5의 통과 기준 자체는 PASS다.**
- **영역**: gateway — `backend/src/server/index.ts`, `backend/src/server/files.ts`

**보안 결함이 아니다.** 아래 세 갈래 모두 요청은 거절되고 파일도 생기지 않는다. **진단 품질 문제다** — 클라이언트가 받는 에러가 진짜 원인(헤더 한 줄)을 지목하지 못한다.

> **[코드 확인]** 이 항목의 행 번호는 커밋 `41ff68e` 시점 기준이다. `backend/src/server/index.ts`·`files.ts`는 이후 단위(#6~)에서 계속 늘어나므로, 나중에 같은 행을 찾으면 다른 코드가 나올 수 있다. 행 번호가 어긋나면 심볼 이름(`#configure`, `assertStreamableBody`)으로 찾을 것.

### 증상

**[코드 확인]** `POST /api/scenes`에 `content-type: application/json`으로 보내면 **본문에 따라 세 갈래로 갈린다.**

| 본문 | 실제 응답 | 어디서 나오는가 |
|---|---|---|
| 유효한 JSON | **415** | 씬 라우트의 가드가 작동 (`backend/src/server/files.ts:345-350`) |
| 파일 바이트, 1MB 미만 | **400** `"... is not valid JSON"` | `express.json()`의 파싱 실패. 가드에 닿지 못함 |
| 파일 바이트, 1MB 초과 | **413** `"request entity too large"` | `express.json()`의 `limit` 초과. 가드에 닿지 못함 |

Builder가 보고한 API 명세는 "`content-type: application/json` → 415"였다. **절반만 맞다** — 세 갈래 중 첫 줄에서만 성립한다.

**[코드 확인]** Tester는 이 갈림을 이미 테스트에 반영해 뒀다. `backend/src/server/smoke.ts:870-877`은 유효 JSON에 대해서만 `415`를 단언하고, `backend/src/server/smoke.ts:879-888`은 파일 바이트에 대해 **status를 단언하지 않고** "거절됐고 파일이 안 생겼다"만 확인한 뒤 실제 status를 `note`로 남긴다(`'설계 문서 표는 415라고 적혀 있지만 express.json이 먼저 파싱에 실패한다'`). 즉 **테스트는 이 이슈를 알고 우회해 둔 상태**이고, 고치면 그 `note`가 무의미해진다.

### 원인

**[코드 확인]** 미들웨어 등록 순서다.

1. `backend/src/server/index.ts:225` — `#configure`가 전역 `app.use(express.json({ limit: '1mb' }))`를 등록한다.
2. `backend/src/server/index.ts:258` — 씬 라우트는 그보다 **뒤**인 "라우트 등록 지점"에서 등록된다(`use(createSceneRoutes(this.#scenes))`).
3. `backend/src/server/files.ts:384` — 415 가드 `assertStreamableBody(req)`는 그 라우트 핸들러의 **첫 줄**이다.

`express.json()`은 `content-type`이 `application/json`일 때만 동작하지만, 동작하는 경우에는 라우팅보다 먼저 본문을 소진하거나 던진다. 따라서 **씬 라우트의 415 가드는 json 파서가 통과시킨 본문에만 닿는다.** 파서가 먼저 실패하면(깨진 JSON → 400, `limit` 초과 → 413) 씬 라우트는 **실행조차 되지 않는다.**

**[코드 확인]** 가드 자신도 이 구조를 알고 쓰였다 — `backend/src/server/files.ts:341-344`의 주석이 "전역 `express.json({limit:'1mb'})`이 요청 스트림을 이미 소진해서 0바이트 파일이 생기거나, 1MB를 넘으면 파서가 413으로 먼저 끊는다"고 적고 있다. 가드는 **전자(0바이트 파일)를 막는 데는 성공했고**, 후자는 애초에 자기 앞에서 끝난다.

**[추론]** 이것은 [ISSUE-002](#issue-002) 및 그 아래 참고 절("깨진 JSON은 없는 경로로 보내도 400", `backend/src/server/smoke.ts:599-609`)과 **같은 계열이다 — 미들웨어 순서가 응답의 의미를 결정한다.** 지금까지 세 번째 사례다.

### 영향 범위

가장 나쁜 것은 **1MB 초과** 갈래다.

**[추론]** 103MB짜리 `.zls`를 `content-type`만 틀리게 보낸 클라이언트가 `413 request entity too large`를 받는다. 이걸 읽으면 **"씬이 서버 상한을 넘었다"로 해석하게 되는데 사실이 아니다.** 실제 씬 상한은 `DEFAULT_MAX_SCENE_BYTES` = **512MiB**이고(`backend/src/server/files.ts:58-59`), 103MB는 그 5분의 1이다. 진짜 원인은 헤더 한 줄이며, 응답 어디에도 그 단서가 없다.

같은 413인데 출처가 둘이라는 점이 특히 나쁘다 — 하나는 본문 파서의 1MB, 하나는 씬 저장소의 512MiB(`backend/src/server/files.ts:257-259`, `:288`, `:303`)다. 클라이언트는 status만으로 둘을 구분할 수 없다.

**[추론]** 씬 라우트의 415 가드가 막으려던 실패 모드가 정확히 이것이었는데, 파서가 앞에 있어 가드까지 도달하지 못한다.

| 대상 | 영향 |
|---|---|
| TASKS #11 이후 프론트엔드 | **직접 영향.** 업로드를 붙일 때 헤더를 한 번 틀리면 이 오진을 만난다 |
| Node SDK·서버 대 서버 호출 | **[추론]** 헤더를 코드로 고정하므로 한 번 맞으면 다시 틀리지 않는다. 노출은 낮다 |
| 올바른 클라이언트 | **영향 없음.** `application/octet-stream`으로 보내면 어느 갈래도 타지 않는다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **씬 라우트를 `express.json()` 앞에 등록한다.** 순서 문제이므로 순서로 푼다.
   - **[추론]** 고려할 점: 현재 등록 지점(`backend/src/server/index.ts:238-261`)이 json 파서(`:225`) **뒤**에 오도록 설계돼 있다. 앞으로 옮기려면 "모든 라우트는 한 자리에서 순서대로 등록된다"는 [ISSUE-001](#issue-001-결정)의 B안 구조를 손봐야 하고, 파서를 필요로 하는 라우트(#7~ 세션 API의 JSON 본문)와 그렇지 않은 라우트를 갈라야 한다.
2. **`express.json()`에 경로 예외를 둔다.** `type` 함수 등으로 `POST /api/scenes`를 건너뛰게 한다.
   - **[추론]** 고려할 점: 구조를 안 건드리는 대신 예외 조건이 `#configure` 안에 숨는다. 라우트가 늘면 예외 목록도 같이 늘어나고, 어긋나면 다시 조용한 오진이 된다.
3. **그대로 두고 API 문서에 명시한다.** 보안 문제가 아니고 올바른 클라이언트는 만나지 않는 실패다.
   - **[추론]** 고려할 점: Builder의 명세("→ 415")가 이미 틀린 채로 유통됐다는 사실은 남는다. 3안을 택한다면 **명세를 위 세 갈래 표로 고치는 일까지가 3안이다.**

### 결정

**미결. TASKS #11(프론트엔드 골격)이 업로드를 실제로 붙일 때 재검토한다.** **[추론]** 어느 쪽도 지금 당장 급하지 않다 — 현재 유일한 클라이언트인 스모크는 헤더를 올바로 보내고, 틀린 경우도 이미 테스트로 고정돼 있다. 브라우저에서 사람이 헤더를 틀릴 수 있게 되는 시점이 재검토 시점이다.

### 참고 — json/multipart 외의 `content-type`은 전부 허용된다 (의도된 설계)

**이슈가 아니다.** 위 해결 방향 2안이 이 계약과 맞물리므로 함께 적어 둔다.

**[코드 확인]** `assertStreamableBody`(`backend/src/server/files.ts:338-359`)가 막는 것은 `application/json`과 `multipart/form-data` **둘뿐**이다. 그 외 값은 검사 없이 통과해 업로드가 성립한다. `backend/src/server/smoke.ts:896-897`이 `application/x-zls` → **201**로 이를 계약으로 박아 뒀다.

**[추론]** 화이트리스트가 아니라 **블랙리스트인 것이 의도**다. 두 타입만 요청 스트림을 가로채거나 파싱을 요구하기 때문이고, 나머지는 파일 바이트가 그대로 디스크로 흘러도 무해하다. 나중에 화이트리스트로 좁히면 `backend/src/server/smoke.ts:897`의 단언이 깨지며 알려준다 — **그 실패는 회귀가 아니라 정책 변경의 신호**이므로, 그때 이 문단을 함께 고칠 것.

---

<a id="issue-004"></a>

## ISSUE-004 — 413 뒤 keep-alive 소켓 재사용이 테스트로 증명되지 않는다

- **상태**: 열림
- **발견**: 2026-08-06, TASKS.json #5 "씬 업로드 + 목록"(커밋 `41ff68e`)의 Tester 검증 중. **#5의 통과 기준은 PASS다.** 이것은 결함 보고가 아니라 **검증 공백** 기록이다.
- **영역**: gateway — `backend/src/server/smoke.ts` (대상 코드는 `backend/src/server/files.ts`)

> **[코드 확인]** 행 번호는 커밋 `41ff68e` 기준이다. 스모크는 단위마다 늘어나므로 나중에는 어긋난다. 어긋나면 섹션 제목(`6-5. 업로드 상한`)과 체크 라벨(`'413 뒤 다음 요청 정상'`)로 찾을 것.

### 증상

**[코드 확인]** `backend/src/server/smoke.ts:936-939`의 체크 `'413 뒤 다음 요청 정상'`은 413을 받은 **직후의 다음 요청이 200인지만** 본다. 정작 검증하려던 명제는 "**같은 keep-alive 소켓**에서 다음 요청이 정상"인데, **소켓 동일성을 강제하지 않는다.** 주석(`backend/src/server/smoke.ts:936-937`)이 스스로 근거를 밝히고 있다 — "undici는 기본적으로 연결을 재사용한다"는 **전제에 기대고 있다.**

**[추론]** 따라서 undici가 어떤 이유로든 새 연결을 맺으면(연결 풀 정책 변경, 서버가 조용히 `Connection: close`를 붙이게 되는 회귀 등) 이 체크는 **여전히 통과한다.** 지키려던 성질이 깨져도 초록불이다.

### 원인

**[코드 확인]** 지켜야 할 동작은 우연이 아니라 **Builder가 의도적으로 넣은 안전장치**다. `backend/src/server/files.ts:279-303`이 상한을 넘긴 뒤에도 `OVERFLOW_DRAIN_BUDGET`(8MiB, `backend/src/server/files.ts:62-67`)만큼 본문을 **버려가며 계속 읽어** 요청을 정상 종료시킨다. 주석이 이유를 적고 있다(`:281-285`): 즉시 던지면 pipeline이 요청 스트림을 파괴하고, 아직 본문을 보내는 중인 클라이언트는 **413 대신 `ECONNRESET`을 본다 — 실제로 그렇게 동작하는 걸 확인했다**고 돼 있다.

**[코드 확인]** 짝이 되는 분기도 있다. `backend/src/server/files.ts:392-403`은 **본문이 끝까지 읽히지 않은 채** 실패한 경우에만 `Connection: close`를 붙이고, 드레인으로 끝까지 읽은 경우에는 **소켓을 재사용하게 둔다**(`if (!res.headersSent && !req.readableEnded)`). 즉 "413 뒤에도 소켓이 살아 있다"는 것은 부수효과가 아니라 **명시적으로 설계된 두 갈래의 한쪽**이다.

**[추론]** 문제는 여기다: 설계는 두 갈래를 구분하는데, 테스트는 **다음 요청이 200이라는 사실 하나로 두 갈래를 구분하지 못한다.** `Connection: close`가 잘못 붙어 매번 새 연결이 맺어져도 200은 나온다.

### 영향 범위

**[추론]** 실제 서비스에서 이 성질이 깨지면 증상은 "대용량 업로드를 한 번 거절당한 뒤 그 클라이언트의 다음 요청이 `ECONNRESET`으로 죽는다"이다. 브라우저·`fetch`·SDK 모두 연결을 재사용하므로 노출 대상이 넓고, 증상이 **거절당한 요청이 아니라 그 다음 요청에** 나타나기 때문에 원인 추적이 어렵다. Builder가 코드에 "재현됨"이라고 적어 둔 실패 모드가 정확히 이것이다.

| 대상 | 영향 |
|---|---|
| 회귀 감지 | **직접 영향.** 8MiB 드레인 로직을 나중에 "불필요해 보인다"며 걷어내도 스모크는 통과한다 |
| 현재 동작 | **[추론]** 영향 없음. 코드는 의도대로 동작하고 있을 가능성이 높다 — 증명이 없을 뿐이다 |
| TASKS #11 이후 프론트엔드 | **[추론]** 간접. 브라우저가 연결을 재사용하므로 회귀 시 가장 먼저 만난다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **응답 헤더로 간접 확인한다.** 413 응답에 `Connection: close`가 **없음**을 단언한다. `backend/src/server/files.ts:403`이 붙이는 헤더를 직접 보는 것이라 대상이 명확하다.
   - **[추론]** 고려할 점: 서버가 소켓을 끊지 않겠다고 말했다는 것까지만 증명한다. 클라이언트가 실제로 재사용했는지는 여전히 미확인이다. **가장 싸고, 회귀의 대부분을 잡는다.**
2. **서버 쪽에서 소켓 동일성을 센다.** `http.Server`의 `connection` 이벤트를 세거나 핸들러에서 `req.socket`을 식별해, 413 전후 요청이 같은 소켓인지 단언한다.
   - **[추론]** 고려할 점: 명제를 그대로 증명한다. 대신 스모크가 서버 내부에 손을 넣게 되고, `Gateway`가 그걸 노출할 방법이 지금 없다(`server` 게터로 가능은 하다).
3. **undici `Agent`를 명시적으로 만들어 연결 재사용을 강제한다.** 전제를 전제로 두지 않고 테스트가 통제한다.
   - **[추론]** 고려할 점: 클라이언트 쪽 통제이므로 서버 코드를 안 건드린다. 다만 새 의존성은 아니어도(undici는 Node 내장) 스모크의 fetch 경로가 복잡해진다.

### 결정

**미결.** **[추론]** 1안이 비용 대비 효과가 가장 크지만, 이 이슈는 **버그가 아니라 증명 공백**이므로 지금 당장 조치할 이유는 약하다. 기록해 두는 이유는 하나다 — 8MiB 드레인 로직(`backend/src/server/files.ts:279-303`)은 **왜 있는지 모르면 지우고 싶어지는 코드**이고, 지웠을 때 스모크가 막아 주지 못한다. 그 로직을 건드리는 작업이 생기면 그때 1안 또는 2안을 함께 넣는다.

---

<a id="issue-005"></a>

## ISSUE-005 — 종료가 진행 중인 acquire와 겹치면 `Gateway.close()`가 영원히 끝나지 않는다

- **상태**: 열림
- **심각도**: **심각** — 프로세스가 종료되지 않는다. 004까지의 이슈와 달리 응답 품질이 아니라 **프로세스 수명**이 걸려 있다.
- **발견**: 2026-08-06, TASKS.json #6 "WS 연결 = 세션 수명"의 Tester 검증 중. **스모크 149/150 — 실패한 1건이 이것이다.**
- **영역**: gateway — `backend/src/server/sessions.ts`, `backend/src/server/index.ts`

> **#6의 통과 기준 자체는 전부 PASS다.** 실제 워커 프로세스로 검증했고 — 연결 → `busy` 1 → 종료 → 0, 상한 초과 거절, 종료 후 pid 소멸까지 확인됐다. **이 이슈는 기준 밖에서 Tester가 추가로 잡은 별건이다.** 다만 아래 [결정](#issue-005-결정)대로 스모크가 이걸 실패로 세므로, **#6을 다시 통과시키려면 이걸 고쳐야 한다.**

> **[코드 확인]** 이 항목의 행 번호는 **커밋되지 않은 작업 트리** 기준이다. #6은 검증 실패로 여기서 중단됐고 커밋되지 않았으므로, 이후 어떤 형태로든 달라질 수 있다. 행이 어긋나면 심볼로 찾을 것 — `SessionManager`의 **`#serve`** 메서드(취소 분기), 모듈 스코프 함수 **`refuse`**, `Gateway`의 **`close()`**.

### 증상

**[코드 확인]** 세션 확보(`pool.acquire()`, 실측 ~110ms)가 **진행 중일 때** `gw.close()`를 부르면 **`Gateway.close()`가 resolve되지 않는다.** 4초·8초 두 관찰 모두 미완이었다.

**세션 정리 자체는 정상이다.** 같은 실행에서 `acquired=1, released=1, closed=1`, `stats.busy === 0`이 모두 성립한다 — 즉 워커는 반납됐고 풀도 닫혔다. 매달리는 것은 그 **다음 단계인 `http.close()`**다.

**[코드 확인]** 실패하는 체크는 `backend/src/server/smoke.ts:1641-1646`의 `'acquire 완료 → close()가 매달리지 않고 끝난다'`이며, 바로 뒤의 `'그 사이 얻은 세션은 반납된다'`(`:1647-1651`)와 `'풀도 닫힌다'`(`:1652`)는 통과한다. 이 대비가 위 문단을 그대로 보여 준다.

### 원인

**[코드 확인 — 격리 실험으로 확정]** `backend/src/server/sessions.ts:462-471`, `#serve()`의 취소 분기:

```ts
// ② 110ms 창. 주인이 사라졌거나 그 사이 게이트웨이가 닫혔으면 즉시 반납한다.
if (gone || socket.destroyed || this.#pool !== pool) {
  await pool.release(session).catch(() => {});
  this.#log(...);
  return;                    // ← 소켓에 응답도 destroy도 하지 않고 버린다
}
```

조건이 세 갈래인데 **뒷감당이 하나뿐이다.**

1. `gone` / `socket.destroyed` — 소켓이 **이미 죽어 있다.** 그냥 `return`해도 무해하다.
2. `this.#pool !== pool` — **게이트웨이가 종료 중이라는 뜻일 뿐, 소켓은 멀쩡히 살아 있다.** 그런데 같은 `return`으로 처리되어 **살아 있는 업그레이드 소켓이 버려진다.**

**[코드 확인]** 다른 모든 거절 경로는 `refuse()`(`backend/src/server/sessions.ts:196-220`)를 지나며, 그 함수는 응답을 쓰고 `socket.end()`로 닫는다(`:219`). 상한 초과 503(`:453`), 워커 기동 실패 502(`:456`), 씬 없음 404(`:438`)가 전부 그렇다. **이 취소 분기만 예외다.**

**[추론]** Node의 http 서버는 `'upgrade'` 이벤트가 나간 뒤의 소켓에서 자기 `close` 리스너를 뗀다(소켓 소유권이 핸들러로 넘어간다는 전제다). 그래서 버려진 그 연결이 서버의 활성 카운트에 **영구히 남고**, `backend/src/server/index.ts:381-384`의 `this.#http.close(cb)` 콜백이 오지 않아 `close()`의 Promise가 풀리지 않는다. 같은 자리의 `closeAllConnections()`(`:383`)도 **업그레이드된 소켓은 건드리지 못한다** — 그래서 이 안전망이 작동하지 않는다.

### 격리 실험

**[코드 확인]** 버려진 그 소켓을 테스트가 직접 파괴하는 **시점만** 바꿔 측정했다.

| 조건 | `close()` 소요 |
|---|---|
| 파괴 안 함 | **매달림 (>4000ms)** |
| 파괴 (close 전) | 38ms |
| 파괴 (close 후) | 66ms |

**클라이언트가 `terminate()`해도 풀리지 않는다** — FIN만으로는 서버 쪽 소켓이 닫히지 않는다. **오직 서버가 그 소켓을 처리해야 풀린다.** 이 사실이 해결 방향을 서버 쪽으로 고정한다.

### 영향 범위

**[추론]** **SIGTERM 재기동이 클라이언트 접속 순간과 겹치면 게이트웨이 프로세스가 종료되지 않는다.** 창은 acquire 1회(~110ms)로 좁지만, 걸리면 결과는 **영구 정지**다. 배포·재시작 자동화가 프로세스를 SIGKILL로 강제 종료해야 풀린다.

| 대상 | 영향 |
|---|---|
| 배포·재기동 자동화 | **직접 영향.** graceful shutdown이 반환되지 않아 파이프라인이 타임아웃까지 대기한 뒤 강제 종료로 넘어간다 |
| 세션·워커 정리 | **영향 없음.** **[코드 확인]** 반납·풀 종료는 정상 완료된다. 좀비 exe가 남지는 않는다 |
| 스모크 하네스 | **[코드 확인]** 매달리지 않는다 — 아래 참고 |

**[코드 확인]** 스모크가 매달리지 않는 이유는 **테스트가 그 소켓을 스스로 정리하기 때문**이다. `backend/src/server/smoke.ts:1617-1623`이 `gw2.server.on('upgrade', …)`로 소켓을 붙잡아 두고, 단언이 깨졌을 때 하네스를 오염시키지 않도록 정리한다(`:1626-1653`의 블록, 주석이 그 의도를 명시한다). **즉 지금 스모크가 끝까지 도는 것은 결함이 가볍다는 증거가 아니라, 테스트가 우회해 둔 결과다.**

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **종료 중 취소 갈래에서 `refuse(socket, 503, …)`를 부른다.** 다른 거절 경로와 처리가 통일되고, 클라이언트가 이유(종료 중)를 받는다.
2. **종료 중 취소 갈래에서 `socket.destroy()`한다.** 가장 짧다. 대신 클라이언트는 이유 없이 끊긴 것만 본다.

**[추론]** 어느 쪽이든 **조건을 갈라야 한다.** 지금의 `if`는 세 조건이 한 덩어리인데, 앞의 둘(`gone`, `socket.destroyed`)은 소켓이 이미 죽어 있어 지금 처리가 옳다. 죽은 소켓에 그대로 쓰기를 시도하면 별도 문제를 만든다.

**[코드 확인]** 다만 `refuse` 자신이 `socket.destroyed`(`:202`)와 `!socket.writable`(`:203-206`)을 이미 검사한다. 조건을 안 가르고 1안을 넣어도 죽은 소켓에서 곧바로 반환되므로 **쓰기 사고가 나지는 않는다.** 조건을 가르는 것은 안전 문제라기보다 **로그와 의도를 구분하기 위한 것**이다(현재 `:465-469`의 로그가 이미 두 문구를 나눠 쓰고 있다).

<a id="issue-005-결정"></a>

### 결정

**미결. 다만 이 건은 다른 열린 이슈들과 성격이 다르다 — 미룰 수 있는 항목이 아니다.** **[코드 확인]** 스모크가 이것을 실패로 세고 있고(149/150), **#6을 다시 통과시키려면 고쳐야 한다.** 1안과 2안 중 어느 쪽을 택할지, 조건을 어떻게 가를지가 남은 판단이다.

---

<a id="issue-006"></a>

## ISSUE-006 — 워커 exe 경로가 틀리면 502가 아니라 게이트웨이 프로세스가 죽는다

- **상태**: 열림
- **심각도**: **심각** — 서버 전체가 죽는다. 요청 하나가 실패하는 것과 장애 범위가 다르다.
- **발견**: 2026-08-06, TASKS.json #6 "WS 연결 = 세션 수명"의 Tester 검증 중. **#6의 통과 기준 자체는 실제 워커로 전부 PASS이며, 이것은 기준 밖에서 잡힌 별건이다.**
- **영역**: **sdk** — `backend/src/sdk/worker.ts` (지금까지의 이슈와 달리 gateway가 아니다)

> **[코드 확인]** 행 번호는 **커밋되지 않은 작업 트리** 기준이다. 어긋나면 심볼로 찾을 것 — `Worker` 클래스의 정적 메서드 **`spawn`**.

### 증상

**[코드 확인]** 설계 문서와 Builder 명세는 "워커 기동 실패(exe 없음) → **502**"라고 적고 있다. **성립하지 않는다.**

재현하면 `UNCAUGHT: spawn … ENOENT`가 나면서 **게이트웨이 프로세스 전체가 죽는다.** 502 응답은 나가지 않는다.

### 원인

**[코드 확인]** `backend/src/sdk/worker.ts:92-105`의 `Worker.spawn()`이 `child_process.spawn()`으로 자식을 만든 뒤 곧바로 `new Worker(child, opts)`를 반환한다. 등록되는 리스너는 생성자의 **`child.on('exit', …)`**(`backend/src/sdk/worker.ts:78`) 하나뿐이고, **`child.on('error')` 리스너가 어디에도 없다.** 파일 전체에서 `'error'` 문자열이 이 파일에 등장하지 않는 것을 확인했다.

**[추론]** Node에서 `EventEmitter`의 `'error'`는 리스너가 없으면 unhandled로 승격되어 프로세스를 종료시킨다. `child_process`의 spawn 실패(ENOENT 등)는 예외로 던져지는 게 아니라 **비동기 `'error'` 이벤트로 전달**되므로, 호출부를 `try/catch`로 감싸도 잡히지 않는다. `backend/src/server/sessions.ts:454-457`의 502 분기는 `pool.acquire()`가 **reject될 때** 도는데, 여기서는 reject 대신 프로세스가 죽는다.

### 영향 범위

**[추론]** **운영에서 exe 경로 오타 하나가 게이트웨이를 못 뜨게 한다.** 게다가 실패 형태가 "요청 하나가 502"가 아니라 "**서버 전체 사망**"이라 장애 범위가 다르다 — 그 시점에 붙어 있던 다른 세션들도 함께 끊긴다.

만나는 시나리오는 특별하지 않다. **[추론]** 배포 환경이 바뀌어 경로가 이동하거나, 데모용/배포용 exe를 바꿔 끼울 때(`zelusSandBoxd-demo.exe` ↔ `zelusSandBoxd.exe`) 바로 걸린다.

| 대상 | 영향 |
|---|---|
| 운영 배포 | **직접 영향.** 경로 설정 실수의 결과가 "그 요청 실패"가 아니라 "프로세스 종료"다 |
| 기동 시점 vs 요청 시점 | **[추론]** spawn은 세션 확보 시점에 일어나므로, 서버는 정상 기동한 뒤 **첫 접속에서** 죽는다. 헬스체크만으로는 드러나지 않는다 |
| Builder 명세 / 설계 문서 | **직접 영향.** "→ 502"가 틀린 채로 유통돼 있다 |

### 특이사항 — 수정 위치가 이 작업 단위 밖이다

**[코드 확인]** 결함은 **SDK(`backend/src/sdk/worker.ts`)**에 있다. #6은 게이트웨이 작업이고, SDK는 스모크 24건이 지키는 완성된 계층이라 **이번 단위에서 손대지 않기로 한 영역**이다. 지금까지의 이슈가 전부 `영역: gateway`였던 것과 이 점이 다르다.

**[코드 확인]** 그리고 **테스트로 잡을 수 없다** — 재현하면 스모크 프로세스 자체가 죽어 나머지 케이스가 실행되지 않는다. Tester가 케이스로 넣지 않고 `backend/src/server/smoke.ts:1518-1522`에 `note`로만 남긴 이유가 그것이다:

> `'exe 경로가 존재하지 않는 경우'` — "ENOENT는 child_process가 error 이벤트로 알리는데 sdk/worker.ts에 그 리스너가 없어 **프로세스가 죽는다**(502로 오지 않는다). 스모크를 통째로 죽이므로 케이스로 넣지 않았다"

**[코드 확인]** 바로 위(`backend/src/server/smoke.ts:1512-1515`)에 **502가 실제로 나가는 케이스는 존재한다** — `exePath`를 `process.execPath`(= node 자신)로 주어 **spawn은 성공하되 프로토콜이 성립하지 않게** 만든 경우다. 즉 502 경로 자체는 살아 있고, **spawn이 아예 실패하는 갈래만 뚫려 있다.**

### 검토된 해결 방향

**아직 없다.** 판단이 필요한 갈래만 적어 둔다.

1. **SDK를 고친다** — `spawn()`에 `child.on('error')`를 달아 `WorkerError`로 바꿔 `acquire()`가 reject되게 한다. 명세대로 502가 나간다. **[추론]** 고려할 점: 이번 단위에서 안 건드리기로 한 계층이고, 기존 스모크 24건에 회귀가 없는지 확인해야 한다.
2. **게이트웨이가 사전 확인한다** — 기동 시 exe 존재를 검사해 아예 뜨지 않게 하거나 경고한다. **[추론]** 고려할 점: SDK의 구멍은 그대로 남는다. 경로가 존재하지만 실행할 수 없는 경우(권한, 손상)는 여전히 프로세스를 죽인다.
3. **둘 다 한다.**

**[추론]** 어느 쪽을 택하든 **테스트 전략이 따라온다** — 1안을 넣으면 그때는 자식 프로세스로 격리해 재현하는 케이스를 쓸 수 있게 되므로, `backend/src/server/smoke.ts:1518`의 `note`를 정식 케이스로 승격할지 함께 정해야 한다.

### 결정

**미결.** SDK를 고칠지, 게이트웨이에서 사전 확인할지, 둘 다 할지 판단이 필요하다.
