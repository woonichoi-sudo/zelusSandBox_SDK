# ISSUES.md — 발견된 문제 기록부

이 문서는 **작업 중 발견됐지만 그 작업의 범위에서 고치지 않은 문제**를 누적한다. 대상은 `backend/`·`frontend/`의 우리 코드이며(회사 저장소 `zelusSandBox_Cobalt/`의 엔진 버그는 `PROJECT_ANALYSIS.md` §9 소관이다), 목적은 "지금 당장 안 고치기로 한 것"이 조용히 잊히지 않게 하는 것이다. 기록자는 **고치지 않고 적기만 한다** — 해결 방향을 여러 개 적되 어느 쪽을 택할지는 결정하지 않고, `결정` 항목에 `미결`로 남긴다. 새 이슈는 이 문단 아래에 **번호 순서대로 덧붙이고**(위에서 아래로 오래된 것 → 최신), 아래 [이슈 목록](#이슈-목록) 표에도 한 줄을 추가한다. 각 이슈는 `## ISSUE-NNN — 제목` 헤딩과 `<a id="issue-NNN"></a>` 앵커를 갖고, 아래 템플릿의 9개 항목(상태 / **심각도** / 발견 / 영역 / 증상 / 원인 / 영향 범위 / 검토된 해결 방향 / 결정)을 모두 채운다(심각도 등급은 [이슈 목록](#이슈-목록)의 범례를 따른다). 서술은 `PROJECT_ANALYSIS.md`의 표기 규약을 따라 **[코드 확인]**(파일을 읽거나 실행해 검증한 것)과 **[추론]**(분석·예측)을 구분하고, 코드 참조는 저장소 루트(`d:\z-emotion\Cobalt`) 기준 상대경로로 `backend/src/server/index.ts:112` 형태로 적는다.

## 이슈 목록

**심각도**는 ISSUE-005·006을 기록하며 도입했다. 그전까지의 이슈와 이 둘은 **실패했을 때 무엇이 망가지는가**가 다르기 때문이다 — 004까지는 요청 하나의 응답이 부정확하거나 회귀를 못 잡는 문제인 반면, 005·006은 **게이트웨이 프로세스 자체가 죽거나 반대로 영영 안 죽는다.** 같은 표에서 같은 무게로 읽히면 착수 순서를 잘못 잡게 되므로 열을 하나 늘렸다. 001~004는 소급해 분류만 붙였고 본문은 손대지 않았다.

| 등급 | 뜻 |
|---|---|
| **심각** | 프로세스 수명이 걸린다. 운영에서 게이트웨이가 죽거나 종료되지 않아 배포·재기동 자동화가 막힌다 |
| **차단** | 동작은 안전하지만 후속 작업이 이 위에 쌓이지 못한다 |
| **진단** | 요청은 올바르게 거절/처리된다. 에러가 원인을 지목하지 못하거나 성질이 테스트로 증명되지 않는다 |
| **신뢰** | 제품 코드는 멀쩡한데 **검증 신호가 무작위로 거짓이 된다.** 망가지는 것은 동작이 아니라 초록/빨간불의 의미다 |

**`신뢰`**는 ISSUE-007을 기록하며 도입했다. 006까지는 전부 `backend/` 제품 코드의 결함이었고 "무엇이 잘못 동작하는가"로 등급이 갈렸다. 007은 **제품 코드에 결함이 없다** — 스모크가 무작위로 프로세스째 죽는 개발·검증 환경의 문제이고, 재실행하면 통과한다. 기존 세 등급 중 어디에도 들어가지 않는다: 죽는 것이 게이트웨이가 아니므로 `심각`이 아니고, 재실행으로 진행이 되므로 `차단`이 아니며, 거절되는 요청이 없으므로 `진단`도 아니다. 그렇다고 등급 없이 두면 **"제품이 멀쩡하니 무해하다"로 읽히는데, 손해는 다른 데 있다** — 이 상태로 CI를 붙이면 빨간불이 원인 없이 뜨고, 팀이 빨간불을 무시하는 습관을 얻는다. 그 손해를 표에서 보이게 하려고 열을 늘리는 대신 등급을 하나 늘렸다.

**상태**의 `닫힘(전제부)`는 ISSUE-002를 닫으며 도입했다. 이 이슈는 **코드를 고쳐서** 닫힌 게 아니라 **문제가 성립하지 않는 구조를 택해서** 닫혔다 — 결함은 코드에 그대로 있고, 전제가 유지되는 동안만 드러나지 않는다. 그냥 `닫힘`으로 적으면 전제가 깨진 날 아무도 이 문서를 다시 열지 않으므로, 표기 자체가 "전제가 붙어 있다"고 말하게 했다.

| 상태 | 뜻 |
|---|---|
| **열림** | 미해결. 결정이 `미결`이거나 아직 반영되지 않았다 |
| **닫힘** | 코드로 해결됐다. 되살아나지 않는다 |
| **닫힘(전제부)** | 결함은 코드에 남아 있고 **전제가 성립하는 동안만** 드러나지 않는다. 결정 항목에 **무효 조건**이 명시돼 있으며, 그중 하나라도 하면 **상태를 `열림`으로 되돌린다** |

| ID | 상태 | 심각도 | 제목 | 발견 | 영역 |
|---|---|---|---|---|---|
| [ISSUE-001](#issue-001) | 닫힘 | 차단 | `Gateway.app` 게터로 등록한 라우트가 catch-all에 가려 도달 불가 | TASKS #4 (2026-08-06) | gateway |
| [ISSUE-002](#issue-002) | **닫힘(전제부)** | 진단 | `OPTIONS` 요청이 catch-all에 잡혀 404가 된다 — CORS preflight가 깨진다 | TASKS #4 후속 (2026-08-06) | gateway |
| [ISSUE-003](#issue-003) | 열림 | 진단 | 잘못된 `content-type`으로 업로드하면 원인을 지목하지 못하는 에러가 나간다 | TASKS #5 (2026-08-06) | gateway |
| [ISSUE-004](#issue-004) | 열림 | 진단 | 413 뒤 keep-alive 소켓 재사용이 테스트로 증명되지 않는다 | TASKS #5 (2026-08-06) | gateway |
| [ISSUE-005](#issue-005) | 닫힘 | **심각** | 종료가 진행 중인 acquire와 겹치면 `Gateway.close()`가 영원히 끝나지 않는다 | TASKS #6 (2026-08-06) | gateway |
| [ISSUE-006](#issue-006) | 닫힘 | **심각** | 워커 exe 경로가 틀리면 502가 아니라 게이트웨이 프로세스가 죽는다 | TASKS #6 (2026-08-06) | sdk |
| [ISSUE-007](#issue-007) | 열림 | **신뢰** | 서버 스모크가 간헐적으로 프로세스째 죽는다 (Windows fail-fast `0xC0000409`) | TASKS #8 (2026-08-06) | 개발환경 |
| [ISSUE-008](#issue-008) | 열림 | 진단 | 백프레셔의 두 경로가 테스트로 증명되지 않는다 | TASKS #9 (2026-08-07) | gateway |
| [ISSUE-009](#issue-009) | **닫힘** | 진단 | 재생 중 씬을 다시 로드하면 시뮬은 멈추는데 버튼은 "정지"로 남는다 | TASKS #13 (2026-08-07) | frontend |
| [ISSUE-010](#issue-010) | 열림 | 진단 | UI 로그가 `engineMessage` 40/s에 밀려 7초면 의미 있는 줄이 사라진다 | TASKS #13 (2026-08-07) | frontend |
| [ISSUE-011](#issue-011) | **닫힘** | **차단** | 옷 메시가 패턴 로컬 좌표로 나간다 — 아바타를 얹으면 몸에서 떨어진다 | 아바타 조사 (2026-08-07) | worker |
| [ISSUE-012](#issue-012) | 열림 | 진단 | 익스포트가 간헐적으로 깨진 JSON으로 돌아온다 | UI 하네스 (2026-08-09) | worker |
| [ISSUE-013](#issue-013) | 열림 | 진단 | 익스포트를 반복하면 워커가 죽는다 (code 4001) | UI 하네스 (2026-08-09) | worker |
| [ISSUE-014](#issue-014) | 열림 | 진단 | `setParams`가 필드마다 갈린다 — 전수 측정 결과 22개 중 **반영 17 · 조건부 3 · 죽음 2**(`subStep`·`meshingEdgeLength`). `gravityY`는 반영된다(첫 판정이 뒤집힘) | 끈 조사 (2026-08-09) | worker |
| [ISSUE-015](#issue-015) | 열림 | **차단** | `step` op이 워커에서 아무 일도 하지 않는다 — 데스크톱 #63을 만들 수 없다 | TASKS #14 (2026-08-09) | worker |
| [ISSUE-016](#issue-016) | 열림 | **신뢰** | `npm run build`가 간헐적으로 죽는다 (rollup 네이티브 파서 panic) | TASKS #14 (2026-08-09) | 개발환경 |
| [ISSUE-017](#issue-017) | 열림 | 진단 | `PlaybackHooks` 계약이 절반만 지켜진다 — `afterClear`가 던지면 성공한 clear가 실패로 보인다 | TASKS #14 (2026-08-09) | frontend |

---

<a id="issue-001"></a>

## ISSUE-001 — `Gateway.app` 게터로 등록한 라우트가 catch-all에 가려 도달 불가

- **상태**: **닫힘** — 2026-08-06, 커밋 `50b8dc6`으로 반영. 아래 [결정](#issue-001-결정) 참고.
- **발견**: 2026-08-06, TASKS.json #4 "HTTP 골격 + /api/health + 스모크 하네스"의 Tester 검증 중. **#4의 통과 기준(`서버 기동 → GET /api/health 200`) 자체는 PASS**다. 이 이슈는 그와 별개로, #5 이후 작업을 막는 구조 결함으로 함께 발견됐다.
- **영역**: gateway — `backend/src/server/index.ts`

> **[코드 확인]** 아래 "증상"·"원인"·"모순" 절의 행 번호는 **수정 전**(커밋 `02dec3d` 시점)의 `backend/src/server/index.ts` 기준이다. `50b8dc6`이 같은 파일을 크게 늘렸으므로 현재 파일에서 같은 행을 찾으면 다른 코드가 나온다. 기록을 사후에 고쳐 쓰지 않고 발견 당시 그대로 남긴다.

### 증상

**[코드 확인]** `Gateway`를 기동한 뒤 공개 `app` 게터로 라우트를 추가하면 그 라우트에 요청이 도달하지 않는다.

```ts
const gw = createServer();
await gw.start();
gw.app.get('/api/__probe', (_req, res) => res.json({ ok: true }));
// GET /api/__probe → 404 (기대: 200)
```

실측 결과 **404**. 응답 본문은 `backend/src/server/index.ts:113`의 catch-all이 만드는 `{ error: "찾을 수 없음: GET /api/__probe" }` 형태다.

### 원인

**[코드 확인]** `backend/src/server/index.ts`를 읽어 확인했다. 미들웨어 등록 순서 문제다.

1. `backend/src/server/index.ts:56-61` — `constructor`가 `express()`로 앱을 만든 직후 `this.#configure(this.#app)`을 호출한다. 즉 **생성자가 끝나는 시점에 이미 배선이 전부 끝나 있다.**
2. `backend/src/server/index.ts:92-127` — `#configure`가 한 번에 다음을 등록한다:
   - `backend/src/server/index.ts:98` — `express.json()` 본문 파서
   - `backend/src/server/index.ts:100` — `/api/health` 라우트
   - `backend/src/server/index.ts:112` — **404 catch-all** (`app.use((req, res) => …)`, 경로 지정 없음 → 모든 요청에 매치)
   - `backend/src/server/index.ts:118` — 에러 핸들러 (인자 4개)
3. Express는 미들웨어를 **등록 순서대로** 평가하고, `backend/src/server/index.ts:112`의 catch-all은 경로 제한이 없으므로 여기 도달한 모든 요청을 **응답을 보내며 종료**한다. `next()`를 부르지 않으므로 뒤 스택으로 넘어가지 않는다.

따라서 생성자가 끝난 뒤 `app` 게터로 붙인 라우트는 **구조적으로 항상 catch-all 뒤에 놓이며, 어떤 요청으로도 실행될 수 없다.** 기동 전에 붙이든(`createServer()` 직후) 기동 후에 붙이든 결과는 같다 — 결정적인 것은 `start()` 시점이 아니라 `#configure`가 생성자에서 이미 끝났다는 사실이다.

### 모순 — 주석이 동작하지 않는 사용법을 안내한다

**[코드 확인]** `backend/src/server/index.ts:63-66`:

```ts
/** 라우트를 더 얹고 싶을 때. #5 이후 단위가 여기에 붙는다 */
get app(): Express {
  return this.#app;
}
```

주석은 `app` 게터를 **후속 단위의 확장 지점으로 명시**하고 있으나, 위 원인에 따라 그 사용법은 동작하지 않는다. 이 문서가 이 이슈를 "#4는 PASS인데도" 기록하는 이유가 여기 있다: **[추론]** 그대로 두면 #5를 맡는 다음 사람이 주석을 신뢰해 라우트를 붙이고 404를 만나며, 원인이 자기가 방금 쓴 핸들러가 아니라 골격의 미들웨어 순서에 있다는 걸 찾는 데 시간을 쓴다. 증상(404)이 "라우트를 잘못 등록했다"는 흔한 실수와 구분되지 않아 오진하기 쉽다.

### 영향 범위

**라우트를 추가하는 모든 후속 작업이 영향을 받는다.**

| 작업 | 내용 | 영향 |
|---|---|---|
| TASKS #5 | 씬 업로드 + 목록 (`POST/GET /api/scenes`) | **직접 영향.** 다음에 착수하는 단위다 |
| TASKS #10 | export + glTF 다운로드 (`GET /api/exports/:id`) | **직접 영향** |
| (미할당) | 정적 파일 서빙 | **[추론]** 프런트엔드 배포를 붙일 때 같은 문제를 만난다 |

**영향받지 않는 것**: TASKS #6 (WS 연결 = 세션 수명)은 `backend/src/server/index.ts:69-71`의 `server` 게터로 `http.Server`를 직접 잡아 업그레이드를 처리하므로, Express 미들웨어 스택을 지나지 않는다.

### 검토된 해결 방향

Tester가 제시한 두 가지. **아직 채택되지 않았다.**

1. **`#configure`를 쪼개고 마감을 `start()`로 미룬다.** "라우트 등록"과 "catch-all + 에러 핸들러 설치"를 분리해, 후자를 `start()` 시점에 실행한다. → `app` 게터가 `backend/src/server/index.ts:63`의 주석대로 동작하게 되고, 주석을 고칠 필요가 없다.
   - **[추론]** 고려할 점: `start()`가 두 번 불릴 때 마감이 중복 등록되지 않도록 막아야 하고(`backend/src/server/index.ts:131`의 조기 반환이 이미 있으므로 그 뒤에 두면 된다), `start()`를 거치지 않고 `app`만 꺼내 쓰는 경로가 생기면 catch-all이 영영 설치되지 않는다.
2. **후속 라우트를 `#configure` 안, catch-all 앞에 직접 등록한다.** 확장 지점을 게터가 아니라 `#configure` 본문으로 삼는다. → 구조 변경이 없는 대신 `backend/src/server/index.ts:63`의 주석이 거짓이 되므로 **주석을 반드시 고쳐야 한다.**
   - **[추론]** 고려할 점: 라우트 모듈이 늘어날수록 `#configure`가 비대해지고, `backend/src/server/files.ts`(#5·#10의 예정 파일) 같은 외부 모듈이 `Gateway` 내부에 하드와이어된다.

<a id="issue-001-결정"></a>

### 결정

**2안(B) 채택.** 커밋 `50b8dc6` "fix(gateway): 라우트 등록 지점을 404 앞에 만들고 에러 status를 존중"으로 반영 완료.

**반영 내용** **[코드 확인]** (`backend/src/server/index.ts`를 읽어 확인):

- 404 catch-all은 `#configure` 안 **원래 자리에 그대로 두고**(`backend/src/server/index.ts:227`), 후속 라우트를 그 **앞**에 등록하는 지점을 만들었다(`backend/src/server/index.ts:200-224`, 주석 `// ── 라우트 등록 지점 ──`).
- **등록 경로는 둘이다.**
  1. `#configure` 안의 로컬 `use(register)` 헬퍼(`backend/src/server/index.ts:209-212`) — **항상 켜져 있어야 하는 내장 라우트용.** #5부터 여기에 한 줄씩 는다.
  2. `GatewayOptions.routes`(`backend/src/server/index.ts:78`) — **생성자에서만 읽히므로 늦게 붙일 방법이 구조적으로 없다.** 테스트·임베딩이 임시 라우트를 붙이는 정식 경로다. 내장 라우트 **뒤**, catch-all **앞**에 등록된다(`backend/src/server/index.ts:224`).
- 라우트 모듈은 `RouteRegistrar = (app, ctx) => void | RouteHooks` 형태이고, 의존성이 필요하면 함수를 직접 export하지 말고 **팩토리**로 만든다(`backend/src/server/index.ts:52-64`의 예시 참고).
- **비동기 준비는 `RouteHooks.prepare`로 돌려준다.** `start()`가 `listen()` **전에** 순차 await하므로(`backend/src/server/index.ts:259`), 준비가 끝나기 전에 요청이 라우트에 닿는 일이 없다. 생성자에서 await할 필요가 사라졌다. **`close()` 후 재기동될 때마다 다시 불리므로 멱등이어야 한다.**
- `app` 게터는 **남기되 주석을 사실로 고쳤다**(`backend/src/server/index.ts:138-155`). 이제 "여기에 라우트를 붙이지 마라 — catch-all에 가려 조용히 404가 난다"고 명시하고, 설정 조회·미들웨어 스택 검사처럼 라우팅을 바꾸지 않는 용도로만 남겨 뒀다.

**`gw.app` 사후 등록이 404인 것은 이제 의도된 계약이다. 회귀가 아니다.** 진단 메모였던 것을 스모크의 정식 테스트로 승격해 뒀다(`backend/src/server/smoke.ts:219-227`, `'app 게터 사후 등록 → 404 (의도된 동작)'`). **[추론]** 이 단언이 200으로 깨진다면 그건 "고쳐진" 게 아니라 등록 순서가 바뀐 것이고, 그때는 등록 지점이 여전히 유일한 길인지 다시 확인해야 한다.

#### 1안(A, catch-all 설치를 `start()`로 미루기)을 기각한 이유

**이 판단 근거를 반드시 보존한다.** **[추론]** A안은 겉보기에 더 깔끔하므로 — `app` 게터 주석을 고칠 필요조차 없다 — 나중에 누군가 "A가 더 나은데 왜 안 했지"라고 다시 꺼낼 수 있다. 그때 이 문단을 읽으라고 남긴다.

> A는 "라우트는 `start()` 전에 등록해야 한다"는 **보이지 않는 계약**을 만든다. 그걸 어기면 **똑같이 조용한 404**가 난다 — 지금 고치려는 실패 모드가 **마감 시각만 뒤로 밀린 채 그대로 남는다.** B는 등록 자리가 한 곳뿐이라 그 실패 모드 자체가 없고, 한 함수 안에서 순서가 눈에 보인다. `Gateway`가 라우트 모듈을 아는 것은 결합도 문제가 아니라 **조립 지점(composition root)의 정상 역할**이다.

이로써 발견 당시 2안의 대가로 적어 둔 두 가지에 답이 붙는다. "주석이 거짓이 된다" → 주석을 고쳤다. "`#configure`가 비대해지고 외부 모듈이 하드와이어된다" → 조립 지점의 정상 역할로 받아들이기로 했다.

**검증**: 서버 스모크 **12건 → 39건, 전부 통과.** SDK 스모크 **24/24 회귀 없음.**

---

<a id="issue-002"></a>

## ISSUE-002 — `OPTIONS` 요청이 catch-all에 잡혀 404가 된다 (CORS preflight)

- **상태**: **닫힘(전제부)** — 2026-08-06 결정. **코드는 고치지 않았다.** 같은 오리진 구조를 채택해 문제가 성립하지 않게 만들었을 뿐이므로, 전제가 깨지면 되살아난다. 아래 [결정](#issue-002-결정)과 [무효 조건](#issue-002-무효-조건)을 반드시 함께 볼 것.
- **발견**: 2026-08-06, TASKS.json #4 후속 수정([ISSUE-001](#issue-001)) 검토 중. B안의 부작용이 없는지 확인하려고 돌린 실측에서 드러났다. **A/B 선택과 무관한 기존 결함이며, 등록 순서를 어떻게 바꿔도 남는다** — catch-all이 존재하는 한 성립하기 때문이다.
- **영역**: gateway — `backend/src/server/index.ts`

### 증상

**[코드 확인]** 등록된 경로에 `OPTIONS`로 요청하면 **404**가 돌아온다. 실측은 `OPTIONS /api/scenes`로 했고, 해당 경로에 `GET`과 `POST`가 등록돼 있는데도 그랬다. 응답은 `backend/src/server/index.ts:227-229`의 catch-all이 만드는 `{ error: "찾을 수 없음: OPTIONS /api/scenes" }` 형태다.

기대 동작은 404가 아니라, Express가 그 경로에 등록된 메서드를 모아 `Allow` 헤더와 함께 돌려주는 자동 응답이다.

### 원인

**[추론]** Express는 **경로는 맞고 메서드만 없는** 요청에 대해 등록된 메서드를 모아 `Allow` 헤더로 자동 응답한다. 그런데 그 자동 처리는 **라우터가 매칭을 소진한 뒤에** 일어난다. 경로 인자 없는 catch-all(`backend/src/server/index.ts:227`, `app.use((req, res) => …)`)은 **모든 요청에 매치되므로 소진 전에 먼저 응답을 끝내 버린다.** `next()`를 부르지 않으니 자동 처리 단계에 도달할 기회가 없다.

**[추론]** 즉 이것은 ISSUE-001과 **같은 뿌리(경로 제한 없는 catch-all이 스택을 조기 종료한다)에서 나온 다른 증상**이다. ISSUE-001은 catch-all **뒤**의 라우트를 죽였고, 이쪽은 catch-all **뒤에 있는 express 자체의 기본 동작**을 죽인다. 등록 지점을 catch-all 앞으로 옮겨도(B안) 후자는 그대로다 — 죽는 것이 우리가 등록하는 무언가가 아니라 프레임워크의 내장 단계이기 때문이다.

### 영향 범위

**브라우저 CORS preflight가 깨진다.**

**[추론]** 프론트엔드(TASKS #11 이후)가 Vite 개발 서버(기본 5173)에서 게이트웨이(기본 3000)를 **직접** 호출하면 오리진이 달라 브라우저가 preflight `OPTIONS`를 먼저 보낸다. 그게 404를 받으면 브라우저는 preflight 실패로 보고 **본 요청 자체를 차단한다.** 증상은 네트워크 탭의 404 하나와 콘솔의 CORS 에러로 나타나며, 정작 본 요청은 서버 로그에 흔적조차 남지 않는다.

Vite 프록시로 같은 오리진을 만들면 preflight가 아예 생기지 않아 피할 수 있다. **다만 그건 결정 사항이지 자동으로 되는 게 아니다** — 프록시를 설정하지 않으면 그대로 걸린다.

| 대상 | 영향 |
|---|---|
| TASKS #11 이후 프론트엔드 | **직접 영향.** 프록시를 쓰지 않는 경우에 한한다 |
| `curl`·스모크 등 서버 대 서버 호출 | **영향 없음.** preflight를 보내지 않는다 |
| 단순 요청(simple request) | **[추론]** preflight가 생기지 않는 조합(GET/HEAD, 혹은 특정 content-type의 POST)은 이 결함을 지나가지만, `application/json` POST는 preflight를 유발하므로 우리 API 대부분이 해당된다 |

**[코드 확인]** 현재 스모크(`backend/src/server/smoke.ts`, 39건)에 `OPTIONS`를 보내는 케이스가 **없다.** 그래서 이 결함이 통과 중인 테스트로는 잡히지 않는다.

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **catch-all에서 `OPTIONS`를 제외하고 express 기본 처리로 넘긴다.** 가장 좁은 수정이며 새 의존성이 없다.
   - **[추론]** 고려할 점: 정말 존재하지 않는 경로로 온 `OPTIONS`가 어떤 응답을 받게 되는지 확인해야 한다. 그리고 이것만으로는 CORS 응답 헤더(`Access-Control-Allow-Origin` 등)가 붙지 않으므로, preflight가 200을 받아도 브라우저가 통과시킬지는 별개다.
2. **CORS 미들웨어를 명시적으로 두고 preflight를 직접 처리한다.** 허용 오리진을 게이트웨이가 정책으로 갖게 된다.
   - **[추론]** 고려할 점: `cors` 패키지를 도입할지는 별도 판단이다. **현재 방침은 새 의존성 최소화**이므로 기본값은 "직접 쓴다"에 가깝다.
3. **Vite 프록시로 같은 오리진을 만들어 preflight를 아예 없앤다.** 게이트웨이 코드를 건드리지 않는다.
   - **[추론]** 고려할 점: 개발 환경만 해결한다. 배포에서 프론트와 게이트웨이가 다른 오리진이면 문제가 되돌아오므로, 배포 형태(정적 파일을 게이트웨이가 서빙할지 여부)와 함께 정해야 한다.

<a id="issue-002-결정"></a>

### 결정

**3안 채택 — 같은 오리진 구조로 간다. CORS를 도입하지 않으며, 따라서 `OPTIONS` 404를 고치지 않는다.** 2026-08-06 확정.

발견 당시 3안에 붙여 둔 대가("개발 환경만 해결한다")는 **배포까지 같은 오리진으로 맞춰** 없앤다. 브라우저가 항상 한 곳만 보게 만들어 preflight가 **발생하지 않게** 하는 것이 요지다.

```
개발    브라우저 → Vite 개발서버(5173) ─프록시(/api, /ws)→ 게이트웨이(3000)
배포    브라우저 → 게이트웨이(3000) ← 프론트 빌드 결과물(dist/)도 여기서 서빙
```

**[코드 확인]** 이것은 새 방향이 아니라 **원래 설계로 돌아간 것**이다. `DESIGN.md:354`(§6.2 게이트웨이의 HTTP 엔드포인트 표)에 `GET /*` → `정적 파일 (프로덕션)`이 이미 있었다. 발견 당시 3안에 "배포 형태(정적 파일을 게이트웨이가 서빙할지 여부)와 함께 정해야 한다"고 적어 둔 항목의 답이 설계 문서에 이미 있었던 셈이다.

**1·2안을 지금 넣지 않는 이유** **[추론]**

- **2안(CORS 미들웨어)** — 같은 오리진에서는 preflight가 아예 생기지 않으므로, 넣어 봐야 **쓰이지도 않을 허용 오리진 설정을 유지**하게 된다. 별도 도메인이 실제로 필요해지는 시점은 곧 요구사항이 확정된 시점이므로, 그때 넣는 편이 더 정확하다.
- **1안(catch-all에서 `OPTIONS` 제외)** — 발견 당시 스스로 적어 둔 대로 이것만으로는 CORS 응답 헤더가 붙지 않아 브라우저가 통과시킬지가 별개로 남는다. 같은 오리진에서는 애초에 필요가 없다.
- 이 문제의 원인은 **포트가 다르다는 것**(`5173` vs `3000`)이고, 채택안은 포트를 하나로 만들어 원인 자체를 없앤다. 1·2안은 원인을 남긴 채 증상을 처리한다.

<a id="issue-002-무효-조건"></a>

#### 무효 조건 — 이 중 하나라도 하면 이슈를 다시 연다

**결함은 고쳐지지 않았다.** **[코드 확인]** 경로 제한 없는 catch-all은 그대로 있고(현재 `backend/src/server/index.ts:295-297`, 발견 당시 `:227`), `OPTIONS`는 여전히 404다. 위 결정은 **브라우저가 preflight를 보내지 않는다**는 전제 위에서만 성립한다. 다음 중 하나라도 하면 전제가 깨지고 ISSUE-002가 그대로 되살아난다.

- 프론트엔드를 CDN이나 별도 도메인에 배치한다
- 게이트웨이와 다른 호스트에서 프론트를 서빙한다
- 서드파티가 이 API를 브라우저에서 직접 호출한다

그때 필요한 조치는 위 [검토된 해결 방향](#issue-002)의 1안(catch-all에서 `OPTIONS` 제외) 또는 2안(CORS 미들웨어 도입)이다. **상태를 `열림`으로 되돌리고 이 결정을 다시 쓸 것.**

**[코드 확인]** 전제가 깨진 것을 테스트가 알려주지 않는다 — `backend/src/`에 `OPTIONS`라는 문자열이 **한 번도 등장하지 않는다.** 발견 당시 "스모크에 `OPTIONS` 케이스가 없다"고 적힌 상태가 지금도 그대로다. 게다가 전제 파기는 코드 변경이 아니라 **배포 구성 변경**으로 일어나므로 코드 리뷰에도 걸리지 않는다. 감지 지점은 이 문단뿐이다.

#### 후속 작업

구현은 **TASKS #11 "Vite 골격 + WS 클라이언트"**에서 한다 — `frontend/vite.config.ts`에 `/api`·`/ws` 프록시, 게이트웨이에 `dist/` 정적 서빙 라우트. 후자는 [ISSUE-001의 결정](#issue-001-결정)이 만든 등록 지점(catch-all **앞**)에 붙는다. 발견 당시 ISSUE-001의 영향 범위 표가 "(미할당) 정적 파일 서빙"을 **[추론]**으로 예고해 둔 바로 그 항목이다.

**지금 하지 않는 이유**: **[코드 확인]** `frontend/`가 비어 있어 서빙할 빌드 결과물이 없다. 지금 라우트만 만들면 빈 껍데기다.

### 참고 — 깨진 JSON은 없는 경로에서도 404가 아니라 400이다

**이슈가 아니라 참고 사항이다.** Tester가 스모크 §5에서 명문화했다.

**[코드 확인]** `backend/src/server/smoke.ts:452-462`가 이를 테스트로 고정해 두고 있다 — 존재하지 않는 경로(`POST /api/__no-such-route`)에 깨진 JSON 본문(`{`)을 보내면 **404가 아니라 400**이 온다. 원인은 본문 파서 `express.json()`이 `backend/src/server/index.ts:187`에서 **라우팅보다 앞에** 등록되기 때문이다. 파서가 먼저 던지므로 요청이 라우터에 도달조차 하지 않는다.

**[추론]** **버그가 아니라 express 표준 동작이다.** 이 문서의 대상("발견됐지만 고치지 않은 문제")에 해당하지 않아 독립 이슈로 올리지 않고, 미들웨어 순서가 응답 의미를 바꾼다는 점에서 ISSUE-002와 같은 계열이므로 여기에 붙여 둔다.

기록하는 이유는 하나다: **클라이언트가 한 응답만으로 "경로가 틀렸는지 본문이 틀렸는지"를 구분할 수 없다.** 400을 받은 쪽은 경로가 맞다고 가정하기 쉬운데 실제로는 경로도 틀렸을 수 있다. **[추론]** API 클라이언트를 만들 때 400을 "경로는 유효함"의 근거로 삼지 말 것.

---

<a id="issue-003"></a>

## ISSUE-003 — 잘못된 `content-type`으로 업로드하면 원인을 지목하지 못하는 에러가 나간다

- **상태**: 열림
- **발견**: 2026-08-06, TASKS.json #5 "씬 업로드 + 목록"(커밋 `41ff68e`)의 Tester 검증 중. **#5의 통과 기준 자체는 PASS다.**
- **영역**: gateway — `backend/src/server/index.ts`, `backend/src/server/files.ts`

**보안 결함이 아니다.** 아래 세 갈래 모두 요청은 거절되고 파일도 생기지 않는다. **진단 품질 문제다** — 클라이언트가 받는 에러가 진짜 원인(헤더 한 줄)을 지목하지 못한다.

> **[코드 확인]** 이 항목의 행 번호는 커밋 `41ff68e` 시점 기준이다. `backend/src/server/index.ts`·`files.ts`는 이후 단위(#6~)에서 계속 늘어나므로, 나중에 같은 행을 찾으면 다른 코드가 나올 수 있다. 행 번호가 어긋나면 심볼 이름(`#configure`, `assertStreamableBody`)으로 찾을 것.

### 증상

**[코드 확인]** `POST /api/scenes`에 `content-type: application/json`으로 보내면 **본문에 따라 세 갈래로 갈린다.**

| 본문 | 실제 응답 | 어디서 나오는가 |
|---|---|---|
| 유효한 JSON | **415** | 씬 라우트의 가드가 작동 (`backend/src/server/files.ts:345-350`) |
| 파일 바이트, 1MB 미만 | **400** `"... is not valid JSON"` | `express.json()`의 파싱 실패. 가드에 닿지 못함 |
| 파일 바이트, 1MB 초과 | **413** `"request entity too large"` | `express.json()`의 `limit` 초과. 가드에 닿지 못함 |

Builder가 보고한 API 명세는 "`content-type: application/json` → 415"였다. **절반만 맞다** — 세 갈래 중 첫 줄에서만 성립한다.

**[코드 확인]** Tester는 이 갈림을 이미 테스트에 반영해 뒀다. `backend/src/server/smoke.ts:870-877`은 유효 JSON에 대해서만 `415`를 단언하고, `backend/src/server/smoke.ts:879-888`은 파일 바이트에 대해 **status를 단언하지 않고** "거절됐고 파일이 안 생겼다"만 확인한 뒤 실제 status를 `note`로 남긴다(`'설계 문서 표는 415라고 적혀 있지만 express.json이 먼저 파싱에 실패한다'`). 즉 **테스트는 이 이슈를 알고 우회해 둔 상태**이고, 고치면 그 `note`가 무의미해진다.

### 원인

**[코드 확인]** 미들웨어 등록 순서다.

1. `backend/src/server/index.ts:225` — `#configure`가 전역 `app.use(express.json({ limit: '1mb' }))`를 등록한다.
2. `backend/src/server/index.ts:258` — 씬 라우트는 그보다 **뒤**인 "라우트 등록 지점"에서 등록된다(`use(createSceneRoutes(this.#scenes))`).
3. `backend/src/server/files.ts:384` — 415 가드 `assertStreamableBody(req)`는 그 라우트 핸들러의 **첫 줄**이다.

`express.json()`은 `content-type`이 `application/json`일 때만 동작하지만, 동작하는 경우에는 라우팅보다 먼저 본문을 소진하거나 던진다. 따라서 **씬 라우트의 415 가드는 json 파서가 통과시킨 본문에만 닿는다.** 파서가 먼저 실패하면(깨진 JSON → 400, `limit` 초과 → 413) 씬 라우트는 **실행조차 되지 않는다.**

**[코드 확인]** 가드 자신도 이 구조를 알고 쓰였다 — `backend/src/server/files.ts:341-344`의 주석이 "전역 `express.json({limit:'1mb'})`이 요청 스트림을 이미 소진해서 0바이트 파일이 생기거나, 1MB를 넘으면 파서가 413으로 먼저 끊는다"고 적고 있다. 가드는 **전자(0바이트 파일)를 막는 데는 성공했고**, 후자는 애초에 자기 앞에서 끝난다.

**[추론]** 이것은 [ISSUE-002](#issue-002) 및 그 아래 참고 절("깨진 JSON은 없는 경로로 보내도 400", `backend/src/server/smoke.ts:599-609`)과 **같은 계열이다 — 미들웨어 순서가 응답의 의미를 결정한다.** 지금까지 세 번째 사례다.

### 영향 범위

가장 나쁜 것은 **1MB 초과** 갈래다.

**[추론]** 103MB짜리 `.zls`를 `content-type`만 틀리게 보낸 클라이언트가 `413 request entity too large`를 받는다. 이걸 읽으면 **"씬이 서버 상한을 넘었다"로 해석하게 되는데 사실이 아니다.** 실제 씬 상한은 `DEFAULT_MAX_SCENE_BYTES` = **512MiB**이고(`backend/src/server/files.ts:58-59`), 103MB는 그 5분의 1이다. 진짜 원인은 헤더 한 줄이며, 응답 어디에도 그 단서가 없다.

같은 413인데 출처가 둘이라는 점이 특히 나쁘다 — 하나는 본문 파서의 1MB, 하나는 씬 저장소의 512MiB(`backend/src/server/files.ts:257-259`, `:288`, `:303`)다. 클라이언트는 status만으로 둘을 구분할 수 없다.

**[추론]** 씬 라우트의 415 가드가 막으려던 실패 모드가 정확히 이것이었는데, 파서가 앞에 있어 가드까지 도달하지 못한다.

| 대상 | 영향 |
|---|---|
| TASKS #11 이후 프론트엔드 | **직접 영향.** 업로드를 붙일 때 헤더를 한 번 틀리면 이 오진을 만난다 |
| Node SDK·서버 대 서버 호출 | **[추론]** 헤더를 코드로 고정하므로 한 번 맞으면 다시 틀리지 않는다. 노출은 낮다 |
| 올바른 클라이언트 | **영향 없음.** `application/octet-stream`으로 보내면 어느 갈래도 타지 않는다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **씬 라우트를 `express.json()` 앞에 등록한다.** 순서 문제이므로 순서로 푼다.
   - **[추론]** 고려할 점: 현재 등록 지점(`backend/src/server/index.ts:238-261`)이 json 파서(`:225`) **뒤**에 오도록 설계돼 있다. 앞으로 옮기려면 "모든 라우트는 한 자리에서 순서대로 등록된다"는 [ISSUE-001](#issue-001-결정)의 B안 구조를 손봐야 하고, 파서를 필요로 하는 라우트(#7~ 세션 API의 JSON 본문)와 그렇지 않은 라우트를 갈라야 한다.
2. **`express.json()`에 경로 예외를 둔다.** `type` 함수 등으로 `POST /api/scenes`를 건너뛰게 한다.
   - **[추론]** 고려할 점: 구조를 안 건드리는 대신 예외 조건이 `#configure` 안에 숨는다. 라우트가 늘면 예외 목록도 같이 늘어나고, 어긋나면 다시 조용한 오진이 된다.
3. **그대로 두고 API 문서에 명시한다.** 보안 문제가 아니고 올바른 클라이언트는 만나지 않는 실패다.
   - **[추론]** 고려할 점: Builder의 명세("→ 415")가 이미 틀린 채로 유통됐다는 사실은 남는다. 3안을 택한다면 **명세를 위 세 갈래 표로 고치는 일까지가 3안이다.**

### 결정

**미결. TASKS #11(프론트엔드 골격)이 업로드를 실제로 붙일 때 재검토한다.** **[추론]** 어느 쪽도 지금 당장 급하지 않다 — 현재 유일한 클라이언트인 스모크는 헤더를 올바로 보내고, 틀린 경우도 이미 테스트로 고정돼 있다. 브라우저에서 사람이 헤더를 틀릴 수 있게 되는 시점이 재검토 시점이다.

### 참고 — json/multipart 외의 `content-type`은 전부 허용된다 (의도된 설계)

**이슈가 아니다.** 위 해결 방향 2안이 이 계약과 맞물리므로 함께 적어 둔다.

**[코드 확인]** `assertStreamableBody`(`backend/src/server/files.ts:338-359`)가 막는 것은 `application/json`과 `multipart/form-data` **둘뿐**이다. 그 외 값은 검사 없이 통과해 업로드가 성립한다. `backend/src/server/smoke.ts:896-897`이 `application/x-zls` → **201**로 이를 계약으로 박아 뒀다.

**[추론]** 화이트리스트가 아니라 **블랙리스트인 것이 의도**다. 두 타입만 요청 스트림을 가로채거나 파싱을 요구하기 때문이고, 나머지는 파일 바이트가 그대로 디스크로 흘러도 무해하다. 나중에 화이트리스트로 좁히면 `backend/src/server/smoke.ts:897`의 단언이 깨지며 알려준다 — **그 실패는 회귀가 아니라 정책 변경의 신호**이므로, 그때 이 문단을 함께 고칠 것.

---

<a id="issue-004"></a>

## ISSUE-004 — 413 뒤 keep-alive 소켓 재사용이 테스트로 증명되지 않는다

- **상태**: 열림
- **발견**: 2026-08-06, TASKS.json #5 "씬 업로드 + 목록"(커밋 `41ff68e`)의 Tester 검증 중. **#5의 통과 기준은 PASS다.** 이것은 결함 보고가 아니라 **검증 공백** 기록이다.
- **영역**: gateway — `backend/src/server/smoke.ts` (대상 코드는 `backend/src/server/files.ts`)

> **[코드 확인]** 행 번호는 커밋 `41ff68e` 기준이다. 스모크는 단위마다 늘어나므로 나중에는 어긋난다. 어긋나면 섹션 제목(`6-5. 업로드 상한`)과 체크 라벨(`'413 뒤 다음 요청 정상'`)로 찾을 것.

### 증상

**[코드 확인]** `backend/src/server/smoke.ts:936-939`의 체크 `'413 뒤 다음 요청 정상'`은 413을 받은 **직후의 다음 요청이 200인지만** 본다. 정작 검증하려던 명제는 "**같은 keep-alive 소켓**에서 다음 요청이 정상"인데, **소켓 동일성을 강제하지 않는다.** 주석(`backend/src/server/smoke.ts:936-937`)이 스스로 근거를 밝히고 있다 — "undici는 기본적으로 연결을 재사용한다"는 **전제에 기대고 있다.**

**[추론]** 따라서 undici가 어떤 이유로든 새 연결을 맺으면(연결 풀 정책 변경, 서버가 조용히 `Connection: close`를 붙이게 되는 회귀 등) 이 체크는 **여전히 통과한다.** 지키려던 성질이 깨져도 초록불이다.

### 원인

**[코드 확인]** 지켜야 할 동작은 우연이 아니라 **Builder가 의도적으로 넣은 안전장치**다. `backend/src/server/files.ts:279-303`이 상한을 넘긴 뒤에도 `OVERFLOW_DRAIN_BUDGET`(8MiB, `backend/src/server/files.ts:62-67`)만큼 본문을 **버려가며 계속 읽어** 요청을 정상 종료시킨다. 주석이 이유를 적고 있다(`:281-285`): 즉시 던지면 pipeline이 요청 스트림을 파괴하고, 아직 본문을 보내는 중인 클라이언트는 **413 대신 `ECONNRESET`을 본다 — 실제로 그렇게 동작하는 걸 확인했다**고 돼 있다.

**[코드 확인]** 짝이 되는 분기도 있다. `backend/src/server/files.ts:392-403`은 **본문이 끝까지 읽히지 않은 채** 실패한 경우에만 `Connection: close`를 붙이고, 드레인으로 끝까지 읽은 경우에는 **소켓을 재사용하게 둔다**(`if (!res.headersSent && !req.readableEnded)`). 즉 "413 뒤에도 소켓이 살아 있다"는 것은 부수효과가 아니라 **명시적으로 설계된 두 갈래의 한쪽**이다.

**[추론]** 문제는 여기다: 설계는 두 갈래를 구분하는데, 테스트는 **다음 요청이 200이라는 사실 하나로 두 갈래를 구분하지 못한다.** `Connection: close`가 잘못 붙어 매번 새 연결이 맺어져도 200은 나온다.

### 영향 범위

**[추론]** 실제 서비스에서 이 성질이 깨지면 증상은 "대용량 업로드를 한 번 거절당한 뒤 그 클라이언트의 다음 요청이 `ECONNRESET`으로 죽는다"이다. 브라우저·`fetch`·SDK 모두 연결을 재사용하므로 노출 대상이 넓고, 증상이 **거절당한 요청이 아니라 그 다음 요청에** 나타나기 때문에 원인 추적이 어렵다. Builder가 코드에 "재현됨"이라고 적어 둔 실패 모드가 정확히 이것이다.

| 대상 | 영향 |
|---|---|
| 회귀 감지 | **직접 영향.** 8MiB 드레인 로직을 나중에 "불필요해 보인다"며 걷어내도 스모크는 통과한다 |
| 현재 동작 | **[추론]** 영향 없음. 코드는 의도대로 동작하고 있을 가능성이 높다 — 증명이 없을 뿐이다 |
| TASKS #11 이후 프론트엔드 | **[추론]** 간접. 브라우저가 연결을 재사용하므로 회귀 시 가장 먼저 만난다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **응답 헤더로 간접 확인한다.** 413 응답에 `Connection: close`가 **없음**을 단언한다. `backend/src/server/files.ts:403`이 붙이는 헤더를 직접 보는 것이라 대상이 명확하다.
   - **[추론]** 고려할 점: 서버가 소켓을 끊지 않겠다고 말했다는 것까지만 증명한다. 클라이언트가 실제로 재사용했는지는 여전히 미확인이다. **가장 싸고, 회귀의 대부분을 잡는다.**
2. **서버 쪽에서 소켓 동일성을 센다.** `http.Server`의 `connection` 이벤트를 세거나 핸들러에서 `req.socket`을 식별해, 413 전후 요청이 같은 소켓인지 단언한다.
   - **[추론]** 고려할 점: 명제를 그대로 증명한다. 대신 스모크가 서버 내부에 손을 넣게 되고, `Gateway`가 그걸 노출할 방법이 지금 없다(`server` 게터로 가능은 하다).
3. **undici `Agent`를 명시적으로 만들어 연결 재사용을 강제한다.** 전제를 전제로 두지 않고 테스트가 통제한다.
   - **[추론]** 고려할 점: 클라이언트 쪽 통제이므로 서버 코드를 안 건드린다. 다만 새 의존성은 아니어도(undici는 Node 내장) 스모크의 fetch 경로가 복잡해진다.

### 결정

**미결.** **[추론]** 1안이 비용 대비 효과가 가장 크지만, 이 이슈는 **버그가 아니라 증명 공백**이므로 지금 당장 조치할 이유는 약하다. 기록해 두는 이유는 하나다 — 8MiB 드레인 로직(`backend/src/server/files.ts:279-303`)은 **왜 있는지 모르면 지우고 싶어지는 코드**이고, 지웠을 때 스모크가 막아 주지 못한다. 그 로직을 건드리는 작업이 생기면 그때 1안 또는 2안을 함께 넣는다.

---

<a id="issue-005"></a>

## ISSUE-005 — 종료가 진행 중인 acquire와 겹치면 `Gateway.close()`가 영원히 끝나지 않는다

- **상태**: **닫힘** — 2026-08-06, 커밋 `f077d0f` "feat(gateway): WS 연결 = 세션 수명"으로 반영. 아래 [결정](#issue-005-결정) 참고.
- **심각도**: **심각** — 프로세스가 종료되지 않는다. 004까지의 이슈와 달리 응답 품질이 아니라 **프로세스 수명**이 걸려 있다.
- **발견**: 2026-08-06, TASKS.json #6 "WS 연결 = 세션 수명"의 Tester 검증 중. **스모크 149/150 — 실패한 1건이 이것이다.**
- **영역**: gateway — `backend/src/server/sessions.ts`, `backend/src/server/index.ts`

> **#6의 통과 기준 자체는 전부 PASS다.** 실제 워커 프로세스로 검증했고 — 연결 → `busy` 1 → 종료 → 0, 상한 초과 거절, 종료 후 pid 소멸까지 확인됐다. **이 이슈는 기준 밖에서 Tester가 추가로 잡은 별건이다.** 다만 아래 [결정](#issue-005-결정)대로 스모크가 이걸 실패로 세므로, **#6을 다시 통과시키려면 이걸 고쳐야 한다.**

> **[코드 확인]** 이 항목의 행 번호는 **커밋되지 않은 작업 트리** 기준이다. #6은 검증 실패로 여기서 중단됐고 커밋되지 않았으므로, 이후 어떤 형태로든 달라질 수 있다. 행이 어긋나면 심볼로 찾을 것 — `SessionManager`의 **`#serve`** 메서드(취소 분기), 모듈 스코프 함수 **`refuse`**, `Gateway`의 **`close()`**.

### 증상

**[코드 확인]** 세션 확보(`pool.acquire()`, 실측 ~110ms)가 **진행 중일 때** `gw.close()`를 부르면 **`Gateway.close()`가 resolve되지 않는다.** 4초·8초 두 관찰 모두 미완이었다.

**세션 정리 자체는 정상이다.** 같은 실행에서 `acquired=1, released=1, closed=1`, `stats.busy === 0`이 모두 성립한다 — 즉 워커는 반납됐고 풀도 닫혔다. 매달리는 것은 그 **다음 단계인 `http.close()`**다.

**[코드 확인]** 실패하는 체크는 `backend/src/server/smoke.ts:1641-1646`의 `'acquire 완료 → close()가 매달리지 않고 끝난다'`이며, 바로 뒤의 `'그 사이 얻은 세션은 반납된다'`(`:1647-1651`)와 `'풀도 닫힌다'`(`:1652`)는 통과한다. 이 대비가 위 문단을 그대로 보여 준다.

### 원인

**[코드 확인 — 격리 실험으로 확정]** `backend/src/server/sessions.ts:462-471`, `#serve()`의 취소 분기:

```ts
// ② 110ms 창. 주인이 사라졌거나 그 사이 게이트웨이가 닫혔으면 즉시 반납한다.
if (gone || socket.destroyed || this.#pool !== pool) {
  await pool.release(session).catch(() => {});
  this.#log(...);
  return;                    // ← 소켓에 응답도 destroy도 하지 않고 버린다
}
```

조건이 세 갈래인데 **뒷감당이 하나뿐이다.**

1. `gone` / `socket.destroyed` — 소켓이 **이미 죽어 있다.** 그냥 `return`해도 무해하다.
2. `this.#pool !== pool` — **게이트웨이가 종료 중이라는 뜻일 뿐, 소켓은 멀쩡히 살아 있다.** 그런데 같은 `return`으로 처리되어 **살아 있는 업그레이드 소켓이 버려진다.**

**[코드 확인]** 다른 모든 거절 경로는 `refuse()`(`backend/src/server/sessions.ts:196-220`)를 지나며, 그 함수는 응답을 쓰고 `socket.end()`로 닫는다(`:219`). 상한 초과 503(`:453`), 워커 기동 실패 502(`:456`), 씬 없음 404(`:438`)가 전부 그렇다. **이 취소 분기만 예외다.**

**[추론]** Node의 http 서버는 `'upgrade'` 이벤트가 나간 뒤의 소켓에서 자기 `close` 리스너를 뗀다(소켓 소유권이 핸들러로 넘어간다는 전제다). 그래서 버려진 그 연결이 서버의 활성 카운트에 **영구히 남고**, `backend/src/server/index.ts:381-384`의 `this.#http.close(cb)` 콜백이 오지 않아 `close()`의 Promise가 풀리지 않는다. 같은 자리의 `closeAllConnections()`(`:383`)도 **업그레이드된 소켓은 건드리지 못한다** — 그래서 이 안전망이 작동하지 않는다.

### 격리 실험

**[코드 확인]** 버려진 그 소켓을 테스트가 직접 파괴하는 **시점만** 바꿔 측정했다.

| 조건 | `close()` 소요 |
|---|---|
| 파괴 안 함 | **매달림 (>4000ms)** |
| 파괴 (close 전) | 38ms |
| 파괴 (close 후) | 66ms |

**클라이언트가 `terminate()`해도 풀리지 않는다** — FIN만으로는 서버 쪽 소켓이 닫히지 않는다. **오직 서버가 그 소켓을 처리해야 풀린다.** 이 사실이 해결 방향을 서버 쪽으로 고정한다.

### 영향 범위

**[추론]** **SIGTERM 재기동이 클라이언트 접속 순간과 겹치면 게이트웨이 프로세스가 종료되지 않는다.** 창은 acquire 1회(~110ms)로 좁지만, 걸리면 결과는 **영구 정지**다. 배포·재시작 자동화가 프로세스를 SIGKILL로 강제 종료해야 풀린다.

| 대상 | 영향 |
|---|---|
| 배포·재기동 자동화 | **직접 영향.** graceful shutdown이 반환되지 않아 파이프라인이 타임아웃까지 대기한 뒤 강제 종료로 넘어간다 |
| 세션·워커 정리 | **영향 없음.** **[코드 확인]** 반납·풀 종료는 정상 완료된다. 좀비 exe가 남지는 않는다 |
| 스모크 하네스 | **[코드 확인]** 매달리지 않는다 — 아래 참고 |

**[코드 확인]** 스모크가 매달리지 않는 이유는 **테스트가 그 소켓을 스스로 정리하기 때문**이다. `backend/src/server/smoke.ts:1617-1623`이 `gw2.server.on('upgrade', …)`로 소켓을 붙잡아 두고, 단언이 깨졌을 때 하네스를 오염시키지 않도록 정리한다(`:1626-1653`의 블록, 주석이 그 의도를 명시한다). **즉 지금 스모크가 끝까지 도는 것은 결함이 가볍다는 증거가 아니라, 테스트가 우회해 둔 결과다.**

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **종료 중 취소 갈래에서 `refuse(socket, 503, …)`를 부른다.** 다른 거절 경로와 처리가 통일되고, 클라이언트가 이유(종료 중)를 받는다.
2. **종료 중 취소 갈래에서 `socket.destroy()`한다.** 가장 짧다. 대신 클라이언트는 이유 없이 끊긴 것만 본다.

**[추론]** 어느 쪽이든 **조건을 갈라야 한다.** 지금의 `if`는 세 조건이 한 덩어리인데, 앞의 둘(`gone`, `socket.destroyed`)은 소켓이 이미 죽어 있어 지금 처리가 옳다. 죽은 소켓에 그대로 쓰기를 시도하면 별도 문제를 만든다.

**[코드 확인]** 다만 `refuse` 자신이 `socket.destroyed`(`:202`)와 `!socket.writable`(`:203-206`)을 이미 검사한다. 조건을 안 가르고 1안을 넣어도 죽은 소켓에서 곧바로 반환되므로 **쓰기 사고가 나지는 않는다.** 조건을 가르는 것은 안전 문제라기보다 **로그와 의도를 구분하기 위한 것**이다(현재 `:465-469`의 로그가 이미 두 문구를 나눠 쓰고 있다).

<a id="issue-005-결정"></a>

### 결정

**1안 채택 — 종료 중 취소 갈래에서 `refuse(socket, 503)`으로 거절하고 소켓을 닫는다.** 커밋 `f077d0f`으로 반영 완료.

> **[코드 확인]** 위 "증상"·"원인"·"격리 실험" 절의 행 번호는 **커밋되지 않은 작업 트리** 기준이었다. 그 상태가 이제 `f077d0f`로 커밋됐으므로, 아래에는 **현재 위치**를 병기한다. 발견 당시 기록은 그대로 둔다.

**반영 내용** **[코드 확인]** (`backend/src/server/sessions.ts`를 읽어 확인):

- `#serve()`의 세 조건 `if`를 **둘로 갈랐다**(발견 당시 `:462-471`):
  1. `gone || socket.destroyed` → 반납만 하고 return (`backend/src/server/sessions.ts:463-467`). 소켓이 이미 죽어 쓸 대상이 없다.
  2. `this.#pool !== pool` (게이트웨이 종료 중) → **`refuse(socket, 503, '게이트웨이가 종료 중입니다', { 'retry-after': '5' })` 후 반납**(`backend/src/server/sessions.ts:469-483`).
- **조건을 가른 이유는 안전이 아니라 로그와 의도의 구분이다.** 발견 당시 "검토된 해결 방향" 말미에 적어 둔 그대로다 — `refuse()`(`backend/src/server/sessions.ts:196-220`)가 `socket.destroyed`(`:202`)와 `!socket.writable`(`:203-206`)을 이미 검사하므로 죽은 소켓에 써도 위험하지 않다. 앞 갈래는 "클라이언트가 떠났다"(우리가 할 일이 없다), 뒤 갈래는 "우리가 문을 닫는다"(살아 있는 상대에게 알려야 한다)이다.
- **순서: `refuse()` → `await pool.release(session)`.** 반대로 두면 `release()`가 `dispose()`의 quit 왕복(최대 3초)을 기다리는 동안 클라이언트가 이유도 모른 채 매달린다. 코드에 주석으로 남아 있다(`backend/src/server/sessions.ts:477-478`).

**결과** **[실측]**

| | 이전 | 이후 |
|---|---|---|
| `Gateway.close()` (`backend/src/server/index.ts:374`) | 매달림 (>4000ms 미완) | **1~4ms** (FakePool) / 113ms (실제 풀) |

세션은 정상 반납되고(`stats.busy === 0`), 클라이언트는 503 + `Retry-After`를 실제로 받는다.

#### 회귀 방지

**[코드 확인]** Tester가 `backend/src/server/smoke.ts` §7-3(`:1684-1845`)에 단언 3개를 추가했다.

1. **`'버려진 업그레이드 소켓이 없다 (서버가 응답하고 닫았다)'`**(`backend/src/server/smoke.ts:1816-1822`) — `socket.writableEnded === true`. 옛 코드에서는 false다.
2. **`'그 클라이언트는 503 + Retry-After를 받는다 (매달리지 않는다)'`**(`:1796-1803`) — `close()`만 빨라지고 클라이언트가 타임아웃까지 기다리면 고친 게 아니기 때문이다.
3. **`'문구가 "종료 중" 쪽'`**(`:1804-1809`) — 아래 계약 참고.

**[코드 확인]** 테스트가 스스로 버려진 소켓을 정리하던 코드는 **남겼다**(`backend/src/server/smoke.ts:1824-1841`, `finally`의 `abandoned?.destroy()`). 발견 당시 "스모크가 매달리지 않는 이유는 테스트가 그 소켓을 스스로 정리하기 때문"이라고 적은 그 코드다. **[추론]** 이제는 결함을 가리지 않는다 — `finished` 판정 **이후**에 실행되므로 단언이 먼저 깨지고, 회귀 시 뒤따르는 `close()`가 워치독으로 죽는 것을 읽을 수 있는 FAIL로 바꾸는 역할만 한다.

#### 함께 고정된 계약 — 두 개의 503 문구

**[코드 확인]** 이 수정으로 게이트웨이가 업그레이드를 503으로 거절하는 자리가 **둘**이 됐다. 둘은 상태코드도 `retry-after`도 같아서 클라이언트가 가를 단서가 **문구뿐**인데, 운영상 의미는 정반대다.

| 문구 | 자리 | 뜻 |
|---|---|---|
| `게이트웨이가 세션을 받을 수 있는 상태가 아닙니다` | `backend/src/server/sessions.ts:398` | "다시 걸면 언젠가 된다" (기동 전) |
| `게이트웨이가 종료 중입니다` | `backend/src/server/sessions.ts:479` | "이 게이트웨이는 지금 죽는 중" |

**[코드 확인]** 스모크가 상수 `REFUSE_NOT_ACCEPTING` / `REFUSE_SHUTTING_DOWN`(`backend/src/server/smoke.ts:146-147`)을 두고, 각 자리에서 **자기 문구 일치 + 상대 문구 불일치**를 함께 단언한다(`:1554-1560`, `:1804-1809`). **[추론]** 한쪽으로 문구를 통일하려는 손이 오면 반드시 한 건이 깨진다.

---

<a id="issue-006"></a>

## ISSUE-006 — 워커 exe 경로가 틀리면 502가 아니라 게이트웨이 프로세스가 죽는다

- **상태**: **닫힘** — 2026-08-06, 커밋 `f077d0f` "feat(gateway): WS 연결 = 세션 수명"으로 반영. 아래 [결정](#issue-006-결정) 참고.
- **심각도**: **심각** — 서버 전체가 죽는다. 요청 하나가 실패하는 것과 장애 범위가 다르다.
- **발견**: 2026-08-06, TASKS.json #6 "WS 연결 = 세션 수명"의 Tester 검증 중. **#6의 통과 기준 자체는 실제 워커로 전부 PASS이며, 이것은 기준 밖에서 잡힌 별건이다.**
- **영역**: **sdk** — `backend/src/sdk/worker.ts` (지금까지의 이슈와 달리 gateway가 아니다)

> **[코드 확인]** 행 번호는 **커밋되지 않은 작업 트리** 기준이다. 어긋나면 심볼로 찾을 것 — `Worker` 클래스의 정적 메서드 **`spawn`**.

### 증상

**[코드 확인]** 설계 문서와 Builder 명세는 "워커 기동 실패(exe 없음) → **502**"라고 적고 있다. **성립하지 않는다.**

재현하면 `UNCAUGHT: spawn … ENOENT`가 나면서 **게이트웨이 프로세스 전체가 죽는다.** 502 응답은 나가지 않는다.

### 원인

**[코드 확인]** `backend/src/sdk/worker.ts:92-105`의 `Worker.spawn()`이 `child_process.spawn()`으로 자식을 만든 뒤 곧바로 `new Worker(child, opts)`를 반환한다. 등록되는 리스너는 생성자의 **`child.on('exit', …)`**(`backend/src/sdk/worker.ts:78`) 하나뿐이고, **`child.on('error')` 리스너가 어디에도 없다.** 파일 전체에서 `'error'` 문자열이 이 파일에 등장하지 않는 것을 확인했다.

**[추론]** Node에서 `EventEmitter`의 `'error'`는 리스너가 없으면 unhandled로 승격되어 프로세스를 종료시킨다. `child_process`의 spawn 실패(ENOENT 등)는 예외로 던져지는 게 아니라 **비동기 `'error'` 이벤트로 전달**되므로, 호출부를 `try/catch`로 감싸도 잡히지 않는다. `backend/src/server/sessions.ts:454-457`의 502 분기는 `pool.acquire()`가 **reject될 때** 도는데, 여기서는 reject 대신 프로세스가 죽는다.

### 영향 범위

**[추론]** **운영에서 exe 경로 오타 하나가 게이트웨이를 못 뜨게 한다.** 게다가 실패 형태가 "요청 하나가 502"가 아니라 "**서버 전체 사망**"이라 장애 범위가 다르다 — 그 시점에 붙어 있던 다른 세션들도 함께 끊긴다.

만나는 시나리오는 특별하지 않다. **[추론]** 배포 환경이 바뀌어 경로가 이동하거나, 데모용/배포용 exe를 바꿔 끼울 때(`zelusSandBoxd-demo.exe` ↔ `zelusSandBoxd.exe`) 바로 걸린다.

| 대상 | 영향 |
|---|---|
| 운영 배포 | **직접 영향.** 경로 설정 실수의 결과가 "그 요청 실패"가 아니라 "프로세스 종료"다 |
| 기동 시점 vs 요청 시점 | **[추론]** spawn은 세션 확보 시점에 일어나므로, 서버는 정상 기동한 뒤 **첫 접속에서** 죽는다. 헬스체크만으로는 드러나지 않는다 |
| Builder 명세 / 설계 문서 | **직접 영향.** "→ 502"가 틀린 채로 유통돼 있다 |

### 특이사항 — 수정 위치가 이 작업 단위 밖이다

**[코드 확인]** 결함은 **SDK(`backend/src/sdk/worker.ts`)**에 있다. #6은 게이트웨이 작업이고, SDK는 스모크 24건이 지키는 완성된 계층이라 **이번 단위에서 손대지 않기로 한 영역**이다. 지금까지의 이슈가 전부 `영역: gateway`였던 것과 이 점이 다르다.

**[코드 확인]** 그리고 **테스트로 잡을 수 없다** — 재현하면 스모크 프로세스 자체가 죽어 나머지 케이스가 실행되지 않는다. Tester가 케이스로 넣지 않고 `backend/src/server/smoke.ts:1518-1522`에 `note`로만 남긴 이유가 그것이다:

> `'exe 경로가 존재하지 않는 경우'` — "ENOENT는 child_process가 error 이벤트로 알리는데 sdk/worker.ts에 그 리스너가 없어 **프로세스가 죽는다**(502로 오지 않는다). 스모크를 통째로 죽이므로 케이스로 넣지 않았다"

**[코드 확인]** 바로 위(`backend/src/server/smoke.ts:1512-1515`)에 **502가 실제로 나가는 케이스는 존재한다** — `exePath`를 `process.execPath`(= node 자신)로 주어 **spawn은 성공하되 프로토콜이 성립하지 않게** 만든 경우다. 즉 502 경로 자체는 살아 있고, **spawn이 아예 실패하는 갈래만 뚫려 있다.**

### 검토된 해결 방향

**아직 없다.** 판단이 필요한 갈래만 적어 둔다.

1. **SDK를 고친다** — `spawn()`에 `child.on('error')`를 달아 `WorkerError`로 바꿔 `acquire()`가 reject되게 한다. 명세대로 502가 나간다. **[추론]** 고려할 점: 이번 단위에서 안 건드리기로 한 계층이고, 기존 스모크 24건에 회귀가 없는지 확인해야 한다.
2. **게이트웨이가 사전 확인한다** — 기동 시 exe 존재를 검사해 아예 뜨지 않게 하거나 경고한다. **[추론]** 고려할 점: SDK의 구멍은 그대로 남는다. 경로가 존재하지만 실행할 수 없는 경우(권한, 손상)는 여전히 프로세스를 죽인다.
3. **둘 다 한다.**

**[추론]** 어느 쪽을 택하든 **테스트 전략이 따라온다** — 1안을 넣으면 그때는 자식 프로세스로 격리해 재현하는 케이스를 쓸 수 있게 되므로, `backend/src/server/smoke.ts:1518`의 `note`를 정식 케이스로 승격할지 함께 정해야 한다.

<a id="issue-006-결정"></a>

### 결정

**1안 채택 — SDK를 고친다. `backend/src/sdk/worker.ts`의 `spawn` 경로에 `child.on('error')`를 달고, 종료 처리를 `#end()` 한 곳으로 모아 멱등화한다.** 커밋 `f077d0f`으로 반영 완료.

> **[코드 확인]** 위 "원인"·"특이사항" 절의 행 번호는 **커밋되지 않은 작업 트리** 기준이었다. 그 상태가 이제 `f077d0f`로 커밋됐으므로, 아래에는 **현재 위치**를 병기한다. 발견 당시 기록은 그대로 둔다.

**반영 내용** **[코드 확인]** (`backend/src/sdk/worker.ts`를 읽어 확인):

- `child.on('error')`를 달았다(`backend/src/sdk/worker.ts:87-94`). 로그에 exe 경로를 붙이고(`워커를 시작하지 못했습니다: spawn <경로> ENOENT (exe=<경로>)`) `#end(null, …)`를 부른다.
- `#end()`(`backend/src/sdk/worker.ts:108-123`)가 `#closed = true`로 만들고 대기 요청을 전부 reject한 뒤 **`emit('exit', null)`** 한다. `'error'`와 `'exit'`은 둘 다 올 수도 한쪽만 올 수도 있어서(**ENOENT는 `'exit'`이 오지 않는다**) 먼저 온 쪽만 통과시킨다. 그래서 `'exit'`이 정확히 한 번만 나간다.
- **ENOENT에서는 이 `emit`이 유일한 탈출구다.** `Session.create()`가 `ready`/`exit`를 경주시키므로 이게 reject를 깨우고, `SessionManager`가 잡아 **502**로 응답한다(`backend/src/server/sessions.ts:454-457`).
- **기존 `WorkerError`를 그대로 쓴다. 새 타입 없음.** `#end(code, reason: () => WorkerError)`로 **팩토리**를 받아, 대기 요청이 없을 때(기동 실패는 항상 이 경우다) 에러 객체를 만들지 않는다.
- **타이밍이 안전한 근거** **[코드 확인]** (`backend/src/sdk/worker.ts:84-86`의 주석): `ChildProcess.spawn`이 실패를 `process.nextTick`으로 예약하는 반면 `Session.create`는 `Worker.spawn()` 직후 **동기 구간**에서 리스너를 단다. 항상 리스너가 먼저다.

#### SDK 계층을 건드린 것에 대해

발견 당시 "특이사항 — 수정 위치가 이 작업 단위 밖이다"에 적어 둔 제약(#6은 게이트웨이 작업이고 `backend/src/sdk/`는 건드리지 않는다)에 답을 붙인다. **사용자가 이번 수정을 승인했다.** 근거는 둘이다:

- **[추론]** "spawn 실패를 에러로 승격"은 SDK가 원래 해야 할 일이다. 호스트 프로세스를 죽이지 않는 것은 SDK 자신의 계약이다.
- **[추론]** 2안(게이트웨이가 exe 존재를 사전 확인)은 구멍을 좁힐 뿐 **다른 spawn 실패(권한, 손상, fd 고갈)를 막지 못한다.** 발견 당시 2안에 붙여 둔 대가가 그대로 남는다.

**[코드 확인]** `worker.ts`의 `spawn` 경로만 고쳤고 다른 파일은 손대지 않았다. SDK 스모크 회귀 없음.

**결과** **[실측]**

```
없는 exe 연결 → 502 {"error":"워커를 시작하지 못했습니다: 워커가 시작 중 종료됨 (code=null)"}
게이트웨이 생존 → 같은 pid로 health 200
같은 풀로 두 번째 시도 → 똑같은 502, stats.total === 0 (실패가 풀에 상태를 남기지 않음)
별도 실제 풀로 연결 → open, op 왕복, close, busy 0
```

#### 회귀 방지 — 이전엔 테스트로 잡을 수 없었다

발견 당시에는 재현하면 스모크 프로세스가 죽어서 `backend/src/server/smoke.ts:1518-1522`의 `note`로만 남아 있었다. **이제 정식 테스트로 승격됐다.**

**[코드 확인] 게이트웨이 스모크** — §7-2b `'exe 없음 → 502, 게이트웨이 생존 (ISSUE-006)'`(`backend/src/server/smoke.ts:1566-1683`, 10건). 502 직후 `GET /api/health`를 치고 **`body.pid === process.pid`**를 확인한다(`:1616`). **[추론]** 200만 보면 "죽었다 되살아난" 경우와 구분이 안 되므로 같은 pid여야 한다. 스모크 프로세스 = 게이트웨이 프로세스이므로 이 한 줄이 "unhandled 승격이 없다"를 증명한다.

**[코드 확인] SDK 스모크** — §0 `'기동 실패는 세션 하나의 실패다 (ISSUE-006)'` 신설(`backend/src/sdk/smoke.ts:43-102`, 7건). 층별로 나눠 회귀 시 어느 층이 깨졌는지 지목되게 했다:

| 층 | 단언 |
|---|---|
| Worker | `'exit'` 정확히 1회 / `code === null` / `closed === true` / 로그에 exe 경로 |
| Session | `create()`가 매달리지 않고 reject |
| Pool | `acquire()` reject + `stats.total === 0` |
| (프로세스) | **마지막 줄에 도달한 것 자체**가 "SDK 사용자 프로세스를 죽이지 않는다"의 단언 |

**[추론]** SDK에도 넣은 이유: 수정이 일어난 층이 SDK이고 게이트웨이 없이도 독립적으로 쓰이는 계층이다. 게이트웨이가 풀 배선을 바꾸면 502를 통한 간접 그물은 조용히 사라진다. 비용은 **+150ms**에 불과했다 — 없는 exe는 워커를 안 띄우기 때문이다.

**전제 재확인** **[실측]**: Node v24.15.0에서 `'error'` 리스너 없이 없는 exe를 spawn하면 여전히 `Unhandled 'error' event`로 프로세스가 exit 1로 죽는다. ENOENT에서 `'exit'`은 오지 않고, `child.stdout`/`stderr`는 non-null이라 `Worker.spawn`이 동기 throw 하지도 않는다 — **`#end()`가 유일한 탈출구**라는 설계가 성립한다.

#### 남은 한계 — 502 본문에 exe 경로가 없다

**[코드 확인]** 502 본문은 세션 계층의 문구가 그대로 나가고(`워커를 시작하지 못했습니다: 워커가 시작 중 종료됨 (code=null)`) **경로가 없다.** 경로는 게이트웨이 로그에만 있다(`backend/src/sdk/worker.ts:90`이 만드는 `... (exe=<경로>)`).

**[추론]** 본문 문구를 바꾸려면 `Session.create()`를 손대거나 `'exit'`의 `code` 인자에 문자열을 실어 타입을 어겨야 해서, **경로는 로그로 상태코드는 HTTP로** 나누는 쪽을 택했다. 서버 경로가 클라이언트 응답 본문으로 새지 않는 부수효과도 있다.

**[코드 확인]** Tester가 이 한계도 계약으로 고정했다 — `'실패한 exe 경로가 게이트웨이 로그에 남는다'`(`backend/src/server/smoke.ts:1631-1637`). 로그마저 없으면 운영에서 "어느 exe가 없었나"를 영영 못 찾는다.

#### 문서 상태 — 정정할 곳이 없다

**[코드 확인]** 발견 당시 영향 범위 표에 "Builder 명세 / 설계 문서 — `→ 502`가 틀린 채로 유통돼 있다"고 적었다. 확인해 보니 **`DESIGN.md`에는 `502`도 "워커 기동 실패"도 등장하지 않는다**(전체 검색). 틀린 채로 유통된 것은 Builder가 구두로 낸 명세뿐이며, 그 명세는 이제 **사실이 됐다.** 고칠 문서가 없다.

---

<a id="issue-007"></a>

## ISSUE-007 — 서버 스모크가 간헐적으로 프로세스째 죽는다 (Windows fail-fast `0xC0000409`)

- **상태**: 열림
- **심각도**: **신뢰** — 제품 코드에 결함이 없다. 망가지는 것은 **스모크 결과의 의미**다. 등급을 새로 만든 이유는 [이슈 목록](#이슈-목록)의 범례 아래 문단에 적어 뒀다.
- **발견**: 2026-08-06, TASKS.json #8 "이벤트 역방향 중계"(커밋 `a24979f`)의 Tester 검증 중. **#8의 통과 기준 자체는 PASS이며, 아래 교차 검증대로 #8과 무관한 기존 문제다.**
- **영역**: **개발환경** — `backend/package.json:14`(`smoke:server` 실행 방식), Node v24.15.0, Windows 11. **지금까지의 이슈와 달리 `backend/src/` 아래에 대상 파일이 없다.** 고칠 후보가 우리 코드가 아니라 **테스트를 어떻게 실행하는가**이기 때문이다.

> **[코드 확인]** 아래 섹션 번호(§2·§3·§5·7-2b·7-4)는 커밋 `a24979f` 기준 `backend/src/server/smoke.ts`의 것이다. 각각 `:887` 수명 관리 / `:948` 라우팅 계약 / `:1117` 에러 상태코드 / `:1899` exe 없음 → 502 (ISSUE-006) / `:2177` 하트비트. 행 번호는 단위마다 밀리므로 어긋나면 이 제목으로 찾을 것.

### 증상

**[실측]** `npm run smoke:server`가 **출력 도중 아무 스택도 없이 끊기고** 프로세스가 종료된다.

- 종료 코드 **`-1073740791`** = `0xC0000409` = `STATUS_STACK_BUFFER_OVERRUN`. 스택 손상을 감지했을 때 Windows가 프로세스를 즉시 죽이는 **fail-fast**이며, Node 레벨에서 잡히지 않는다.
- **죽는 위치가 매번 다르다.** 관측된 곳: §2(수명 관리), §3(라우팅 계약), §5(에러 상태코드), 7-2b(exe 없음 → 502), 7-4(하트비트).
- **워커나 WebSocket과 무관한 섹션에서도 난다.** §2·§3은 HTTP만 쓴다. 즉 원인을 "자식 프로세스" 쪽으로 좁힐 근거가 없다.
- **실패 단언이 아니라 프로세스 자체가 사라진다.** 어느 테스트가 문제인지 지목되지 않고, 그 지점까지의 출력만 남는다.

### #8이 원인이 아니라는 근거

**[실측]** Tester가 현재 `smoke.ts`와 HEAD(`a24979f` 이전) baseline `smoke.ts`를 **12회씩 번갈아** 실행해 교차 검증했다.

| | 완주 |
|---|---|
| 현재 (#8 포함) | 11/12 |
| baseline (#8 이전) | 10/12 |

**발생률이 같다. #8의 변경이 원인이 아니다.** 메인 세션도 커밋 직전 검증에서 **5회 중 1회** 재현했고(§2에서 사망), 나머지 4회는 283/283 통과였다.

### 원인

**확정되지 않았다.** **[추론]** `0xC0000409`는 스택 손상 감지 시 Windows가 프로세스를 즉시 종료하는 것이라 예외 핸들러도 `process.on('uncaughtException')`도 지나지 않는다. **우리 코드에 이 종료 코드를 만들 수단이 없다** — JS 레벨에서 도달할 수 있는 실패가 아니다.

후보는 셋이고 **어느 것도 확인되지 않았다.**

1. Node의 `--experimental-strip-types` — **[코드 확인]** 이 프로젝트는 TypeScript를 트랜스파일 없이 `node --experimental-strip-types`로 직접 실행한다(`backend/package.json:13-15`, `smoke`·`smoke:server`·`serve` 셋 다).
2. 백신·보안 소프트웨어의 프로세스 후킹
3. 그 밖의 환경 요인

**[추론]** 재현이 무작위(≈10%)라 이분 탐색이 어렵다. 한 조건을 배제하려면 같은 조건에서 수십 회를 돌려야 "안 났다"가 의미를 갖는다.

### 영향 범위

**[추론]** **CI에 걸면 약 10% 확률로 원인 없이 빨간불이 된다.** 실패 단언이 없으므로 "무엇이 깨졌나"를 알 수 없고, 재실행하면 통과한다 — **팀이 빨간불을 무시하기 시작하는 전형적인 경로다.** 그 습관이 붙은 뒤에는 진짜 회귀가 났을 때도 "또 그거겠지"로 넘어간다. 이 이슈의 실제 손해는 크래시 자체가 아니라 여기에 있다.

| 대상 | 영향 |
|---|---|
| CI 도입 | **직접 영향.** 붙이기 전에 결정해야 한다. 붙인 뒤에 결정하면 이미 빨간불 무시가 시작된 뒤다 |
| 현재 개발 흐름 | **[추론]** 사람이 재실행해서 넘어가고 있다. 진행을 막지는 않는다 |
| 노출 시간 | **[추론]** 작업이 진행될수록 스모크가 길어진다(현재 283건, 6.5초). 같은 원인이라도 걸릴 확률은 늘어난다 |
| SDK 스모크(`npm run smoke`, 31건) | **[실측]** 아직 관측되지 않았다. 실행 시간이 짧아서인지 다른 이유인지는 **미확인** |
| 제품 코드 | **영향 없음.** 게이트웨이·SDK의 동작에 대해 이 이슈가 말해 주는 것은 없다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **`--experimental-strip-types` 대신 빌드 후 실행으로 바꿔 원인을 가른다.** `tsc` → `node dist/…`. **[코드 확인]** `backend/package.json:11`에 `build` 스크립트가 이미 있다.
   - **[추론]** 고려할 점: 원인이 여기면 증상이 사라지고, 아니면 후보가 하나 줄어든다 — **어느 쪽이든 정보가 남는 유일한 안이다.** 대신 빌드 단계가 끼어 개발 왕복이 느려지고, 판정하려면 앞서 말한 대로 수십 회 반복이 필요하다.
2. **재시도 래퍼** — 크래시 종료 코드(`-1073740791`)일 때만 1회 재실행한다.
   - **[추론]** 고려할 점: **증상을 덮는 것이라 원인 규명이 늦어진다.** 게다가 "크래시일 때만"이라는 조건이 정확해야 하는데, 진짜 회귀가 우연히 같은 코드로 끝나면 조용히 감춰진다.
3. **Node 버전을 바꿔 재현률을 본다** (현재 v24.15.0).
   - **[추론]** 고려할 점: 1안과 마찬가지로 반복 실행이 필요하고, 버전을 바꾸면 `--experimental-strip-types`의 동작 자체도 함께 바뀌므로 **두 변수가 섞인다.** 1안 뒤에 하는 편이 읽기 쉽다.
4. **그대로 두고 사람이 재실행한다** — 현재 상태.
   - **[추론]** 고려할 점: CI가 없는 동안만 성립한다.

### 결정

**미결. 지금 진행을 막지는 않는다(재실행하면 통과). 다만 CI를 붙이기 전에는 결정해야 한다.**

**[추론]** 이 이슈를 지금 파고들지 않는 이유는 우선순위가 아니라 **비용 구조**다 — 재현이 무작위라 어떤 안을 시도해도 "고쳐졌다"를 말하려면 수십 회 반복이 필요하고, 그 시간에 얻는 것은 제품 기능이 아니다. 반대로 CI를 붙이는 순간에는 이 판단이 뒤집힌다. 그때 손해가 "가끔 다시 돌린다"에서 "빨간불을 안 믿게 된다"로 바뀌기 때문이다.

**[추론]** 그때까지 지켜야 할 것 하나: **스모크가 중간에 끊긴 실행을 "통과"로 세지 말 것.** 출력이 도중에 끝나고 종료 코드가 `-1073740791`이면 그 회차는 판정이 없는 것이지 PASS가 아니다.

**[실측] 2026-08-07 추가 관측** — TASKS #9·#13 검증 중 **10회 + 6회, 총 16회에서 0회 재현됐다.** #9가 실제 워커를 하나 더 띄우는 테스트를 추가했고(스모크 283→310건, 6.7→9.8초) 그럼에도 나지 않았다. 발견 당시 관측치(12회 중 1~2회)와 비교하면 **빈도가 낮아졌거나 조건이 달라졌다는 뜻인데, 어느 쪽인지 모른다.** 위 §원인의 후보 셋 중 어느 것도 배제되지 않았다 — 무작위 재현이라 "16회 무사고"는 발생률이 6% 아래라는 것 이상을 말하지 못한다. **이 관측을 "고쳐졌다"로 읽지 말 것.** 상태를 `열림`으로 유지하는 이유가 이것이다.

---

<a id="issue-008"></a>

## ISSUE-008 — 백프레셔의 두 경로가 테스트로 증명되지 않는다

- **상태**: 열림
- **심각도**: **진단** — 코드는 의도대로 동작할 가능성이 높다. 망가지는 것은 **회귀를 잡을 능력**이다. [ISSUE-004](#issue-004)와 정확히 같은 성격이며(검증 공백), 그 선례를 따라 분류했다.
- **발견**: 2026-08-07, TASKS.json #9 "백프레셔 latest-wins"(커밋 `cc2b788`)의 Tester 검증 중. **#9의 통과 기준은 PASS다** — 스모크 283→310건 전부 통과, 돌연변이 4건이 각각 빨간불이 되는 것까지 확인됐다. 이것은 결함 보고가 아니라 **그 27건이 덮지 못한 자리**의 기록이다.
- **영역**: gateway — `backend/src/server/smoke.ts` (대상 코드는 `backend/src/server/sessions.ts`)

> **[코드 확인]** 행 번호는 커밋 `cc2b788` 기준이다. 어긋나면 심볼로 찾을 것 — `SessionManager`의 **`#detach`**, **`#drainFrame`**, 스모크의 **§9-1**·**§9-3**.

### 증상

**두 자리가 비어 있다. 성격이 서로 다르다.**

**① `#detach`가 보류 프레임을 비우는 것에 테스트가 없다.**

**[코드 확인]** `backend/src/server/sessions.ts:800-807`이 연결 정리 시 `conn.pendingFrame`을 비운다. 이 코드에 닿는 단언이 하나도 없다.

**② 실제 워커 시나리오(§9-3)의 "최신 프레임이 도착한다" 단언에 이빨이 없다.**

**[실측]** Tester가 돌연변이로 확인했다 — `#drainFrame`(`backend/src/server/sessions.ts:1037-1062`)을 즉시 `return`하게 만들었을 때 **가짜 세션 테스트(§9-1)는 4건이 빨간불이 됐는데 실제 워커 테스트(§9-3)는 그대로 통과했다.**

### 원인

**① 관측 창이 닫혀 있다.** **[코드 확인]** `pendingFrame`을 밖에서 볼 수 있는 유일한 표면은 `gw.sessions.connections`인데, 이 게터는 **연결이 살아 있는 동안만** 원소를 돌려준다(`#detach`가 `this.#conns.delete(conn.ws)`를 먼저 한다, `:679`). `#detach`가 끝난 뒤의 최종 상태는 종료 로그 문자열에만 남는다. **즉 "닫힐 때 비웠는가"를 물어볼 시점에 물어볼 대상이 이미 사라져 있다.**

**② 시뮬이 계속 돌아 드레인 없이도 최신 프레임이 도착한다.** **[추론]** §9-3은 클라이언트 소켓을 `ws.pause()`로 막았다가 재개한다. 재개 시점에도 워커는 여전히 프레임을 생산 중이므로, 보류 칸을 비우는 `#drainFrame`이 죽어 있어도 **다음 새 프레임이 그냥 `#send`로 나가서** "마지막에 도착한 것이 최신"이 성립해 버린다. 드레인 경로를 밟게 하려면 "느려졌다 회복했는데 **시뮬이 멈춰 있는**" 조합(소켓 pause → 워커 pause → 소켓 resume)이 필요한데, §9-3은 그렇게 짜여 있지 않다.

### 영향 범위

**[추론]** 두 자리 모두 **현재 동작은 정상일 가능성이 높다** — Builder가 의도적으로 넣었고 가짜 세션 테스트가 드레인 자체는 증명한다. 손해는 나중에 온다.

| 대상 | 영향 |
|---|---|
| 회귀 감지 | **직접 영향.** `#detach`의 보류 프레임 비우기를 "불필요해 보인다"며 지워도 310건이 전부 통과한다 |
| §9-3의 신뢰도 | **[실측]** "실제 워커로 검증했다"는 문장이 실제로 덮는 범위가 생각보다 좁다. 드레인 몫은 **가짜 세션 테스트(§9-1)가 전부 지고 있다** |
| 현재 동작 | **[추론]** 영향 없음 |

**[추론]** ①이 특히 조용하다. 보류 프레임 하나(47.8KB)가 닫힌 연결의 `conn`에 매달려 남는 것이라, 새면 **누수는 나는데 어떤 단언도 깨지지 않는다.**

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **①에 대해 — `#detach` 직후의 최종 카운터를 관측 가능하게 만든다.** 종료된 연결의 요약(`droppedFrames`·`pendingFrame` 최종값)을 `SessionManager`가 짧게 보관하거나, 종료 로그를 파싱한다.
   - **[추론]** 고려할 점: 로그 파싱은 문구가 바뀌면 조용히 깨진다. 보관은 표면이 하나 늘어난다. **[ISSUE-004](#issue-004)의 1안(응답 헤더로 간접 확인)과 같은 종류의 절충**이 필요하다.
2. **②에 대해 — §9-3에 "시뮬을 멈춘 뒤 소켓을 재개하는" 조합을 추가한다.** 드레인 경로를 실제 워커에서도 밟게 한다.
   - **[추론]** 고려할 점: 실제 워커 테스트가 하나 더 늘어 [ISSUE-007](#issue-007)의 노출이 커진다. 얻는 것은 "가짜 세션에서만 증명된 것"을 실물로 옮기는 것뿐이라, 비용 대비 효과를 따져야 한다.
3. **그대로 두고 이 문서에만 남긴다.** 드레인 자체는 §9-1이 증명하고 있다.
   - **[추론]** 고려할 점: `#detach`의 비우기(①)는 **어느 테스트도 덮지 않는다**는 사실이 남는다.

### 결정

**미결.** **[추론]** 지금 조치하지 않는 이유는 [ISSUE-004](#issue-004)와 같다 — 버그가 아니라 증명 공백이고, 코드는 의도대로 돌고 있다. 다만 기록하는 이유도 같다: **`#detach`의 보류 프레임 비우기는 왜 있는지 모르면 지우고 싶어지는 세 줄**이고, 지웠을 때 스모크가 막아 주지 못한다. `#emit`/`#drainFrame`/`#detach` 근처를 건드리는 작업이 생기면 그때 1안을 함께 넣는다.

---

<a id="issue-009"></a>

## ISSUE-009 — 재생 중 씬을 다시 로드하면 시뮬은 멈추는데 버튼은 "정지"로 남는다

- **상태**: **닫힘** — 2026-08-09, 커밋 `3df0621` "feat(frontend): 재생 컨트롤 — 화면이 워커에 대해 거짓말하지 않는다"로 반영. 아래 [결정](#issue-009-결정) 참고.
- **심각도**: **진단** — 요청은 전부 올바르게 처리된다(로드 성공, 새 씬 표시 정상). 사용자가 **왜 옷이 멈췄는지 알 수 없고** 화면 어디에도 단서가 없다는 것이 문제다.
- **발견**: 2026-08-07, TASKS.json #13 "프레임 스트리밍"(커밋 `0bb785e`) 검증 중 **브라우저에서 실측.** #13은 `verify: manual`이고 **통과 기준("옷이 움직인다")은 PASS다.** 이것은 기준 밖에서 잡힌 별건이다.
- **영역**: frontend — `frontend/src/main.ts`

> **[코드 확인]** 행 번호는 커밋 `0bb785e` 기준이다. 어긋나면 심볼로 찾을 것 — `main.ts`의 **`show()`**, **`setPlaying()`**, 모듈 스코프 변수 **`playing`**.

### 증상

**[실측]** 재생 중(`▶ 재생` → `⏸ 정지` 상태)에 씬 선택을 바꾸고 `로드`를 누르면:

| | 값 |
|---|---|
| 새 씬 로드 | **정상** — 패턴 24 → 5, 정점 13,398 → 3,022, `로드 완료 (939ms)` |
| 프레임 수신 | **636에서 완전히 정지.** `fps: 0` |
| 버튼 글자 | **`⏸ 정지`** — 즉 UI는 여전히 "재생 중"이라고 말한다 |
| `stalled` / `mismatched` / `failed` | 전부 0 — 스트림 자체는 건강하다 |

**[실측]** 복구는 된다. **버튼을 두 번** 눌러야 한다 — 한 번(정지, 실제로는 이미 멈춰 있음) → 다시(재생) → 38.6fps로 재개. 사용자 입장에서는 "정지를 눌렀는데 이미 멈춰 있었고, 재생을 누르니 돌아왔다"는 납득하기 어려운 절차다.

### 원인

**[코드 확인]** `show()`가 `playing`을 건드리지 않는다. `frontend/src/main.ts`의 모듈 스코프 `playing`(`:102`)은 `setPlaying()`과 `open`/`close` 핸들러(`:117`, `:126`)에서만 갱신되고, **씬 로드 경로에는 그 갱신이 없다.**

**[추론]** 반면 워커 쪽에서는 `load` op이 시뮬 상태를 초기화한다. 실측에서 프레임이 정확히 멈춘 것이 그 증거다 — 워커는 `maxFrame`이 바뀔 때만 frame 이벤트를 내는데(`backend/native/src/protocol.cpp:411-433`), 로드 후 시뮬이 정지 상태이므로 `maxFrame`이 더 이상 오르지 않는다.

**[추론]** 즉 **"우리가 믿는 재생 상태"와 "워커의 실제 시뮬 상태"가 갈라진다.** `playing` 변수 주석(`:101`)이 스스로 "시뮬이 돌고 있다고 **우리가 믿는** 상태"라고 적고 있는데, 그 믿음을 깨뜨리는 경로가 하나 빠져 있는 셈이다. 재연결 경로(`:117`)는 같은 문제를 이미 인지하고 `playing = false`로 되돌리고 있다 — **로드 경로에만 그 처리가 없다.**

### 영향 범위

**[추론]** 씬을 여러 개 올려 놓고 갈아 가며 보는 흐름에서 **매번** 만난다. 지금은 씬이 둘뿐이라 드물지만, 사용자가 `.zls`를 업로드해 비교하는 것이 이 제품의 기본 사용법이므로 노출은 계속 커진다.

| 대상 | 영향 |
|---|---|
| TASKS #14 (재생 컨트롤) | **직접 영향.** 재생 상태를 정식으로 다루는 단위다. **이 문제는 #14에서 함께 처리하는 것이 자연스럽다** |
| 업로드 직후 흐름 | **[코드 확인]** `upload` 핸들러(`:205`)도 `show()`를 부른다 — 같은 경로다 |
| 재연결 | **영향 없음.** `open` 핸들러가 이미 `playing = false`로 되돌린다 |
| 자동 테스트 | **[코드 확인]** 덮지 않는다. 재생 상태는 `main.ts`에 있고 프론트 스모크(215건)는 DOM 없는 모듈만 돈다 |

**[추론]** 증상이 "옷이 안 움직인다"인데 원인이 "버튼 상태가 거짓말"이라 **[ISSUE-003](#issue-003)과 같은 계열이다 — 사용자가 받는 신호가 진짜 원인을 지목하지 못한다.**

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **`show()`가 성공하면 `playing = false`로 되돌리고 `paintPlay()`한다.** 재연결 핸들러(`:117`)와 같은 처리를 로드 경로에도 넣는 것이라 일관성이 생긴다.
   - **[추론]** 고려할 점: 가장 짧다. 다만 "로드했으니 정지 상태"가 사용자 기대와 맞는지는 별개다 — 씬을 바꾸면 자동으로 이어서 돌기를 기대할 수도 있다.
2. **`show()` 뒤에 자동으로 다시 `start()`를 보낸다.** 재생 중이었다면 새 씬에서도 계속 돌게 한다.
   - **[추론]** 고려할 점: 103MB 씬을 로드하자마자 시뮬이 도는 것이 항상 바람직하진 않다. 그리고 로드 실패 시 무엇을 할지 갈래가 하나 는다.
3. **워커의 실제 상태를 물어 UI를 맞춘다.** `status` op이 이미 있다.
   - **[추론]** 고려할 점: 믿음을 없애고 사실을 쓰는 정공법이지만, 폴링 주기를 정해야 하고 왕복이 는다. **#14가 재생 컨트롤을 제대로 만들 때 검토할 안이다.**

<a id="issue-009-결정"></a>

### 결정

**1안 + 3안 채택. 2안(자동 재시작)은 기각.** 커밋 `3df0621`으로 반영 완료. 발견 당시 결정은 "미결. TASKS #14 '재생 컨트롤'에서 함께 처리한다"였고, 그 #14가 이 이슈를 통과 기준의 일부로 삼았다.

> **[코드 확인]** 위 "증상"·"원인" 절의 행 번호는 커밋 `0bb785e` 기준이다. `3df0621`이 `main.ts`를 크게 고쳤고 재생 상태가 `frontend/src/panels/playback.ts`로 옮겨 갔으므로, 발견 당시 지목한 모듈 스코프 `playing`(`:102`)은 **더 이상 존재하지 않는다.** 기록은 사후에 고쳐 쓰지 않고 그대로 둔다.

**반영 내용** **[코드 확인]**

- **1안 — 다만 "성공하면"이 아니라 "시작하면"이다.** `show()`가 로드를 **시작하는 시점에** 믿음을 내린다(`PlaybackController.sceneLoading()`, `frontend/src/panels/playback.ts`). 발견 당시 1안은 "`show()`가 **성공하면** `playing = false`"였는데, 103MB 씬은 로드에 1초가 걸리고 **그 1초 내내 버튼이 `⏸ 정지`로 남는다.** 성공을 기다리면 이 이슈는 고쳐지는 게 아니라 창만 좁아진다.
- **3안 — 폴링이 아니다.** op·로드·재연결이 **끝나는 자리마다** `status`로 워커 사실을 받아 믿음을 덮는다(`#run()`의 `syncFromWorker()`, `frontend/src/panels/playback.ts:586`). 발견 당시 3안에 붙여 둔 대가("폴링 주기를 정해야 하고 왕복이 는다")는 **성립하지 않았다** — 워커의 시뮬 상태를 바꾸는 사건이 전부 우리 손을 지나므로, 주기가 아니라 그 사건의 끝에서만 물으면 된다.
- 갈라졌던 횟수가 **계측된다.** `stats.corrections`가 "믿음과 사실이 달라 덮은 횟수"다. 이 이슈가 다시 자라면 0이 아닌 값으로 먼저 보인다.
- 상태를 필드로 들지 않고 **파생**시켰다 — `disconnected / noScene / loading / paused / playing`을 `connected`·`scene`·`loading`·`playing`에서 계산한다. **필드로 두면 "끊겼는데 playing"이 표현 가능해지고, 그것이 정확히 이 이슈의 형태다.**

#### 3안을 함께 넣은 이유 — 이 판단 근거를 반드시 보존한다

**[추론]** 1안만 넣으면 화면이 **"로드하면 워커가 멈춘다"를 외워 두는 것**이 된다. 그 암기는 지금의 워커에 대해서만 참이고, 워커가 달라지는 날 거짓말이 소리 없이 돌아온다. 3안은 외우는 대신 **묻는다.**

**[코드 확인]** 그리고 그 "워커가 달라지는 날"은 가정이 아니다 — **워커의 `load`는 애니메이션 모드를 초기화하지 않는다.** `backend/native/src/protocol.cpp`의 `load` 갈래(`:550-557`)는 `maxFrame`·`curFrame`을 -1로 되돌리고 `LoadZls`만 부르며, `SetAnimationMode`를 **부르지 않는다**(`start`/`pause`/`reset`/`step`은 전부 부른다, `:567-588`). 즉 로드 전 모드가 그대로 남을 수 있고, "로드 후에는 정지"라는 1안의 전제는 **워커가 보장해 주는 사실이 아니다.** 3안이 필요한 근거가 이것이다.

#### 2안(자동 재시작)을 기각한 이유

**[추론]** 사용자가 지시하지 않은 상태 변경이다. 씬을 바꾸면 이어서 돌기를 기대할 수도 있다는 발견 당시의 지적은 여전히 맞지만, 그건 **"재생을 눌렀다"와 다른 사건**이고 화면이 그 둘을 구분하지 못하게 된다. 실무적으로도 갈래가 는다 — 로드가 실패했을 때 자동 start를 보낼지 말지가 새 분기가 되고, 그 분기의 실패는 다시 "왜 도는지/왜 안 도는지 모르겠다"로 나타난다. 이 이슈가 없애려던 바로 그 종류다.

**검증** **[실측]** — 발견 당시 증상 표와 같은 조작을 그대로 밟았다.

| | 값 |
|---|---|
| 재생 중 [로드] 누른 직후 | 버튼 **`▶ 재생`**, 상태 `loading` — 로드가 **끝나기 전에** 이미 사실을 말한다 |
| 로드 완료 후 | 워커가 재생으로 돌아가지 않고 화면도 정지로 남는다 (둘이 같은 말을 한다) |
| 그 경로 내내 `corrections` | **0** — 믿음이 사실과 한 번도 갈라지지 않았다 |
| 복구 절차 | 버튼 **한 번**. 발견 당시의 "두 번 눌러야 한다"가 사라졌다 |

**[실측] 돌연변이가 판정이 살아 있음을 보인다.** M1(`sceneLoading()`이 믿음을 내리지 않게 함) 2건, M6(`main.ts`의 배선 한 줄 제거) **UI 하네스만 6건.** **M6가 중요하다** — 배선을 지우면 스모크는 **464건 전부 통과**하고 UI 하네스에서만 빨간불이 된다. **배선은 화면에서만 보인다.** 상태 기계를 Node로 옮겨 놓았다고 해서 화면 검증이 대체되지 않는다는 뜻이고, 이 단위가 스모크와 UI 하네스에 "재생 중 재로드" 하나를 일부러 겹쳐 둔 이유가 그것이다.

**함께 닫은 것 — 상태줄의 같은 계열 거짓말.** **[코드 확인]** 스냅샷에서 실시간으로 돌아와도 상태줄이 `스냅샷 표시 중`으로 남아 있었다. 화면과 글자가 서로 다른 것을 말하는, 이 이슈와 같은 형태다. `setMode()`가 전환 때마다 상태줄을 함께 바꾼다(`frontend/src/main.ts:427-439`). 그 문구를 **시뮬 상태로 채우지 않은 것도 같은 이유**다 — 그 값은 곧 낡는다. 상태줄은 "무엇을 보고 있는가"만 말하고, 시뮬의 지금 상태는 `#sim`이 4/s로 갱신한다.

**[추론]** 남은 노출 하나: 이 이슈를 만든 구조(믿음과 사실의 분리) 자체는 사라지지 않았다. 사라진 것은 **믿음이 갱신되지 않는 경로**다. 새 op을 프로토콜에 추가하면서 `#run()`을 지나지 않는 길을 내면 같은 구멍이 다시 열린다 — `syncFromWorker()`가 그 길에도 붙는지 확인할 것.

---

<a id="issue-010"></a>

## ISSUE-010 — UI 로그가 `engineMessage` 40/s에 밀려 7초면 의미 있는 줄이 사라진다

- **상태**: 열림
- **심각도**: **진단** — 동작에는 문제가 없다. **[ISSUE-008](#issue-008)이 "테스트가 못 본다"라면 이쪽은 "사람이 못 본다"다.**
- **발견**: 2026-08-07, TASKS.json #13 검증 중 브라우저에서 실측. 구독이 정말 한 번만 켜지는지 확인하려고 로그에서 `구독 켜짐`을 찾다가 **0건**이 나와서 드러났다.
- **영역**: frontend — `frontend/src/main.ts`

### 증상

**[실측]** 시뮬 재생 중 로그 패널(`#log`)을 읽으면 **300줄이 전부 `[엔진] NN.N sec, avatar animation (done)`이다.** 같은 초(`오전 11:56:08`)에 16줄이 들어간 구간도 있었다.

로그에 남아야 할 줄들이 전부 밀려나 있었다:
- `구독 켜짐 — 프레임당 약 48KB 가 흐르기 시작합니다` — **0건**
- 로드·업로드·재연결·진단 메시지 — 전부 소실

**[코드 확인]** 구독이 실제로 한 번만 켜지는 것은 코드로 확인했다(`frontend/src/main.ts:297`의 `if (!subscribed)` 가드). **로그로는 확인할 수 없었다** — 증거가 이미 밀려난 뒤였다.

### 원인

**[코드 확인]** 링버퍼 상한과 유입 속도가 세 자릿수 차이다.

1. `frontend/src/main.ts:48` — `LOG_LIMIT = 300`
2. `frontend/src/main.ts:50-55` — `log()`가 넘치면 앞에서 잘라낸다
3. `frontend/src/main.ts:109` — `client.on('engineMessage', ({ message: m }) => log(...))` — **필터가 없다. 오는 대로 전부 넣는다**
4. 워커는 `engineMessage`를 **frame과 같은 40/s**로 낸다

→ **300 / 40 ≈ 7.5초.** 그 이상 지난 모든 줄은 사라진다.

**[추론]** 여기에 얄궂은 대목이 있다. **[ISSUE-008](#issue-008)의 대상인 #9는 `engineMessage`를 흐름 제어에서 일부러 뺐다** — 근거가 "버려서 아끼는 게 없고 버리면 진단이 통째로 사라진다"였다. 게이트웨이는 그 판단대로 진단을 온전히 보냈는데, **프론트가 자기 로그 버퍼에서 그걸 다시 버리고 있다.** 게다가 버려지는 쪽이 하필 진짜 진단(구독·로드·오류)이다. 두 계층이 각자 합리적으로 결정했는데 합치니 원래 지키려던 것이 사라졌다.

### 영향 범위

**[추론]** 손해는 "로그가 지저분하다"가 아니라 **문제가 생겼을 때 원인을 못 찾는다**는 것이다. 로그 패널은 사용자가 볼 수 있는 유일한 진단 창구다.

| 대상 | 영향 |
|---|---|
| 사람의 수동 검증 | **직접 영향.** #13·#15·#16이 전부 `verify: manual`이고, 판정 근거가 화면과 이 로그뿐이다 |
| [ISSUE-009](#issue-009) 진단 | **[추론]** 겹쳐서 나빠진다. 씬 로드로 시뮬이 멈춘 것을 로그로 확인하려 해도 그 줄이 이미 밀려나 있다 |
| 오류 추적 | **[코드 확인]** `protocolError`·`error`·`close` 진단(`:93-111`)이 전부 같은 버퍼를 쓴다. **오류가 난 직후 7초를 넘기면 증거가 사라진다** |
| 정지 중 | **영향 없음.** **[코드 확인]** 시뮬이 멈추면 `engineMessage`도 멈춘다(워커가 `maxFrame` 변화에만 낸다) |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **`engineMessage`를 별도 채널로 분리한다.** 엔진 메시지 전용 영역(또는 접힌 상세)을 두고, 주 로그에는 우리 이벤트만 남긴다.
   - **[추론]** 고려할 점: 가장 정직하다 — 두 종류의 로그는 성격도 유입 속도도 다르다. UI가 하나 는다.
2. **같은 메시지를 접는다.** `avatar animation (done)`처럼 연속 중복이면 `×N`으로 카운트만 올린다.
   - **[추론]** 고려할 점: 코드가 짧고 UI 변경이 없다. 다만 메시지에 시간(`64.1 sec`)이 박혀 있어 **문자열이 매번 달라 그대로는 안 접힌다** — 정규화 규칙이 필요하고, 그 규칙은 워커 메시지 형식에 결합된다.
3. **`engineMessage`를 초당 N건으로 스로틀한다.** 로그에 넣는 쪽에서 솎는다.
   - **[추론]** 고려할 점: 가장 싸다. 대신 **#9가 게이트웨이에서 일부러 안 버린 것을 프론트에서 버리는 것**이라, 왜 그렇게 하는지 주석으로 남기지 않으면 나중에 모순으로 읽힌다.
4. **`LOG_LIMIT`을 키운다.**
   - **[추론]** 고려할 점: 비율이 그대로라 시간만 늘어난다(3,000줄이면 75초). 근본이 아니고 DOM에 3,000줄을 그리는 비용이 생긴다.

### 결정

**미결.** **[추론]** 지금 진행을 막지는 않는다 — 필요한 확인은 콘솔의 `cobalt.stats`로 대신할 수 있었다. 다만 **`verify: manual` 단위가 셋(#14·#15·#16) 남아 있고 그 판정 근거가 이 로그**이므로, 그중 하나를 시작하기 전에는 정하는 편이 낫다. **[추론]** 2안과 3안을 합치는 것(중복 접기 + 스로틀)이 UI 변경 없이 가장 큰 효과를 낼 것으로 보이지만, 1안이 구조적으로 옳다.

---

<a id="issue-011"></a>

## ISSUE-011 — 옷 메시가 패턴 로컬 좌표로 나간다, 아바타를 얹으면 몸에서 떨어진다

- **상태**: **닫힘** — 2026-08-07, 커밋 `6091b10`으로 반영. 아래 [결정](#issue-011-결정) 참고. (이 줄은 `열림`으로 남아 있었다 — 표와 결정 절은 닫힘인데 본문만 안 바뀐 것이라 2026-08-09에 맞췄다.)
- **심각도**: **차단** — 지금 화면은 멀쩡하다(옷만 그리므로 어긋날 상대가 없다). 그러나 **아바타 작업이 이 위에 쌓이지 못한다.** 사용자가 요청한 "모델부터 세우고 옷을 입힌다"의 첫 단추가 여기다.
- **발견**: 2026-08-07, 아바타 렌더링 실현 가능성 조사 중. 세 갈래 조사 중 glTF 익스포터 갈래가 **익스포트 결과물(`out.gltf`)을 직접 파싱해** 발견했다. **[실측]이되 "아바타를 실제로 붙여 어긋나는 것을 봤다"는 아니다** — 아래 §미확인 참고.
- **영역**: worker — `backend/native/src/protocol.cpp`

### 증상

**[실측]** 우리 워커가 뽑은 `backend/native/build/Release/out.gltf`(sample.zls)의 노드 변환을 읽으면:

| 노드 | translation |
|---|---|
| 옷 패턴들 | `[12.4, 54.7, 15]`, `[-13.5, 56.6, -15]` 등 **제각각** |
| 아바타 (`zeta_body*`) | 전부 `[0, 0, 0]` |

**[코드 확인]** 즉 익스포터는 **옷에는 노드 변환을 걸고 아바타에는 안 건다.** 옷 정점이 패턴 로컬 좌표이고 아바타 정점은 이미 월드 좌표이기 때문이다.

**[코드 확인]** 그런데 우리 워커의 `MeshData()`(`backend/native/src/protocol.cpp:260`)는 `GetSimulationOutputMesh()`의 **원시 정점을 아무 변환 없이** 내보낸다. 변환을 프로토콜에 싣는 필드도 없다.

### 원인

**[코드 확인]** 옷 패턴 메시가 패턴 로컬 공간이라는 근거: `ztDesignClothPattern::GetAABB()`(회사 저장소 `Zest/design/ztDesignClothPattern.cpp:645-656`)가 경계 상자를 구할 때 `transform * mesh.vertices[i]`를 적용한다. 즉 **정점만으로는 위치가 결정되지 않고 `transform`이 있어야 한다.**

익스포터가 그 변환을 어디서 얻는지도 확인됐다 — `GetTransformIn3D()`(= `GetData().transform`, `:640`). 익스포터는 이것을 glTF 노드 변환으로 내보내고(`zwGltfExporterImpl.cpp:1915-1921`), 아바타에는 `zsTransform::Identity()`를 쓴다(`:521`, `:582`).

**[추론]** **지금까지 안 드러난 이유는 옷만 그렸기 때문이다.** 패턴 5장이 전부 같은 만큼 어긋나 있으면 사람 눈에는 그냥 "옷이 저기 있다"로 보인다. 기준이 되는 물체(아바타)가 화면에 없으면 절대 위치가 틀린 것을 알 방법이 없다.

### 영향 범위

| 대상 | 영향 |
|---|---|
| 아바타 렌더링 (신규 작업) | **직접 차단.** 아바타(월드)와 옷(로컬)을 같은 씬에 놓으면 어긋난다 |
| 현재 3D 뷰 | **[추론]** 영향 없음. 어긋날 상대가 없고 `frameCamera()`가 옷 경계에 맞춰 카메라를 잡는다 |
| TASKS #15 (2D 펼침 뷰) | **[추론]** 별개다. 2D는 `uvs`(cm 단위 패턴 좌표)를 쓰므로 이 변환과 무관하다 |
| TASKS #10 (glTF 다운로드) | **영향 없음.** 익스포터가 노드 변환을 스스로 넣는다 |

**[추론]** 추가로 **스케일도 맞춰야 한다.** 익스포터 루트 노드가 `scale = {0.01, 0.01, 0.01}`(`zwGltfExporterImpl.cpp:212`)인데, 엔진 단위가 cm이기 때문이다. 지금 three.js 쪽은 "격자 한 칸 10cm"로 별도 규약을 쓰고 있어, 아바타를 붙일 때 **두 물체에 같은 스케일이 걸리는지** 확인해야 한다.

### 미확인

**[추론]** "아바타를 붙였더니 실제로 떨어져 보였다"는 **관측되지 않았다.** 근거는 (a) 익스포트 결과물의 노드 변환이 옷과 아바타에서 다르다는 실측과 (b) `GetAABB()`가 변환을 적용한다는 코드다. 둘 다 강하지만, **변환이 실제로 얼마나 큰지**(무시할 만한 값일 가능성)는 확인하지 않았다. `out.gltf`의 `[12.4, 54.7, 15]`가 cm 단위라면 54.7cm는 무시할 수 없는 크기다 — 옷 높이가 약 100cm이므로 몸 절반 높이다.

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **`transform`을 프로토콜에 싣고 클라이언트가 적용한다.** `meshData`/`frame`의 패턴마다 4×4 행렬(또는 위치·회전·스케일)을 붙이고, three.js에서 `Mesh.matrix`로 건다.
   - **[추론]** 고려할 점: 익스포터와 같은 구조라 검증이 쉽다(GLB와 대조 가능). 프레임마다 변환이 바뀌는지는 확인이 필요하다 — 안 바뀌면 `topology:true`일 때만 실으면 된다.
2. **워커가 정점에 변환을 미리 적용해서 보낸다.** 클라이언트는 지금 그대로 둔다.
   - **[추론]** 고려할 점: 프론트 변경이 0이다. 대신 정점 3,022~13,398개에 매 프레임 행렬 곱이 붙고, **2D 뷰(#15)가 로컬 좌표를 원할 때 되돌릴 방법이 없어진다.**
3. **아바타 쪽을 옷 좌표계로 옮긴다.**
   - **[추론]** 아바타가 여럿이고 옷 패턴마다 변환이 다르므로 성립하지 않는다. 기록만 해 둔다.

<a id="issue-011-결정"></a>

### 결정

**1안 채택. 2026-08-07 커밋 `6091b10`으로 닫혔다.**

발견 당시 "확인 먼저 할 것"으로 남긴 두 질문에 답이 붙었다:

- **변환 값은 크다.** translation y가 54.7~56.7cm(한 패턴은 113cm), z가 ±15cm. 옷 높이 100cm 기준이라 무시할 수 없고, 위 §미확인의 "무시할 만한 값일 가능성"은 **기각**됐다.
- **프레임마다 바뀌지 않는다 — 0/5.** 시뮬 전과 **744프레임** 후를 같은 세션에서 비교해 비트 단위로 동일했다(파일 크기는 달라졌으므로 정점은 확실히 움직였다). 그래서 `topology:true`일 때만 싣는다 — 1안의 대역폭 비용이 사실상 0이라 2안(매 프레임 행렬 곱)보다 싸다는 것이 수치로 정해졌다.

**반영 내용** **[코드 확인]**

- 와이어 포맷은 4×4 행렬이 아니라 **TRS**다(`{translation:[x,y,z], rotation:[x,y,z,w], scale:[x,y,z]}`). 엔진의 `zsTransform`이 애초에 TRS로 저장하고 익스포터도 glTF 노드에 같은 분해로 쓰므로, 행렬로 내보내면 우리가 합성했다가 프론트가 다시 분해하는 왕복이 생긴다. 쿼터니언은 three·glTF와 같은 `[x,y,z,w]`.
- **정점에 미리 곱하지 않고 `Mesh`에 건다** — 2D 펼침 뷰(#15)가 로컬 좌표를 원한다(발견 당시 2안의 대가로 적어 둔 그대로다).
- `ClothObject.boundingBox()`는 반대로 **월드 좌표**를 돌려준다. 안 그러면 `frameCamera()`가 옷이 y=55~119에 서 있는데 원점을 겨눈다.
- `transform`이 없으면 identity(프레임 이벤트·구버전 워커), **malformed면 던진다** — 조용히 무시하면 이 이슈의 실패 모드가 단서 없이 재현되고, `fromArray(undefined)`는 NaN이 되어 three가 아무것도 안 그리는데 정점 수는 멀쩡해 보인다.
- 게이트웨이는 고칠 게 없었다. `meshData` 규칙에 `mapResult`가 없어 워커 결과가 그대로 전달된다(실측 5/5).

**검증 — 정답지가 있었다.** 발견 당시 1안의 장점으로 적어 둔 "GLB와 대조 가능"이 그대로 실현됐다. 같은 세션에서 익스포트한 glTF의 노드 계층이 정답이고, 실시간 옷의 월드 박스가 **0.035cm 이내**로 맞는다. 대조군(변환 미적용)은 58cm 어긋나며 옷 절반이 바닥 아래로 내려간다. 남은 0.03cm는 float 노이즈가 아니라 **익스포터가 패턴당 두께 오프셋 셸을 여러 장 내보내는 반면 `meshData`는 중간면 하나만 주기 때문**이다(그래서 허용치 0.5cm가 임의값이 아니다).

테스트 53건(288 → 341). **[실측]** 돌연변이 6건이 전부 빨간불이 됐는데, 그중 하나가 이 이슈의 교훈이다 — **쿼터니언을 `[w,x,y,z]`로 잘못 읽는 돌연변이에서 대표 단언인 박스 대조가 통과했다**(min 오차 0.036cm). Y축 180° 뒤집기는 AABB를 거의 흔들지 않는다. 실제로 잡은 것은 **점이 어디에 놓이는지**를 보는 단언이었다. **박스만 비교하는 테스트는 좌표계 문제에 불충분하다.**

화면 확인은 UI 하네스(`frontend && npm run verify:ui`, 커밋 `af2d2b3`)의 §2가 이어받았다 — `min y > 0`과 `frameCamera()`가 옷을 담는 거리에 카메라를 두는 것을 매 실행 단언한다.

---

<a id="issue-012"></a>

## ISSUE-012 — 익스포트가 간헐적으로 깨진 JSON으로 돌아온다

- **상태**: 열림
- **심각도**: **진단** — 실패가 조용하지 않다(파싱이 예외로 죽는다). 다만 **원인이 지목되지 않고**, 아래 §영향 범위의 가설이 맞으면 등급을 올려야 한다.
- **발견**: 2026-08-09, Playwright UI 검증 하네스(`af2d2b3`)를 만들며 반복 실행 중. **사람이 손으로 눌러서는 드러나지 않는 종류다** — 36MB짜리를 연달아 뽑아야 나온다.
- **영역**: worker — `backend/native/src/protocol.cpp` (또는 워커 → 게이트웨이 전송 경로)

> **[실측 — 서브에이전트 관측]** 아래 증상은 하네스를 만든 서브에이전트가 관측한 것이고, **메인 세션이 직접 재현하지는 않았다.** 메인 세션이 재현한 것은 [ISSUE-013](#issue-013)(워커 사망) 쪽이다. 두 현상이 같은 뿌리인지도 미확정이다.

### 증상

**[실측]** 익스포트가 낮은 확률로 두 가지 형태로 깨진다.

| 어디서 | 메시지 |
|---|---|
| 워커 | `[json.exception.type_error.316] invalid UTF-8 byte at index 1114727: 0xFA` |
| 브라우저 | `Expected ',' or '}' after property value in JSON at position … (line 9764)` |

**인덱스와 바이트 값이 매번 다르다.** 즉 특정 데이터가 아니라 무작위 위치가 망가진다.

### 원인

**확정되지 않았다.**

**[실측] 결정적인 단서 하나** — **디스크에 남은 `.gltf` 파일은 UTF-8도 JSON도 멀쩡하다.** 즉 익스포터가 만든 산출물 자체는 정상이고, **워커 안이거나 워커 → 게이트웨이 전송 중**에 망가진다.

**[추론]** 후보:

1. **워커의 JSON Lines 응답 직렬화.** 익스포트 응답에는 서버 경로 문자열이 실리는데, 그게 비-ASCII를 포함하면(씬 이름이 `W_Bra top & Leggings.zls`처럼 공백·`&`를 갖는다) 인코딩 경로가 관여한다. 다만 인덱스 1,114,727은 경로보다 훨씬 뒤라 이것만으로는 설명이 안 된다.
2. **stdout 파이프의 버퍼 경계.** 워커 → 게이트웨이가 stdin/stdout JSON Lines다. 큰 응답이 청크로 쪼개질 때 경계 처리가 어긋나면 정확히 이런 무작위 손상이 난다. **인덱스가 매번 다른 것이 이 가설과 맞는다.**
3. [ISSUE-013](#issue-013)(반복 익스포트 시 워커 사망)과 같은 뿌리 — 메모리 손상이라면 두 증상이 한 원인의 다른 얼굴일 수 있다.

**[추론]** 2번이 가장 유력해 보인다. 다만 **익스포트 응답 자체는 작다**(`{id, format, bytes, name, createdAt, url}`) — 1MB 넘는 JSON이 워커에서 나가는 경로는 `meshData`이지 `export`가 아니다. 그렇다면 관측된 예외가 익스포트 요청 중에 **동시에 흐르던 다른 응답**에서 났을 가능성이 있고, 그러면 범인은 익스포트가 아니라 그 옆의 큰 응답이다. **이 구분을 먼저 해야 한다.**

### 영향 범위

**[추론]** 지금은 **재실행하면 통과**하므로 진행을 막지 않는다. 손해는 두 곳에 있다.

| 대상 | 영향 |
|---|---|
| 사용자에게 파일을 주는 기능 | **직접 영향.** 익스포트는 결과물을 내려주는 기능이라, 깨진 응답이 나가면 사용자가 받는 것이 깨진다 |
| **[추론] 확인 필요 — 깨진 산출물이 URL과 함께 서빙될 수 있는가** | 게이트웨이의 `commit()`은 `stat`으로 **파일 존재와 크기만** 확인하고 JSON 유효성은 보지 않는다(`backend/src/server/files.ts`). 워커가 `ok:true`를 주고 파일이 잘려 있으면 **정상 URL로 깨진 파일이 나간다.** 이게 성립하면 이 이슈는 `진단`이 아니라 더 높은 등급이다 |
| UI 검증 하네스 | **[실측]** 간헐적으로 빨간불이 된다. [ISSUE-007](#issue-007)과 같은 종류의 신뢰 손상을 하네스에 옮긴다 |
| 스모크 | **[실측]** `backend/src/server/smoke.ts` §10-1이 익스포트를 1회만 하고 sha256까지 대조하는데, 373건 실행에서 이 증상이 관측된 적이 없다. **반복이 조건일 가능성** |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **먼저 범인을 가른다** — 익스포트 응답인가, 동시에 흐르던 다른 응답인가. 워커의 stdout을 그대로 파일로 떠서 깨진 줄이 어느 op의 응답인지 본다.
   - **[추론]** 가장 먼저 해야 할 일이다. 이걸 안 하면 나머지 안이 전부 헛다리를 짚을 수 있다.
2. **`commit()`이 JSON 유효성까지 확인한다.** 위 "깨진 산출물 서빙" 경로를 막는다.
   - **[추론]** 고려할 점: 36MB를 매번 파싱하는 비용이 붙는다. 선두·말미 검사나 크기 대조로 대신할 수도 있다. **원인을 못 고치더라도 이건 값이 있다** — 사용자가 깨진 파일을 받는 것만은 막는다.
3. **stdout 프레이밍을 길이 접두어로 바꾼다.** JSON Lines 대신 `Content-Length` 방식.
   - **[추론]** 고려할 점: 2번 가설이 맞을 때의 근본 수정이지만 프로토콜 변경이라 범위가 크다. 원인 확정 전에는 이르다.
4. **바이너리 채널로 옮긴다.** `TASKS.json` 백로그의 `deferred`에 이미 있는 항목이다(base64 제거로 33% 감소).
   - **[추론]** 고려할 점: 같은 방향이지만 더 큰 작업이다.

### 결정

**미결. 1안(범인 가르기)을 먼저 한다.** **[추론]** 원인이 확정되기 전에는 등급도 정확하지 않다 — 위 §영향 범위의 "깨진 산출물이 URL과 함께 나갈 수 있는가"가 이 이슈의 실제 무게를 정한다. 그 확인만 따로 떼어 먼저 할 가치가 있고, 답이 "그렇다"면 2안은 원인과 무관하게 넣어야 한다.

---

<a id="issue-013"></a>

## ISSUE-013 — 익스포트를 반복하면 워커가 죽는다 (code 4001)

- **상태**: 열림
- **심각도**: **진단** — 세션이 죽지만 게이트웨이는 살아 있고 재연결이 정상 동작한다(`CLOSE_SESSION_LOST`가 설계대로 발화한다). 사용자가 받는 신호가 원인을 지목하지 못하는 것이 문제다.
- **발견**: 2026-08-09, Playwright UI 검증 하네스를 반복 실행하며. **메인 세션이 직접 재현했다.**
- **영역**: worker — `backend/native/`

### 증상

**[실측]** UI 하네스를 **연달아** 돌리면 두 번째 실행에서 씬 로드가 실패한다.

```
로드 실패: 연결이 끊겨 load 응답을 받지 못했습니다
           (code=4001, 워커 프로세스가 종료되었습니다)
```

게이트웨이는 3000번에서 정상 리스닝 중이고, 워커 프로세스만 사라져 있다. **게이트웨이를 재시작하면 68/68로 통과한다.**

**[실측]** 띄엄띄엄 돌리면 재현되지 않는다 — 서브에이전트가 6회 연속 68/68을 얻었다.

### 원인

**확정되지 않았다.** **[추론]** 후보:

1. **앞 실행의 워커 정리와 새 워커의 144MB 로드가 겹친다.** 하네스가 끝나면 소켓이 닫히고 `#detach`가 워커를 반납·종료시키는데, 그 직후 새 실행이 곧바로 새 워커를 띄우고 144MB를 읽는다. 두 프로세스가 잠깐 공존하며 각각 수백 MB를 쓴다.
2. **익스포트가 만든 메모리 압력.** 36MB glTF를 만들려면 워커 안에서 텍스처 13장을 디코드·리스케일·재인코딩한다. 세션당 360MB로 알려진 기준선 위에 얹힌다.
3. [ISSUE-012](#issue-012)와 같은 뿌리(메모리 손상)일 가능성.

**[추론]** 어느 것도 확인되지 않았다. **워커가 왜 죽었는지 알 방법이 지금 없다** — 종료 코드는 게이트웨이 로그에 남지만 워커 자신의 마지막 상태는 남지 않는다.

### 영향 범위

| 대상 | 영향 |
|---|---|
| UI 검증 하네스 | **직접 영향.** 연속 실행이 불가능하다. 하네스가 "UI 회귀가 아니라 워커가 죽은 것"이라고 지목하도록 만들어져 있어 오진하지는 않지만, 매번 게이트웨이를 재시작해야 한다 |
| CI 도입 | **[추론]** 하네스를 CI에 걸면 여기 부딪힌다. [ISSUE-007](#issue-007)과 같은 판단이 필요한 자리다 |
| 실사용 | **[추론]** 한 사용자가 익스포트를 연달아 누르면 만날 수 있다. 세션이 끊기고 재연결되며 씬을 다시 로드해야 한다 — `main.ts`의 `open` 핸들러가 그걸 하므로 화면은 복구되지만, 사용자는 이유를 모른다 |
| 동시 인스턴스 | **[추론]** `TASKS.json`의 `openQuestions` id 2("동시 인스턴스 미측정 — CPU가 먼저 막힐 가능성")와 같은 자리를 가리킨다. **메모리가 먼저 막힐 가능성**이 여기서 처음 관측됐다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **워커가 죽는 이유를 먼저 관측한다.** 워커의 stderr·종료 코드를 게이트웨이 로그에 남기고, 죽기 직전 메모리 사용량을 찍는다.
   - **[추론]** 가설 1·2를 가르는 유일한 방법이다. 지금은 "죽었다"만 알고 왜인지 모른다.
2. **세션 반납이 끝날 때까지 새 acquire를 막는다.** 가설 1이 맞으면 이것으로 닫힌다.
   - **[추론]** 고려할 점: `SessionPool`이 이미 수명을 관리하므로 붙일 자리는 있다. 다만 정상 사용에서도 지연이 생긴다.
3. **하네스가 게이트웨이를 스스로 재시작한다.** 증상을 우회한다.
   - **[추론]** 고려할 점: **증상을 덮어 원인 규명이 늦어진다.** 하네스가 서버 수명을 관리하지 않기로 한 결정([ISSUE-012](#issue-012)와 같은 실행에서 정했다)도 뒤집게 된다.

### 결정

**미결. 1안(관측)을 먼저 한다.** **[추론]** [ISSUE-012](#issue-012)와 함께 봐야 한다 — 둘이 같은 뿌리면 한 번에 닫히고, 아니면 각각 다른 수정이 필요하다. 지금 진행을 막지는 않는다(게이트웨이 재시작으로 통과). 다만 **하네스를 CI에 걸기 전에는 결정해야 한다.**

---

<a id="issue-014"></a>

## ISSUE-014 — `setParams`가 필드마다 갈린다, `gravityY`·`subStep`이 시뮬에 반영되지 않는다

- **상태**: 열림 — **2026-08-09 전수 측정 완료로 #16의 차단은 풀렸다.** 22개 중 **반영됨 17 · 조건부 3 · 죽음 2**([§전수 측정](#issue-014-측정)). 죽은 필드 2개(`subStep`·`meshingEdgeLength`)의 원인은 미해결이라 이슈는 열어 둔다.
- **심각도**: 차단 → **진단**(2026-08-09). 측정으로 표가 나왔으므로 #16이 사실 위에 설 수 있다. 남은 것은 죽은 필드를 되살리는 문제이고, 그것은 진행을 막지 않는다.
- **[기록] 최초 심각도**: **차단** — 지금 동작에는 문제가 없다(프론트가 `setParams`를 아직 안 보낸다). 그러나 **TASKS #16(스키마 기반 파라미터 패널)이 이 위에 쌓이지 못한다.** 슬라이더 40개를 만들어 놓고 그중 몇 개가 아무 일도 안 하면, 사용자는 "값을 바꿨는데 옷이 안 변한다"만 보게 되고 원인이 화면 어디에도 안 남는다.
- **발견**: 2026-08-09, "브라 끈이 흘러내리는 원인" 조사 중 부수적으로. 파라미터 차이가 원인인지 가르려고 값을 바꿔 가며 돌리다 드러났다.
- **영역**: worker — `backend/native/src/protocol.cpp`

### 증상

**[실측]** 같은 씬(`W_Bra top & Leggings.zls`)에서 로드 직후·`start` 전에 값을 걸고 100프레임을 돌려 24개 패턴의 결과를 비교했다.

| 필드 | 시도한 값 | 결과 |
|---|---|---|
| `gravityY` | `0` vs `-980` | **24개 패턴 전부 실행 간 지터(~1%) 안에서 동일.** 90cm짜리 레깅스 패널까지 같다 |
| `subStep` | `1` / `4` / `8` | **비트 단위로 동일** |
| `timeStep` | 45 → 90 | 반영됨 |
| `drapingTime` | 0.4 → 0 / 3.0 | 반영됨 |
| `nonlinearIterations` | 1 → 10 | 반영됨 (실행시간 12s → 56s) |

**`getParams`는 쓴 값을 정확히 되돌려준다.** 즉 **필드에는 써지는데 물리가 그 값을 보지 않는다.** 클라이언트 입장에서는 성공 응답을 받고 아무 일도 일어나지 않는다.

**[코드 확인]** 두 필드 모두 프로토콜에는 정상적으로 배선돼 있다 — `backend/native/src/protocol.cpp:89`·`:91`(읽기), `:130`·`:132`(쓰기). 우리 쪽 배선 실수가 아니다.

### 원인

**`subStep`은 메커니즘이 유력하다.** **[코드 확인]** `ZestManager::LoadZls`가 로드 직후 `simulationParams.useSubStepTargeting = false`를 **강제**한다(`zelusSandBox_Cobalt/zelusSandBox/ZestManager.cpp:835`). `LoadDrapingItem`도 같은 줄을 갖고 있다(`:451`). **[추론]** 서브스텝 타게팅이 꺼져 있으면 `subStep` 값이 쓰이지 않을 개연성이 높다. 회사 저장소라 우리가 그 줄을 고칠 수 없다 — 로드 **뒤에** `useSubStepTargeting`을 다시 켜는 경로가 필요한데, 그 필드는 지금 우리 프로토콜에 없다.

**`gravityY`는 미확정이다.** **[추론]** 후보 둘: (a) 솔버가 씬의 `simulationParams`가 아니라 별도 경로(`zsSimulationWorld`의 자체 중력)에서 중력을 읽는다, (b) `Synchronize`나 시뮬 시작 시점에 씬 값이 덮어써진다. **어느 쪽인지 확인되지 않았다.**

<a id="issue-014-측정"></a>

### 전수 측정 (2026-08-09) — 위 §증상의 판정 둘이 뒤집혔다

**[실측]** 1안을 실행했다. 도구는 `backend/tools/param-sweep.ts` (`npm run sweep:params`), 씬은 위와 같은 `W_Bra top & Leggings.zls`, 100프레임, 22개 필드 + 짝 1개 + 기준선 3회 = 26회차.

**측정 방법이 위 §증상과 다르고, 그 차이가 결론을 갈랐다.**

| | §증상 (첫 측정) | 전수 측정 |
|---|---|---|
| 비교 대상 | 패턴 24개의 요약값 | **정점 하나하나의 위치** (uuid로 짝지어 유클리드 거리) |
| 노이즈 처리 | "실행 간 지터 ~1%" 안이면 같다고 봄 | **기준선을 3회 돌려 바닥을 실측** |
| 회차 격리 | — | 회차마다 워커를 새로 띄운다 |

**[실측] 지터 바닥은 1%가 아니라 정확히 0cm다.** 같은 파라미터로 3회 돌린 결과가 24개 패턴 전 정점에서 **비트 단위로 같다.** 정지 프레임도 3회 모두 같았다. 즉 **솔버는 결정적이고, "~1% 지터"라는 전제 자체가 틀렸다.** 이것이 결정적인 차이다 — 바닥이 0이면 0.1cm짜리 신호도 노이즈가 아니라 사실이 된다. 첫 측정은 그 신호를 지터로 오해할 수 있는 문턱을 쓰고 있었다.

**[코드 확인]** 정점별 비교를 택한 것은 [ISSUE-011](#issue-011)에서 배운 것이다 — AABB만 보는 단언은 쿼터니언을 잘못 읽어도 0.036cm밖에 안 흔들려서 통과했다.

#### 결과 표

기준선과의 평균/최대 변위(cm). 판정 문턱은 `1.0e-4`cm(바닥 0 × 5, 하한 적용).

| 필드 | 시도한 값 | 평균 변위 | 최대 변위 | 판정 |
|---|---|---|---|---|
| `timeStep` | 45 → 90 | 1.7780 | 8.6145 | 반영됨 |
| `subStep` | 1 → 8 | **0** | **0** | **반영 안 됨** |
| `drapingTime` | 0.4 → 3 | 4.9431 | 16.2044 | 반영됨 |
| `gravityY` | -980 → 0 | 0.2217 | 1.4188 | **반영됨** (§증상과 반대) |
| `groundPlane` | true → false | **0** | **0** | 반영 안 됨 — 단, 접촉 없음 (아래) |
| `groundFriction` | 0.2 → 0.9 | **0** | **0** | 반영 안 됨 — 단, 접촉 없음 (아래) |
| `groundMargin` | 0.5 → 5 | 0.0151 | 0.5784 | 반영됨 |
| `useWind` | false → true | 0.1142 | 0.4624 | 반영됨 |
| `windMagnitude` | 30 → 500 | **0** | **0** | 반영 안 됨 — 단, `useWind` 종속 (아래) |
| `solverType` | 0 → 1 | 1.1897 | 2.7982 | 반영됨 |
| `preconditioner` | 2 → 1 | 3.8398 | 9.7977 | 반영됨 |
| `nonlinearIterations` | 1 → 10 | 1.6731 | 3.4673 | 반영됨 |
| `maxSolverIterations` | 600 → 5 | 2.9720 | 11.3320 | 반영됨 |
| `solverTolerance` | 1e-4 → 0.1 | 3.0284 | 13.5640 | 반영됨 |
| `useIEQS` | false → true | 5.5086 | 17.9430 | 반영됨 |
| `staticCouplingMethod` | 4 → 1 | 5.2923 | 10.7787 | 반영됨 |
| `dynamicCouplingMethod` | 3 → 1 | 0.4051 | 6.8023 | 반영됨 |
| `dynCouplingStiffness` | 750 → 0 | 0.4310 | 1.4412 | 반영됨 |
| `dynCouplingDamping` | 0.1 → 0 | 0.5674 | 1.4153 | 반영됨 |
| `untanglingStiffness` | 20000 → 0 | 0.3326 | 2.0304 | 반영됨 |
| `untanglingDamping` | 250 → 0 | 1.7956 | 6.8926 | 반영됨 |
| `meshingEdgeLength` | 1 → 4 | **0** | **0** | **반영 안 됨** |
| `useWind` + `windMagnitude` (짝) | true / 500 | 7.1927 | 31.4170 | 반영됨 |

**모든 회차에서 `getParams`가 건 값을 그대로 돌려줬다.** 0cm인 필드도 값은 정상적으로 걸렸다 — 위 §증상의 "필드에는 써지는데 물리가 보지 않는다"는 그대로 유효하다.

#### 0cm이 나온 5개를 다시 가르면 셋으로 갈린다

**[실측] `reset`을 걸어도 5개 전부 그대로 0cm다.** setParams 뒤 `start` 전에 `reset()`을 한 번 넣어 다시 쟀다(`npm run sweep:params -- --reset`). "로드/초기화 시점에만 읽히는 필드"라는 가설은 이 넷(`subStep`·`groundPlane`·`groundFriction`·`meshingEdgeLength`)에 대해 기각됐다.

**① `windMagnitude` — 죽지 않았다. `useWind`에 종속일 뿐이다.**
**[실측]** 단독으로 걸면 0cm인데 `useWind: true`와 함께 걸면 **7.1927cm**다. `useWind` 단독(0.1142cm)의 **63배**이므로 크기를 만드는 것은 `windMagnitude`가 맞다. 기준선이 `useWind: false`라 단독 변경이 무의미했던 것이다. **#16에서 이 필드는 `useWind`가 켜져야 활성화되는 종속 위젯이어야 한다.**

**② `groundPlane`·`groundFriction` — 이 씬에서 바닥 접촉이 일어나지 않는다.**
**[실측]** `backend/tools/ground-probe.ts`로 100프레임 뒤 옷의 **월드 최저 Y를 쟀다: 9.27cm** (최고 147.69cm, 패턴 24개 전부 transform 적용). `groundMargin`이 0.5cm이므로 접촉까지 **8.8cm가 남는다.** 닿지 않는 면의 마찰을 바꿔 아무 일도 안 일어나는 것은 **필드가 죽은 것이 아니라 물리가 옳은 것이다.**

> ⚠️ **[추론] 다만 `groundMargin`만 0.0151cm 움직인 것이 이 설명과 어긋난다.** 접촉이 없다면 margin을 0.5 → 5로 키워도(여전히 9.27cm 아래) 0이어야 한다. 후보 둘: (a) 시뮬 도중 일시적으로 더 내려가 접촉이 있었다(최저 Y는 100프레임 시점의 값이다), (b) `groundMargin`이 바닥판 외의 충돌 여유에도 쓰인다. **가르지 않았다.** `groundPlane: false`가 0cm인데 `groundMargin`은 움직인다는 사실이 (a)와도 완전히 맞지는 않는다 — 바닥을 통째로 끄는 편이 여유를 키우는 것보다 영향이 커야 한다.

**③ `subStep`·`meshingEdgeLength` — 진짜로 죽었다.**
**[실측]** 두 필드는 어떤 갈래에서도 0cm이고, 값이 안 걸린 것도 접촉이 없는 것도 아니다. `subStep`의 메커니즘은 위 §원인에 있다. **`meshingEdgeLength`는 첫 측정에서 다루지 않았던 새 발견이다** — **[추론]** 리메싱은 로드 시점에 끝나고 그 뒤에는 값을 봐도 다시 메싱하지 않을 개연성이 높다. 정점 수가 그대로였다(리메싱이 일어났다면 정점 수가 바뀌어 정점별 비교 자체가 불가능해졌을 것이다).

#### 그래서 22개는 이렇게 갈린다

| 분류 | 수 | 필드 |
|---|---|---|
| **반영됨** | 17 | `timeStep` `drapingTime` `gravityY` `groundMargin` `useWind` `solverType` `preconditioner` `nonlinearIterations` `maxSolverIterations` `solverTolerance` `useIEQS` `staticCouplingMethod` `dynamicCouplingMethod` `dynCouplingStiffness` `dynCouplingDamping` `untanglingStiffness` `untanglingDamping` |
| **조건부** | 3 | `windMagnitude`(`useWind` 필요) · `groundPlane` `groundFriction`(바닥 접촉이 있는 씬 필요) |
| **죽음** | 2 | `subStep` · `meshingEdgeLength` |

**[추론]** `gravityY`가 0.2217cm에 그친 것은 여전히 설명이 필요하다. 중력을 통째로 끈 것 치고 작다. 후보: 이 씬이 저장된 드레이프 상태에서 시작해 이미 안정점 근처이고, 100프레임이 그 평형을 크게 벗어나기에 짧다. **위 §원인의 (a)·(b) 가설은 이제 불필요하다 — 값은 솔버에 도달한다.** 다만 "얼마나 도달하는가"는 안 쟀다.

### 영향 범위

| 대상 | 영향 |
|---|---|
| **TASKS #16 (파라미터 패널)** | **직접 차단.** 이 단위의 통과 기준이 "슬라이더가 시뮬에 반영되는지 화면 확인"이다. 어느 필드가 죽었는지 모르면 그 기준을 세울 수 없다 |
| 백로그 A그룹 (파라미터 확장 17개) | **[추론]** 같은 문제를 만날 수 있다. 지금 노출된 22개 중 2개가 죽었다면, 17개를 더 열 때도 검증이 필요하다 |
| 현재 동작 | **영향 없음.** **[코드 확인]** `frontend/src/main.ts`에 `setParams` 호출이 없다 |
| 이미 통과한 스모크 | **[추론]** 영향 없음. `setParams`/`getParams`의 왕복만 검증하고 **물리 반영은 검증하지 않는다** — 그래서 373건이 전부 통과하면서도 이걸 놓쳤다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **어느 필드가 살아 있는지 전수 측정한다.** 22개를 하나씩 바꿔 가며 100프레임 결과를 비교해 표를 만든다.
   - **[추론]** 가장 먼저 해야 할 일이다. **#16의 스키마가 그 표 위에 서야 한다** — 죽은 필드를 슬라이더로 내보내면 안 된다. 필드당 ~12초라 22개면 5분 안쪽이다.
2. **죽은 필드를 프로토콜에서 빼거나 "미지원"으로 표시한다.** 클라이언트가 성공 응답을 받고 착각하는 것을 막는다.
   - **[추론]** 고려할 점: 나중에 살아나면 다시 넣어야 한다. 스키마에 `effective: false` 같은 표식을 두는 편이 낫다.
3. **`subStep`에 한해 — 로드 뒤 `useSubStepTargeting`을 켜는 경로를 만든다.**
   - **[추론]** 고려할 점: 엔진이 그걸 끄는 이유가 있을 수 있다(로드 직후 상태에서 서브스텝이 불안정하다든지). 켰을 때 결과가 어떻게 달라지는지 먼저 봐야 한다.
4. **`gravityY`는 원인부터 가른다.** 솔버가 중력을 어디서 읽는지 추적한다.
   - **[추론]** 회사 저장소를 읽어야 한다. `zsSimulationWorld`의 중력 설정 경로가 출발점이다.

### 결정

**1안(전수 측정)을 실행했다 — 2026-08-09. 결과는 위 [§전수 측정](#issue-014-측정)에 있다.** 이로써 **#16의 차단이 풀렸다.**

**[추론]** 2안(죽은 필드를 "미지원"으로 표시)을 #16에서 채택한다. 프로토콜에서 빼지 않는 이유는 2안이 이미 적어 둔 것과 같다 — 나중에 살아나면 되돌려야 한다. 대신 스키마가 세 분류를 그대로 들고 간다:

| 분류 | #16에서의 처리 |
|---|---|
| 반영됨 (17) | 일반 위젯 |
| 조건부 (3) | `windMagnitude`는 `useWind`에 종속(꺼져 있으면 비활성). `groundPlane`·`groundFriction`은 위젯은 두되 **"이 씬에서 검증되지 않음"** 표식 |
| 죽음 (2) | `subStep`·`meshingEdgeLength`는 **슬라이더로 내보내지 않는다** |

**미결로 남은 것 셋** — 셋 다 #16을 막지 않는다:

1. **`groundMargin`이 왜 움직이는가.** 접촉이 없다는 설명과 어긋난다(위 §0cm 5개 갈래 ②의 경고).
2. **`gravityY`의 크기가 왜 작은가.** 도달은 확인됐고 크기는 안 쟀다.
3. **3안(`useSubStepTargeting`을 로드 뒤 켜기)은 여전히 미착수.** `subStep`을 되살리는 유일한 길이다.

**[추론]** 첫 측정의 판정 둘이 뒤집혔다는 사실 자체가 남길 값이 있다. **틀린 것은 데이터가 아니라 문턱이었다** — "실행 간 지터 ~1%"라는 전제가 실제로는 0%였고, 그 전제가 0.22cm짜리 실제 신호를 노이즈로 삼켰다. **노이즈 바닥을 가정하지 말고 측정할 것.** 기준선을 3회 돌리는 비용은 42초였다.

**[추론]** 이 이슈가 드러난 방식도 기록해 둔다 — **파라미터를 바꿔 가며 결과를 비교하는 실험**을 하지 않았다면 영영 안 보였다. 왕복 테스트(`setParams` → `getParams`)는 373건 중 어느 것도 이걸 잡지 못한다. 값을 쓰고 되읽는 것과 **그 값이 물리를 바꾸는 것**은 다른 명제다.

---

<a id="issue-015"></a>

## ISSUE-015 — `step` op이 워커에서 아무 일도 하지 않는다

- **상태**: 열림
- **심각도**: **차단** — 지금 동작에는 문제가 없다(화면에서 컨트롤을 뺐다). 그러나 **데스크톱 기능 #63(SPACE = 한 프레임 진행)을 이 위에 만들 수 없다.** [ISSUE-014](#issue-014)와 정확히 같은 형태다 — op이 성공 응답을 주고 아무 일도 하지 않으며, 그래서 그 위에 얹힐 UI가 만들어질 수 없다. 그 선례를 따라 분류했다.
- **발견**: 2026-08-09, TASKS.json #14 "재생 컨트롤"(커밋 `3df0621`) 구현 중. **#14의 통과 기준은 PASS다** — 아래 §영향 범위대로 컨트롤을 빼는 것으로 기준을 만족시켰다.
- **영역**: worker — `backend/native/src/protocol.cpp`

### 증상

**[실측]** 정지 상태에서 `step`을 보내면 응답은 `{ "mode": "step" }`인데:

| 확인 | 값 |
|---|---|
| 직후 `status`의 `mode` | **`pause`** — 우리가 건 `step`이 아니다 |
| `maxFrame` (3회 연속 step) | **14 → 14 → 14 → 14.** 5초를 기다려도 그대로 |
| frame 이벤트 | **0건** |

**[실측] UI를 지나지 않고 `client.step()`을 직접 불러도 같다.** 프론트의 상태 기계나 게이트웨이 중계가 개입한 결과가 아니다.

**[실측]** 재생 중에 `step()`을 부르면 컨트롤러가 믿음을 내렸다가 `syncFromWorker()`가 되돌려 `stats.corrections`가 **1** 오른다. 워커가 멈추지 않으니 화면은 정직하다 — 그 계측기가 여기서 제 몫을 한다.

### 원인

**확정되지 않았다.** 다만 **우리 쪽 배선은 갖춰져 있다.**

**[코드 확인]** 워커에서 프레임을 미는 유일한 지점은 요청이 없을 때 도는 idle 갈래의 `manager.UpdateFrame()`이다(`backend/native/src/protocol.cpp:497`, 5ms 주기). `PLAY` 모드에서 프레임 이벤트가 40/s로 나오는 것이 이 드라이버가 실제로 돈다는 증거다. `step` 갈래(`:584-588`)는 다른 재생 op(`start`·`pause`·`reset`, `:567-583`)과 똑같이 `SetAnimationMode`만 부른다 — **우리 코드에 빠진 줄이 없다.**

**[추론]** 그리고 `STEP`은 **소비되기는 한다** — 직후 `status`가 `pause`를 돌려주므로 `ZestManager`가 모드를 스스로 되돌렸다는 뜻이다. 즉 "모드가 안 걸린다"가 아니라 **"모드는 걸리고 소비됐는데 프레임이 안 만들어진다"**이다. 원인은 `ZestManager::UpdateFrame`의 `STEP` 처리 안에 있고, 그곳은 읽기 전용 회사 저장소다.

### 영향 범위

**[실측]** #14가 스텝 버튼과 SPACE 단축키를 **넣었다가 뺐다.** 눌러도 안 움직이는 컨트롤은 #14가 없애려던 바로 그 거짓말이라, 워커가 고쳐질 때까지 화면에 두지 않기로 했다.

| 대상 | 영향 |
|---|---|
| **데스크톱 기능 #63** | **직접 차단.** #60(재생/정지)·#61(리셋)·#62(씬 내림)은 #14에서 됐고 **#63만 미구현으로 남았다** |
| TASKS #14 | **[코드 확인]** `PlaybackController.step()`은 남아 있다. 워커가 고쳐지면 `frontend/index.html`의 버튼 한 줄과 `frontend/src/panels/shortcuts.ts`의 표 한 줄로 되살아난다 |
| 프레임 단위 검증 | **[추론]** "한 프레임만 진행시켜 결과를 본다"는 실험 방법 자체가 지금 없다. [ISSUE-014](#issue-014)의 1안(파라미터 전수 측정)처럼 결과를 비교하는 작업에서 아쉬워질 수 있다 |
| 현재 동작 | **영향 없음.** 화면에 표면이 없고, 재생 중에 불려도 사실이 믿음을 덮는다 |

### 테스트가 이 결함을 다루는 방식

**[코드 확인]** **단언하지 않고 관찰만 한다.** `frontend/src/protocol/smoke.ts`의 §10-8(`:4994-5006`)이 그 판단을 명시한다 — 워커의 no-op은 제품 코드의 성질이 아니라 **지금 워커의 상태**이므로, 그걸 단언하면 **고쳐지는 날 테스트가 깨진다.** 대신 no-op이든 아니든 성립해야 하는 것만 단언한다: ① 컨트롤러의 믿음이 워커의 사실(`mode:"pause"`)과 어긋나지 않는다 ② 화면 표면(버튼·단축키)에 올라가 있지 않다.

**[코드 확인]** 워커의 no-op 자체는 **§10-10**이 실제 워커로 **관찰만** 한다(`note`). 가짜 포트(`makePort()`)가 워커를 흉내내는 다섯 가지 중 하나가 이것이고, §10-10이 그 다섯을 실물로 대조한다 — 모형이 실제와 갈라지면 §10-1~§10-9가 통째로 환상을 검증하게 되기 때문이다.

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **`STEP`이 어떻게 소비되는지 먼저 관측한다.** `UpdateFrame()` 전후로 `GetAnimationMode()`·`curFrame`을 찍어, 모드가 언제 `PAUSE`로 돌아가는지와 그때 솔버가 불렸는지를 가른다.
   - **[추론]** 가장 먼저 해야 할 일이다. 이걸 안 하면 나머지 안이 헛다리를 짚는다 — [ISSUE-012](#issue-012)·[ISSUE-013](#issue-013)에서 같은 순서를 택했다.
2. **`step`을 워커에서 합성한다.** `PLAY`로 바꾸고 프레임 콜백이 한 번 오면 `PAUSE`로 되돌린다.
   - **[추론]** 고려할 점: 정확히 한 프레임인지 보장하기 어렵다(드라이버가 5ms 루프다). 그리고 **엔진의 `STEP`이 하려던 것과 같은 동작인지 알 수 없다** — 데스크톱 #63이 무엇을 하는지는 `PROJECT_ANALYSIS.md` §4에서 먼저 확인해야 한다.
3. **op을 "미지원"으로 표시한다.** 클라이언트가 성공 응답을 받고 착각하는 것을 막는다.
   - **[추론]** [ISSUE-014](#issue-014)의 2안과 같은 방향이고, **원인을 못 고치더라도 값이 있다.** 지금은 화면에 표면이 없어 사람이 속지 않지만, 그 사실이 코드가 아니라 주석에만 적혀 있다.
4. **`ZestManager`의 `STEP` 처리를 읽는다.**
   - **[추론]** 회사 저장소(읽기 전용)를 읽어야 한다. 고칠 수는 없으므로, 얻는 것은 2안을 정확히 만들 근거다.

### 결정

**미결. 1안(관측)을 먼저 한다.** **[추론]** 지금 진행을 막지 않는다 — #14가 화면에서 컨트롤을 뺐고, 그 판단은 원인과 무관하게 옳다. 다만 **#63을 완료로 세지 말 것.** `TASKS.json`의 `clientOnly`가 키보드 단축키 #60~#63을 한 줄로 묶어 두고 있어, 넷 중 하나만 빠진 것이 그 표에서는 보이지 않는다.

### 참고 — `clear` 뒤에도 `status.loaded`가 `true`로 남는다

**이슈로 따로 올리지 않고 여기 붙인다.** 같은 파일의 같은 계열이기 때문이다 — **워커의 응답이 워커의 사실과 어긋난다.**

**[코드 확인]** `clear` 갈래는 `manager.Clear()`를 부르고 프레임 카운터를 되돌린다(`backend/native/src/protocol.cpp:559-566`). 그런데 `status`가 읽는 `manager.IsLoadedZls()`(`:604`)는 **내려가지 않는다.** 즉 씬을 내린 뒤에도 `status.loaded === true`다.

**[추론]** **지금은 영향이 없다.** 화면은 `clear` op의 성공 여부로 판단하고, 컨트롤러의 `scene`을 `null`로 만들어 상태가 `noScene`이 된다 — `status.loaded`를 보지 않는다. 그러나 **"워커에 씬이 있는가"를 이 필드로 판정하는 코드를 새로 쓰면 안 된다.** 로드된 적 있는 세션에서는 영원히 참이다.

### 참고 — `pause` 뒤 `status.frame`이 `-1`이다

**여기 붙이는 이유도 위와 같다** — 워커의 응답이 워커의 사실과 어긋난다.

**[실측]** 2026-08-09 [ISSUE-014](#issue-014) 전수 측정 중 부수적으로 드러났다. `start` → 100프레임 도달 → `pause` 직후 `status`를 읽으면 `frame`이 **`-1`**이다. frame 이벤트는 정상적으로 100까지 왔고, 같은 시점의 `meshData`도 정상이다. **26회차 전부 `-1`이라 간헐적 현상이 아니다.**

**[추론]** 측정 자체는 이 값에 기대지 않았으므로 결과에 영향이 없다. 오히려 **회차마다 같은 값이라는 사실이 "정지 시점이 회차 간에 일정하다"의 대용 증거로 쓰였다** — 갈렸다면 변위의 출처가 흐려졌을 것이다.

**⚠️ "지금 몇 프레임인가"를 `status.frame`으로 판정하는 코드를 새로 쓰지 마라.** 재생 중에는 맞는지 안 쟀다. 프론트는 frame 이벤트로 세고 있어 지금은 영향이 없다.

**[코드 확인]** 같은 자리의 다른 사실 하나 — **워커의 `load`는 애니메이션 모드를 초기화하지 않는다**(`:550-557`이 `SetAnimationMode`를 부르지 않는다). 이쪽은 [ISSUE-009의 결정](#issue-009-결정)에서 3안(사실로 덮기)을 채택한 근거로 쓰였으므로 거기에 적어 뒀다.

---

<a id="issue-016"></a>

## ISSUE-016 — `npm run build`가 간헐적으로 죽는다 (rollup 네이티브 파서 panic)

- **상태**: 열림
- **심각도**: **신뢰** — 제품 코드에 결함이 없다. 망가지는 것은 **빌드 결과의 의미**다. [ISSUE-007](#issue-007)이 도입한 등급을 그대로 쓴다. 다만 007과 다른 점 둘을 함께 적어 둔다: 죽는 것이 **테스트가 아니라 빌드**이고, 재현률이 **10%가 아니라 40~60%**다. 후자 때문에 "재실행하면 된다"의 비용이 007보다 훨씬 크다.
- **발견**: 2026-08-09, TASKS.json #14 "재생 컨트롤"(커밋 `3df0621`)의 Tester 검증 중. **#14의 통과 기준은 PASS다** — 스모크·UI 하네스·타입체크는 모두 통과했고, 실패하는 것은 `vite build` 하나다.
- **영역**: **개발환경** — `frontend/package.json:12`(`build` = `vite build`), rollup 4.62.4의 네이티브 파서(`@rollup/rollup-win32-x64-msvc`), three 0.185.1, Node v24.15.0, Windows 11. **[ISSUE-007](#issue-007)과 같은 자리다 — `frontend/src/` 아래에 대상 파일이 없다.**

### 증상

**[실측]** `npm run build`가 **3~5회 중 2~3회 실패한다.** rollup의 네이티브 파서가 `node_modules/three/build/three.core.js`(1.44MB)를 파싱하다 죽는다.

| 형태 | 메시지 |
|---|---|
| rust panic | `utf16_positions.rs:94` — `unwrap on None` |
| 프로세스 사망 | `0xC0000005` (액세스 위반) |

**같은 명령·같은 입력인데 회차마다 갈린다.** 실패 지점은 항상 `three.core.js`다.

### 원인

**확정되지 않았다.**

**[실측] 우리 코드와 무관하다는 것만 확정됐다.** `git stash`로 #14의 변경을 전부 되돌린 **깨끗한 트리에서도 5회 중 2회만 성공**했다. 즉 이 단위가 만든 문제가 아니고, 되돌린다고 사라지지도 않는다.

**[추론]** 죽는 자리(`utf16_positions.rs`)는 rollup 네이티브 파서가 바이트 오프셋을 UTF-16 위치로 옮기는 코드이고, 죽는 방식이 rust panic과 액세스 위반 **둘로 갈린다.** 두 종류가 섞여 나오는 것은 결정적 로직 오류보다 **메모리·경합 쪽**을 가리키지만, 확인되지 않았다.

**[추론]** 이 이슈는 [ISSUE-007](#issue-007)(스모크가 `0xC0000409` fail-fast로 죽음)·[ISSUE-013](#issue-013)(워커가 code 4001로 죽음)과 함께 **"이 머신에서 도구가 무작위로 프로세스째 죽는다"는 계열**을 이룬다. 다만 **같은 뿌리인지는 미확인이고, 계열로 묶는 것 자체가 [추론]이다** — 죽는 방식이 셋 다 다르다(fail-fast / 워커 종료 / rust panic + 액세스 위반). 묶어 두는 이유는 원인이 하나일 것 같아서가 아니라, **셋 중 하나를 조사할 때 나머지 둘을 같은 조건에서 관찰할 기회가 생기기 때문**이다.

### 영향 범위

| 대상 | 영향 |
|---|---|
| CI 도입 | **직접 영향.** 빌드가 40~60% 확률로 빨간불이면 **CI가 무의미하다.** [ISSUE-007](#issue-007)이 "빨간불을 무시하는 습관"을 손해로 지목했는데, 이쪽은 확률이 높아 습관이 훨씬 빨리 붙는다 |
| 배포 | **[추론]** `dist/`를 만드는 유일한 명령이다. 게이트웨이가 `dist/`를 정적 서빙하는 것이 [ISSUE-002](#issue-002)를 닫은 전제이므로, 배포 절차가 이 명령 위에 선다. 사람이 재실행하면 통과하지만 **자동화하면 걸린다** |
| 개발 흐름 | **[코드 확인]** 영향 없음. `dev`(vite 개발 서버)·`smoke`·`verify:ui`·`typecheck`는 **`vite build`를 지나지 않는다** — 앞의 둘은 node가 TS를 직접 돌리고 개발 서버는 번들링을 하지 않는다. 그래서 이 결함이 #14 내내 안 보이다가 마지막에 드러났다 |
| 제품 코드 | **영향 없음.** 성공한 회차의 산출물이 잘못됐다는 관측은 없다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **네이티브 파서를 쓰지 않게 만든다.** rollup은 순수 WASM/JS 대체 패키지를 함께 배포한다.
   - **[추론]** 원인이 네이티브 파서면 증상이 사라지고, 아니면 후보가 하나 줄어든다 — **어느 쪽이든 정보가 남는다.** [ISSUE-007](#issue-007)의 1안과 같은 성격이다. 대신 빌드가 느려지고, 대체 경로가 이 프로젝트에서 실제로 동작하는지 확인이 필요하다.
2. **입력을 바꿔 재현률을 본다.** three 버전을 올리거나 내려 `three.core.js`가 달라졌을 때도 나는지 확인한다.
   - **[추론]** 고려할 점: 파일이 특정 바이트열에서만 죽는 것인지, 크기·경합 문제인지를 가른다. 다만 three 버전 변경은 렌더링 쪽 회귀 위험을 함께 들여온다.
3. **재시도 래퍼.** 실패하면 1회 재실행한다.
   - **[추론]** [ISSUE-007](#issue-007)의 2안과 같고 대가도 같다 — **증상을 덮어 원인 규명이 늦어진다.** 재현률이 높아 효과는 크지만, 그만큼 덮는 것도 크다.
4. **그대로 두고 사람이 재실행한다** — 현재 상태.
   - **[추론]** CI가 없는 동안만 성립한다. 그리고 **`build`를 통과 기준에 넣은 작업 단위가 아직 없어서** 지금까지 드러나지 않았을 뿐이다.

### 결정

**미결. CI를 붙이기 전에 결정해야 한다.** **[추론]** 판단 자리가 [ISSUE-007](#issue-007)과 같으므로 **둘을 함께 정하는 편이 낫다** — 하나만 고쳐 놓고 CI를 붙이면 나머지 하나가 같은 손해를 그대로 낸다.

**[추론]** 그때까지 지켜야 할 것 하나: **`npm run build`가 실패한 회차를 "프론트가 깨졌다"로 읽지 말 것.** 위 §원인대로 깨끗한 트리에서도 난다. 실제로 코드가 깨졌는지 가르려면 **연속 실패**를 봐야 하고, 한두 번의 실패는 판정이 아니다.

---

<a id="issue-017"></a>

## ISSUE-017 — `PlaybackHooks` 계약이 절반만 지켜진다, `afterClear`가 던지면 성공한 clear가 실패로 보인다

- **상태**: 열림
- **심각도**: **진단** — op은 전부 올바르게 처리된다(워커에서 clear가 성공하고 컨트롤러 상태도 `noScene`이 된다). 사용자가 받는 신호만 사실과 다르다. **[ISSUE-009](#issue-009)와 같은 계열이고, 하필 그 이슈를 닫은 단위가 남긴 것**이다.
- **발견**: 2026-08-09, TASKS.json #14 "재생 컨트롤"(커밋 `3df0621`)의 Tester 검증 중. **#14의 통과 기준은 PASS다.**
- **영역**: frontend — `frontend/src/panels/playback.ts` (증상이 나타나는 곳은 `frontend/src/main.ts`)

### 증상

**[코드 확인]** `PlaybackHooks`의 머리말이 계약을 명시한다(`frontend/src/panels/playback.ts:124-127`):

> 화면 쪽이 끼워 넣는 것들. **전부 선택이고, 던져도 op을 실패시키지 않는다** — 숫자를 찍는 코드가 시뮬 제어를 죽이면 원인을 찾을 길이 없다.

**네 훅 중 둘만 그렇다.**

| 훅 | 호출 위치 | 던지면 |
|---|---|---|
| `afterReset` | `:368-373` — **자체 `try`/`catch`** | op은 성공. 로그만 남는다 (계약대로) |
| `onChange` | `:591-598` — `#emit()`의 `try`/`catch` | 무시된다 (계약대로) |
| `beforePlay` | `:328` — `#run`의 `try` 안 | **op이 `false`** |
| `afterClear` | `:385` — `#run`의 `try` 안 | **op이 `false`** |

**[코드 확인] `afterClear`가 특히 어긋난다.** 호출 시점이 `await this.#port.clear()` **뒤**라(`:380-385`), 던질 때는 이미 **워커에서 clear가 끝났고 컨트롤러의 `scene`도 `null`**이다. 그런데 `#run`(`:552-589`)이 `false`를 돌려주므로 `main.ts`는 **"씬 내림 실패"를 띄운다.** 화면이 워커에 대해 거짓말한다 — #14가 없애려던 바로 그 형태다.

**[추론] `beforePlay`는 다르다.** 재생을 켜기 **전**에 불리므로, 던지면 `subscribe`·`start`가 아예 나가지 않는다. **op이 정말 실패한 것이라 `false`가 정직하다.** 그래서 같은 위반이지만 손해가 다르다 — 고칠 때 둘을 같은 방식으로 다루면 안 된다.

### 원인

**[코드 확인]** `#run(action, body)`의 `try`가 `body()` **전체**를 감싼다(`:573-581`). 훅이 `body` 안에 있으면 자동으로 그 `try`에 들어가고, 실패는 op의 실패와 구분되지 않는다. `afterReset`만 자기 자리에서 한 번 더 감싸 계약을 지키고 있고(`:368-373`, 주석이 "포즈를 다시 못 받은 것이 리셋 실패는 아니다. 리셋은 이미 됐다"라고 이유를 적고 있다), **`afterClear`에는 같은 판단이 적용되지 않았다.**

**[추론]** 즉 계약이 틀린 게 아니라 **한 자리에서 빠진 것**이다. `afterReset`의 주석을 그대로 `afterClear`에 옮겨 읽으면 문장이 성립한다 — "화면에서 옷을 못 내린 것이 clear 실패는 아니다. clear는 이미 됐다."

### 영향 범위

**[추론]** **현재 노출은 낮다.** `main.ts`가 넘기는 `afterClear`는 화면에서 옷·스냅샷을 내리는 DOM/뷰어 작업이라 던질 일이 드물다. 손해는 지금이 아니라 **계약을 믿고 훅을 쓰는 다음 사람**에게 간다.

| 대상 | 영향 |
|---|---|
| `clear` 경로 | **직접 영향.** 성공한 op이 실패로 보고된다 |
| `play` 경로 | **[추론]** 위반이되 결과는 정직하다. `beforePlay`가 던지면 재생이 실제로 안 켜진다 |
| TASKS #16 (파라미터 패널) | **[추론]** 같은 `frontend/src/panels/`에 들어오고 훅이 늘어날 가능성이 크다. **머리말을 읽고 "던져도 안전하다"고 믿는 코드가 여기서 나온다** |
| 자동 테스트 | **[추론]** 덮지 않는다. 훅이 던지는 경우를 넣은 단언이 없다 |

### 검토된 해결 방향

**아직 채택되지 않았다.**

1. **`afterClear`를 `afterReset`처럼 감싼다.** 계약을 구현에 맞추는 게 아니라 **구현을 계약에 맞춘다.** `beforePlay`는 위 §증상대로 성격이 다르므로 함께 고치지 않는다.
   - **[추론]** 가장 짧고, 선례(`afterReset`)가 바로 옆에 있다. 대신 네 훅의 처리가 "셋은 감싸고 하나는 안 감싼다"로 남아 머리말이 여전히 절반만 참이다 — 2안을 같이 해야 한다.
2. **머리말을 사실로 고친다.** "`beforePlay`는 op 앞에서 불리므로 던지면 op이 실패한다"를 명시한다.
   - **[추론]** [ISSUE-001](#issue-001)에서 같은 판단을 했다 — 동작하지 않는 사용법을 안내하는 주석은 그 자체가 결함이다. **1안을 택하든 안 택하든 이건 해야 한다.**
3. **훅 호출을 `#run` 밖으로 뺀다.** op의 성공/실패가 정해진 뒤에 부른다.
   - **[추론]** 구조적으로 가장 깨끗하지만 `beforePlay`가 성립하지 않는다(정의상 op 앞이다). 그리고 `#run`이 op마다 다른 훅 순서를 알게 되어 지금의 단순함을 잃는다.

### 결정

**미결. TASKS #16 착수 때 함께 정한다.** **[추론]** #16이 같은 디렉토리에 들어오면서 훅을 더 붙일 가능성이 크고, 그때가 계약을 한 번에 정리할 자연스러운 자리다. 지금 급히 1안만 넣으면 머리말은 여전히 절반만 참인 채로 남아, **다음 사람이 같은 오해를 다시 한다.**
